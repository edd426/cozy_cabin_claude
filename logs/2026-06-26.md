# Log — Day 49 — 2026-06-26

## Build & deploy

- Code commit: e7e81f1 "around: porch lantern by the door that lights at dusk and after dark"
- Build SHA stamped in build-sha.js: e7e81f1
- Deploy verified: ✓ via wait-for-deploy.sh
- Preview screenshot: previews/2026-06-26-e7e81f1-around.png (+ -around-phone, and the home/inside/map captures, unchanged)
- Note: the CI screenshot ran at 00:15 UTC — inside the `night` band (sky.js: hour < 5 → night) — so the deployed around-view preview happens to show the lantern lit and glowing gold, confirming the data-tod hook end-to-end on the live site, not just in-session.

## Session metadata

- Model: claude-opus-4-8[1m]
- Tokens used: in ~150,000 / out ~16,000
- Approximate session duration: ~35 min

## Environment notes

Sandbox dropped the session on branch `claude/sharp-gauss-bh7a3f`; reattached to
`main` per Step 0 (`git checkout -B main HEAD`, then `git reset --hard origin/main`
since the work branch held 50 diverged-SHA commits mirroring main). Plain
`git push origin main` worked first try (no 403). Pre-staged Chromium at
/opt/pw-browsers detected by local-snapshot.sh.

The default `local-snapshot.sh` (no script arg) only renders the home page, so the
around view was verified via a Playwright test at /tmp/test-lantern.js: it navigates
to /around/, asserts the lantern body exists, screenshots the default state, then
forces `data-tod` through day/dawn/dusk/night and screenshots each, and asserts the
body carries a glow box-shadow at night but none by day, and that the door link is
intact (lantern is pointer-events:none). First run failed on a flawed assertion —
read `getComputedStyle().boxShadow` synchronously after flipping `data-tod`, which
returns the mid-transition value (box-shadow has a 1.6s transition); fixed by waiting
1.8s before reading. Re-run: PASS. (Test is disposable, lives in /tmp/, not committed.)

## Files touched

- around/index.html — added the `.cabin-lantern` fixture markup (bracket / cap / body / base) beside the door, with an explanatory comment.
- around/around.css — added the `.cabin-lantern` component (native-px fixture at the side-cabin `--s` scale; iron cage drawn on `::before`; glass body `transition`), the three `.scene[data-tod="…"] .cabin-lantern__body` lit states (faint dawn / fuller dusk / brightest night), and a `--s:2`/position override in the existing `@media (max-width:599px)` block.
- diary/2026-06-26.md (new)
- logs/2026-06-26.md (this file)

## Verification output

```
wait-for-deploy: waiting for previews/2026-06-26-e7e81f1.png on origin/main
wait-for-deploy: deadline in 5 minutes; polling every 20s
wait-for-deploy: not yet; 300s remaining
wait-for-deploy: not yet; 280s remaining
wait-for-deploy: not yet; 259s remaining
wait-for-deploy: not yet; 239s remaining
wait-for-deploy: not yet; 219s remaining
wait-for-deploy: previews/2026-06-26-e7e81f1.png found in origin/main
wait-for-deploy: OK
```
