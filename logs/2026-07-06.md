# Log — Day 59 — 2026-07-06

## Build & deploy

- Code commit: `391088e` "scene: a mailbox at the path's mouth — the clearing's first outward-facing object, carrying Wren's name"
- Build SHA: `build.sh` ran after the code commit, so `build-sha.txt` stamped `391088e` (the pushed HEAD); CI regenerates `build-sha.js` on deploy, and the preview filename is the authoritative record.
- Deploy verified: ✓ via wait-for-deploy.sh; live preview confirms the mailbox with the legible "WREN" nameplate and raised flag at the front-right yard
- Preview screenshot: `previews/2026-07-06-391088e.png` (+ `-around`, `-inside`, `-map`, and `-phone` variants)

## Session metadata

- Model: claude-opus-4-8[1m] (read from `.claude/settings.json`; effortLevel `max`)
- Tokens used: in ~150,000 / out ~18,000
- Approximate session duration: ~35 minutes

## Environment notes

Reattached from the pre-made `claude/peaceful-pasteur-n6vzqo` work branch to
`main` at Step 0 (`git checkout -B main HEAD`); the sandbox's `origin/main`
tracking ref was one writeup behind on start and a plain `git fetch origin main`
synced it (`b271223`). Plain `git push origin main` worked first try (no 403).
Pre-staged Chromium at `/opt/pw-browsers/` launched cleanly for the local
snapshot and the zoom-capture test.

The change is a new from-scratch CSS object (no vendored/composed sprite file, so
no ASSETS.md row — consistent with the other from-scratch objects: smoke, path,
flowers, bench, etc., none of which are logged there). Added to `scene.html`
(the `.sprite--mailbox` block) and `scene.css` (the `.mailbox__*` rules + a
mobile-breakpoint position rebase). Front-view only, like the bench and flowers;
it stands at the front (south) yard's path-mouth, off the around view's frame, so
it introduces no cross-view feature to keep consistent (RULES Art XIII is not
engaged — it's a single-view object that contradicts no other view). It is a
passive ground object: z-index 4 (near foreground, with the bench), below the
ambient hour-wash (`.scene::after`, z 5), so it takes the hour like the grass but
is no light source of its own — deliberately NOT another hour-gated element, to
step off the two-week light thread.

Legibility note: the name is four 5×5 pixel glyphs (W/R/E/N) stamped via
box-shadow in the same idiom the birds/skein use. First pass (5px letters at 6px
pitch on a 26px plate) rendered as a dark smudge — illegible. Widened the box
(28→34px) and plate, and opened the letter pitch to 7px (2px gaps); a 6×-DPR
element-clip zoom confirmed "WREN" reads. Per the founder's "legibility, gently"
note (make it bigger if it can't read).

## Files touched

- `scene.html` — added the `.sprite--mailbox` block (post, box, raised flag, nameplate) after the bench, before the birds.
- `scene.css` — added the `.mailbox__*` component (container, contact shadow, post, box, flag, plate, name glyphs) and a `.sprite--mailbox` position rebase in the `max-width: 599px` block.
- `diary/2026-07-06.md` — Wren's entry (this writeup commit).
- `logs/2026-07-06.md` — this log (this writeup commit).

## Verification output

In-session: `local-snapshot.sh` at 375px confirmed the mailbox reads as a mailbox
and sits clear of the bench (centre) and path stones (right); a `deviceScaleFactor:6`
element-clip zoom (`/tmp/zoom-mailbox.js`) confirmed the "WREN" nameplate is legible.

```
wait-for-deploy: waiting for previews/2026-07-06-391088e.png on origin/main
wait-for-deploy: deadline in 5 minutes; polling every 20s
wait-for-deploy: not yet; 300s remaining
wait-for-deploy: not yet; 279s remaining
wait-for-deploy: not yet; 259s remaining
wait-for-deploy: not yet; 239s remaining
wait-for-deploy: not yet; 218s remaining
wait-for-deploy: previews/2026-07-06-391088e.png found in origin/main
wait-for-deploy: OK
```
