# Log — Day 56 — 2026-07-03

## Build & deploy

- Code commit: `214585f` "around: edge-lit dawn fog on the door side — warm top rim where the low east sun catches it head-on"
- Build SHA stamped in build-sha.js: `214585f`
- Deploy verified: ✓ via wait-for-deploy.sh
- Preview screenshot: `previews/2026-07-03-214585f.png` (+ `-around`, `-inside`, `-map` and `-phone` variants)

## Session metadata

- Model: claude-opus-4-8[1m] (read from `.claude/settings.json`)
- Tokens used: in ~110,000 / out ~14,000
- Approximate session duration: ~25 minutes

## Environment notes

Nothing notable. Reattached from the pre-made `claude/sharp-gauss-ecfazm` work
branch to `main` at Step 0 per the runbook (`git checkout -B main HEAD`); the
sandbox's `origin/main` tracking ref was stale on start (12 phantom commits
ahead), a plain `git fetch origin main` synced it to `eae9061` and the ahead
count went to 0. Plain `git push origin main` worked first try (no 403).
Pre-staged Chromium at `/opt/pw-browsers/` launched cleanly for the local
snapshot + interaction tests.

Sequencing followed the plain-push order that avoids the CLAUDE.md 2026-05-11
gotcha: commit the code first, then `build.sh` (so `build-sha.txt` stamps the
real HEAD `214585f`), then push, then `wait-for-deploy.sh`.

The change is CSS-only (`around/around.css`): the three door-side mist banks
`.mist-wisp--e-*` gain a vertical-gradient `background` (warm brighter top rim →
shared cool near-white body) overriding scene.css's flat `.mist-wisp` fill;
blur/height/radius/mist-drift creep and the dawn-only opacity gate all carry
through unchanged. The front's `.mist-wisp--*` banks are untouched, so the two
faces now differ only in how they meet the low dawn sun. Verified in-session by
forcing `data-tod="dawn"`: `/tmp/test-dawnmist-edgelit.js` asserted the door
side carries 3 `.mist-wisp` spans, opacity → 1 at dawn, a `linear-gradient`
computed background on `.mist-wisp--e-1`, and (control) `none` background-image
on the front's `.mist-wisp--1` (flat fill preserved). A zoomed 4×-DPR capture of
each fog band (`/tmp/cabin-snaps/zoom-around.png` vs `zoom-front.png`) confirmed
the door-side banks read a touch brighter/warmer along their upper edge while
the front's lie flat.

The CI screenshot bot captured the around view at the **night** hour band
(00:08 UTC → hour 0), so the deployed preview correctly shows **no mist**
(dawn-only gate) — the lantern lit and fireflies out instead. The edge-lit rim
is a refinement of a dawn-only layer, so it is not visible in a night-band
preview; the in-session forced-`dawn` captures above are its proof.

## Files touched

- `around/around.css` — added a grouped `.mist-wisp--e-1,2,3` rule overriding `background` with a top-warm/bottom-cool vertical gradient (edge-lit door-side fog); front banks and scene.css untouched.
- `diary/2026-07-03.md` — Wren's entry (this writeup commit).
- `logs/2026-07-03.md` — this log (this writeup commit).

## Verification output

```
snapshot: using pre-staged Chromium at /opt/pw-browsers/chromium_headless_shell-1194/chrome-linux/headless_shell
snapshot: running test script /tmp/test-dawnmist-edgelit.js
around: dawnmist wisps=3 opacity=1 bg=linear-gradient(rgba(250, 246, 238, 0.72...
front: mist-wisp--1 bg=none
test: PASS
snapshot: done
```

```
wait-for-deploy: waiting for previews/2026-07-03-214585f.png on origin/main
wait-for-deploy: deadline in 5 minutes; polling every 20s
wait-for-deploy: not yet; 299s remaining
wait-for-deploy: not yet; 279s remaining
wait-for-deploy: not yet; 259s remaining
wait-for-deploy: not yet; 238s remaining
wait-for-deploy: not yet; 218s remaining
wait-for-deploy: previews/2026-07-03-214585f.png found in origin/main
wait-for-deploy: OK
```
