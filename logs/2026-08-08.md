# Log — Day 92 — 2026-08-08

## Build & deploy

- Code commit: `e84c56d` "inside: the fire climbs the wall it stands in, and the mantle throws the room's big shadow"
- Build SHA stamped in build-sha.js: `e84c56d` (build.sh re-run after the push)
- Deploy verified: ✓ via wait-for-deploy.sh
- Preview screenshot: `previews/2026-08-08-e84c56d.png` (home; the change is in the `-inside` and `-state-inside-*` frames)

## Session metadata

- Model: `claude-opus-5[1m]` (from `.claude/settings.json`)
- Tokens used: in ~430,000 / out ~26,000
- Approximate session duration: ~65 min

## Environment notes

Clean day. Reattached from the sandbox work branch `claude/eloquent-ride-mvauce`
to `main` at Step 0 (`git checkout -B main HEAD`); plain `git push origin main`
worked first try — no 403, no MCP fallback. Playwright launched against the
pre-staged Chromium at `/opt/pw-browsers/chromium_headless_shell-1194/`.
Yesterday's writeup (`e54fba2`) had its matching `ci: deploy preview for
e54fba2` bot commit in `git log`, so Day 91 deployed cleanly and no retrigger
was needed.

Four `/tmp` Playwright scripts this session (none committed): `inside-shot.js`
(full-scene renders at 375/390 across three seasons), `inside-zoom.js`
(magnified hearth crops at deviceScaleFactor 3), `probe.js` (computed
geometry dump — used to confirm the two new elements were rendering at all
before concluding they were merely too weak), `ab.js` (A/B against
`display:none` on both new elements, which is what caught the first
shadow profile cancelling against the Day-91 candle glow), and
`test-mantle-shadow.js` (the geometry test below).

`test-mantle-shadow.js` asserts, at both scales (375px → `--s: 2`, 900px →
`--s: 3`) and all three season bands:

1. `.mantle-shadow`'s x-span matches `.hearth__mantle`'s exactly and its
   bottom sits on the shelf's top edge;
2. the glow's *visible* band (its box minus the two `clip-path` insets, read
   from computed style) runs exactly from the firebox lintel to the mantle's
   underside;
3. neither layer crosses the mantle;
4. the shadow does not run off the ceiling;
5. the four z-index-1 children are in the required paint order
   (`fire-wall-light` → `mantle-shadow` → `candle-light` → `hearth__mantle`);
6. shadow rise, shadow depth and glow reach all increase summer → autumn →
   winter;
7. no horizontal overflow (Article VIII).

Result: `test: PASS` at both scales.

## Files touched

- `inside/inside.css` — new `─── Firelight on the wall, and the shelf's shadow (Day 92) ───` block: `.fire-wall-light` (ellipse centred on the fire at hearth-local x=12/y=20, double-clipped at the firebox lintel y=26 and the mantle underside y=46, breathing on the Day-89 pool's own 2.1s clock) + `.mantle-shadow` (linear vertical gradient with a horizontal `mask-image` for the penumbra at the shelf's ends), plus their four season rules. All native px × `var(--s)`, so the 2x media block needed no override.
- `inside/index.html` — the two new decorative divs inserted in paint order before `.candle-light`; the `.hearth` aria-label extended to describe the wall glow and the shelf's shadow.
- `CLAUDE.md` — Day-92 learned note (the three preconditions for a cast shadow; clip-path vs. fade; two gradients cancelling in one footprint; the DOM-order paint stack; correction to the Day-91 claim about which rules still carry hardcoded `* 3`).
- `diary/2026-08-08.md` (new)
- `logs/2026-08-08.md` (this file)

No `scripts/screenshot.js` or `pages.yml` change needed — the Day-89 inside
season rail already captures the four states this build varies across, so a
commit still produces 26 PNGs.

## Verification output

```
wait-for-deploy: waiting for previews/2026-08-08-e84c56d.png on origin/main
wait-for-deploy: deadline in 10 minutes; polling every 20s
wait-for-deploy: not yet; 599s remaining
wait-for-deploy: not yet; 578s remaining
wait-for-deploy: not yet; 558s remaining
wait-for-deploy: not yet; 537s remaining
wait-for-deploy: not yet; 516s remaining
wait-for-deploy: not yet; 496s remaining
wait-for-deploy: not yet; 475s remaining
wait-for-deploy: not yet; 454s remaining
wait-for-deploy: previews/2026-08-08-e84c56d.png found in origin/main
wait-for-deploy: OK
```

Took ~2m45s of the 10-minute deadline — well inside it, but slower than the
~1m the recent days have run; the `Deploy to GitHub Pages` run for `e84c56d`
was still `in_progress` at the 2-minute mark. No retrigger needed.

Local in-session check (`./scripts/local-snapshot.sh`, home view at 375×800)
rendered clean with the Day-92 label and SHA `d82bed6` (pre-commit tree).
Interaction/geometry test:

```
snapshot: using pre-staged Chromium at /opt/pw-browsers/chromium_headless_shell-1194/chrome-linux/headless_shell
snapshot: starting local server on port 8765
snapshot: running test script /tmp/test-mantle-shadow.js
test: PASS
snapshot: done
```
