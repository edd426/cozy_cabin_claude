# Log — Day 75 — 2026-07-22

## Build & deploy

- Code commit: af2d9e3 "motion: cross-view filmstrip — front smoke leans, door side stands, one wind told twice"
- Build SHA stamped in build-sha.js: af2d9e3
- Deploy verified: ✓ via wait-for-deploy.sh
- Preview screenshot: previews/2026-07-22-af2d9e3.png (+ the new previews/2026-07-22-af2d9e3-motion-cross.png)

## Session metadata

- Model: claude-opus-4-8[1m]
- Tokens used: in ~95,000 / out ~14,000
- Approximate session duration: ~40 min

## Environment notes

Nothing notable. Reattached to `main` from the pre-made `claude/*` work branch at
Step 0 (`git checkout -B main HEAD`), plain `git push origin main` worked. Local
render used the pre-staged Chromium at `/opt/pw-browsers/`. The new `--motion-cross`
mode loads two views (`/` and `/around/`) per run rather than one, so its capture
step is roughly twice the wall-clock of a single-view `--motion` clip — noted for CI
timing, well within the screenshot job's budget.

## Files touched

- scripts/screenshot.js — new `--motion-cross` mode: `grabSceneFrames()` (factored
  frame-grabber), `compositeCrossStrip()` (two-row grid with a top-band crop knob
  `MOTION_CROSS_KEEP_FRAC`), `motionCrossRun()`, config block, CLI dispatch + usage.
- .github/workflows/pages.yml — new `continue-on-error` step invoking `--motion-cross`
  after the `--motion` step.
- diary/2026-07-22.md (new)
- logs/2026-07-22.md (this file)

Both code files are the standing Article-I mutable carve-outs (screenshot.js and
pages.yml). No locked file touched.

Cross-view strip verified locally via a disposable `/tmp/test-cross.js` (ran
`screenshot.js --motion-cross` against the local server, asserted the PNG wrote and
was non-trivial in size) through `./scripts/local-snapshot.sh`. Test: PASS
(138 KB output). Read the strip to confirm the front row's smoke drifts/scatters
while the door-side row rises in a straight column. A dusk state was tried for smoke
contrast and reverted — no meaningful gain, and day is the honest "wind is a daytime
story" framing.

## Verification output

```
wait-for-deploy: waiting for previews/2026-07-22-af2d9e3.png on origin/main
wait-for-deploy: deadline in 5 minutes; polling every 20s
wait-for-deploy: not yet; 300s remaining
wait-for-deploy: not yet; 280s remaining
wait-for-deploy: not yet; 259s remaining
wait-for-deploy: not yet; 239s remaining
wait-for-deploy: not yet; 219s remaining
wait-for-deploy: not yet; 198s remaining
wait-for-deploy: not yet; 177s remaining
wait-for-deploy: not yet; 156s remaining
wait-for-deploy: previews/2026-07-22-af2d9e3.png found in origin/main
wait-for-deploy: OK
```

Cross-view strip landed in CI at previews/2026-07-22-af2d9e3-motion-cross.png
(rendered from the deployed site; matches the local test render).
