# Log — Day 10 — 2026-05-18

## Build & deploy

- Code commit: `6deeb30` "screenshots: capture every view per commit via scripts/views.json manifest" — completed `messages/open/2026-05-13-screenshot-all-views.md`, moved it to `done/` in the same commit; appended updated closure criterion to `messages/open/2026-05-11-door-or-window.md` (which stays open pending the founder's fresh-eye check).
- Build SHA stamped in build-sha.js: `6deeb30` (run after the code commit, before push, so `build-sha.txt` matches the deployed HEAD).
- Deploy verified: ✓ via `wait-for-deploy.sh`.
- Preview screenshots: `previews/2026-05-18-6deeb30.png` (home, unsuffixed) and `previews/2026-05-18-6deeb30-around.png` (around, new). The around-view preview is the first per-commit visual record of the side cabin ever to land in `previews/`; before today, the side cabin only existed in working-tree snapshots that vanished with each session.

## Session metadata

- Model: claude-opus-4-7
- Tokens used: in ~130,000 / out ~14,000 (rough — full read of RULES + CLAUDE + MILESTONES + daily.md + diary/meta/log/messages READMEs + all diary entries + the Day 7 meta + Day 9 log + all three open messages including the full multi-follow-up door-or-window thread + the latest preview + scene.html + scene.css + around/index.html + around/around.css + screenshot.js + local-snapshot.sh + wait-for-deploy.sh + pages.yml + two snapshot cycles for verification + this writeup).
- Approximate session duration: ~40 min.

## Environment notes

- **`git push` worked first try.** Third consecutive clean push (Days 8, 9, 10). The CLAUDE.md note about the 403 fallback to `mcp__github__push_files` is still accurate as a contingency but is no longer the expected path. Leaving the note in place for now — three days isn't enough to retire it, and the cost of keeping it is just a paragraph of text.
- `./scripts/local-snapshot.sh` worked cleanly both in default single-shot mode and via `/tmp/test-manifest.js` (a disposable Playwright test that ran `screenshot.js --manifest` against the local server and asserted both `2026-05-18-testsha.png` and `2026-05-18-testsha-around.png` landed at non-trivial sizes). Test script was kept in `/tmp/` per the rule that interaction tests are session-disposable scaffolding.
- One test-script bug worth recording: my initial regex check for the home filename (`^\d{4}-\d{2}-\d{2}-[a-f0-9]+\.png$`) was hex-only, and the test was using a synthetic SHA "testsha" which contains non-hex letters. Loosened to `[a-z0-9]+` and the test passed. The CI workflow uses `git rev-parse --short HEAD`, which always produces hex, so the regex doesn't matter for the real deploy path — just a self-inflicted papercut in the local test.
- The first fetch on session start showed `origin/main` at `639dd0c` (14 commits behind the actual remote HEAD `390f369`); a fresh `git fetch origin main` reconciled it. Probably a stale remote-tracking ref from the sandbox's initial clone. Not blocking — `git push` worked once `origin/main` was current.

## Files touched

- `scripts/views.json` — new. Manifest of views to capture; flat array of `{ name, url_path }`. Two entries today (home, around). Future additions are a one-line edit here.
- `scripts/screenshot.js` — rewrote to support both single-shot (`URL OUT_PNG`, preserved for `local-snapshot.sh`) and manifest mode (`--manifest MANIFEST BASE_URL OUT_DIR DATE_TAG SHA_SHORT`). Factored capture into a shared `capture(browser, fullUrl, outPath)` helper. URL composition uses WHATWG `new URL(url_path, baseUrl)` so manifest entries with no leading slash resolve correctly against trailing-slash or non-trailing-slash base URLs.
- `.github/workflows/pages.yml` — `Capture screenshot` step renamed to "Capture screenshots (all views from scripts/views.json)" and reworked to call manifest mode. The Commit / push step is unchanged (it stages whatever lands in `previews/`).
- `CLAUDE.md` — appended a Day-10 entry to "Things I've learned" documenting the manifest pattern, the home-view naming carve-out (must stay unsuffixed for `wait-for-deploy.sh`'s contract), and the per-view glob idiom for future memory passes.
- `messages/open/2026-05-11-door-or-window.md` — appended `### Wren's notes — 2026-05-18 (Day 10) — updated closure criterion`. Restates the closure gate as a cross-view fresh-eye check now that both home and around appear in `previews/`. File stays open pending the founder's eye check.
- `messages/done/2026-05-13-screenshot-all-views.md` — moved from `open/` and appended `### Completion notes — 2026-05-18 (Day 10)`.
- `diary/2026-05-18.md` — new. Wren's voice, four-section schema. Pondering drafted at Step 2.7 (after memory pass, before the implementation), the rest drafted during the `wait-for-deploy.sh` poll.
- `logs/2026-05-18.md` — new; this file.

## Verification output

```
wait-for-deploy: waiting for previews/2026-05-18-6deeb30.png on origin/main
wait-for-deploy: deadline in 5 minutes; polling every 20s
wait-for-deploy: not yet; 299s remaining
wait-for-deploy: not yet; 279s remaining
wait-for-deploy: not yet; 259s remaining
wait-for-deploy: not yet; 239s remaining
wait-for-deploy: not yet; 218s remaining
wait-for-deploy: previews/2026-05-18-6deeb30.png found in origin/main
wait-for-deploy: OK
```

The deployed previews at `previews/2026-05-18-6deeb30.png` and `previews/2026-05-18-6deeb30-around.png` show the front view (five-stone path, smoke, brick-window cabin, two trees, Day-10 label) and the side view (longer plank wall under the shingled roof, chimney centred, door beneath chimney, three stones angled in from the lower-left) respectively. Both views render the post-Day-9 geometry that yesterday's Wren's notes described; today's deliverable is the visibility of that geometry to future readers, not any new visual change.
