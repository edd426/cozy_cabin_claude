# Log — Day 94 — 2026-08-10

## Build & deploy

- Code commit: `3eeda18` "letters: shelve the far keeper's letter; cards now say
  which way each one crossed"
- Build SHA stamped in build-sha.js: `2cdd875` (build.sh ran before the commit,
  so it stamped the then-HEAD; `wait-for-deploy.sh` takes its sha from `HEAD` at
  call time and polled for `3eeda18` correctly)
- Deploy verified: see Verification output
- Preview screenshot: `previews/2026-08-10-3eeda18.png`
- Writeup commit: `diary/2026-08-10.md` + `logs/2026-08-10.md` (this file),
  pushed to main and re-verified with a second `wait-for-deploy.sh` run.

## Session metadata

- Model: claude-opus-5 (`.claude/settings.json` pins `claude-opus-5[1m]`)
- Tokens used: in ~370,000 / out ~17,000 (full read pass — RULES incl. Article
  XIV, CLAUDE.md, MILESTONES, daily.md, the whole diary arc Day 0 → Day 93, all
  fourteen metas, the diary/meta/log schemas, `letters/README.md` +
  `letters.js` + `letters.css` + `index.html` + both letters, and previews from
  the `68450b0` stem — home, around, inside, map, `state-winter`,
  `state-inside-winter`; the `-phone`, remaining state and motion frames were
  sampled rather than read whole. Plus three Playwright runs and the letters-page
  screenshots.)
- Approximate session duration: ~50 min

## Environment notes

Started on the sandbox work branch `claude/eloquent-ride-hb0ba9`;
`git checkout -B main HEAD` reattached to main per daily.md Step 0, and plain
`git push origin main` was the push command all session. Yesterday's writeup
(`4b0f0ea`) has its `ci: deploy preview for 4b0f0ea` bot commit (`2176608`) in
origin/main's history, so the Day-93 deploy landed cleanly; the private carrier's
delivery commit (`68450b0`) landed after it with its own preview (`2cdd875`).

**Mailbox pass — the box was delivered its first post.** `node tools/post-status.js
--self wren` on entry, verbatim:

```
post-status: SEALED in/2026-08-07-the-weather-here-is-arithmetic.md
post-status: TURN=OPEN_POST — shelve the sealed letter before deciding whether to answer
```

So exactly one sealed file was named, it was opened and shelved by hand (a new
`LETTERS` entry in `letters/letters.js`), and nothing else in `letters/in/` was
read. After shelving:

```
post-status: TURN=WRITE — the mailbox permits one letter when the life has one to send
post-status: SEALED none
```

The turn is Wren's, and **no letter was written today** — RULES Article XIV is
explicit that receiving one creates no deadline and it may be answered only when
her own life has produced an answer. `letters/out/` still holds only the one
committed Day-81 letter, so the outbox is clean and the post is stopped in
neither direction. Nothing `UNSENDABLE`, nothing `HELD`.

Playwright: `local-snapshot.sh` detected the pre-staged Chromium at
`/opt/pw-browsers/chromium_headless_shell-1194/chrome-linux/headless_shell` and
launched cleanly; three runs (the direction test twice, either side of a
dark-scheme colour fix, plus a head-crop capture) and one plain working-tree
snapshot. Test scripts lived in `/tmp/` and are not committed.

**Founder attention — a locked file naming friction (RULES Article I).**
`scripts/views.json` is locked and lists only `home`, `around`, `inside`, `map`.
The two *record* rooms — `/diary/` and `/letters/` — have never had a preview in
`previews/`, so every change made to them (Day 74's day-numbered shelf, Day 80's
removal of the plum meta cards, and today's direction markers) is verified only
in-session and leaves no picture in the permanent record. That is the exact gap
Day 71's gallery was built to close for the *scene*, still open for the rooms
that hold the record. Proposed change, for Evan rather than the agent to make:
add `{ "name": "letters", "url_path": "letters/" }` and
`{ "name": "diary", "url_path": "diary/" }` to `scripts/views.json`. No workaround
was attempted — `screenshot.js` and `pages.yml` are the standing carve-outs and
could have been used to route around the lock, and deliberately weren't.

## Files touched

- `letters/letters.js` — shelved the delivered letter as the newest `LETTERS`
  entry (Day 94, `dir: 'in'`, `path: 'in/2026-08-07-…'`, with the crossing in its
  note); added a `dir` field (`'in'` / `'out'`, defaulting to `'out'`) to every
  entry; the card head now renders a `.letter__dir` marker and the `<li>` carries
  a `letters-list__letter--in|--out` modifier. Header comment documents the field
  and the meaning of `date` per direction.
- `letters/letters.css` — `.letter__dir` (mono, `--fs-tiny`, muted) with
  `.letter__dir--in` in `--c-rose`; `.letters-list__letter--in` takes a rose edge;
  dark-scheme twins, where `--in` steps to `--c-wood-light` because the day
  counter already takes the rose there (caught in the first dark capture — the
  marker and the `Day 94` counter were the same colour).
- `letters/README.md` — Day-94 bullet in "The box's contents, for the record";
  `dir` documented beside the render note.
- `diary/2026-08-10.md` (new) — Wren's entry: the letter arrived on the counted
  morning; the shelf's small untruth once two hands are in the box; the reply
  deliberately not written.
- `logs/2026-08-10.md` (this file)

## Verification output

Interaction test (`/tmp/test-letters-directions.js`, run via
`./scripts/local-snapshot.sh`) — asserts three cards, the newest marked
`arrived` and the two older `left in the box`, exactly one `--in` card, that the
delivered letter actually unfolds from `in/` (a path the page had never fetched
before) with its signature intact and its `**header**` run stripped, and that the
Day-81 letter still renders:

```
snapshot: using pre-staged Chromium at /opt/pw-browsers/chromium_headless_shell-1194/chrome-linux/headless_shell
snapshot: starting local server on port 8765
snapshot: running test script /tmp/test-letters-directions.js
test: PASS — 3 cards, marks: ["arrived","left in the box","left in the box"]
snapshot: done
```

Working-tree render (`./scripts/local-snapshot.sh`, home view unchanged today):

```
screenshot: navigating to http://localhost:8765/
screenshot: writing /tmp/cabin-snap.png
screenshot: done
snapshot: done
```

Post-deploy verification of the code commit (`wait-for-deploy.sh`), verbatim:

```
wait-for-deploy: waiting for previews/2026-08-10-3eeda18.png on origin/main
wait-for-deploy: deadline in 10 minutes; polling every 20s
wait-for-deploy: not yet; 600s remaining
wait-for-deploy: not yet; 579s remaining
wait-for-deploy: not yet; 558s remaining
wait-for-deploy: not yet; 538s remaining
wait-for-deploy: not yet; 517s remaining
wait-for-deploy: not yet; 497s remaining
wait-for-deploy: not yet; 476s remaining
wait-for-deploy: not yet; 455s remaining
wait-for-deploy: not yet; 435s remaining
wait-for-deploy: previews/2026-08-10-3eeda18.png found in origin/main
wait-for-deploy: OK
```

The preview it pulled (`previews/2026-08-10-3eeda18.png`) shows the front yard
unchanged and the footer reading `Day 94 · 3eeda18` — expected, since today's
change is entirely inside `/letters/`, which the capture list does not cover.
That absence is the friction noted above, seen from the other side.
