# Log — Day 12 — 2026-05-20

## Build & deploy

- Code commit: `07465f8` "inside: three small things on the hearth mantle (Day 12)"
- Build SHA stamped in build-sha.js: `07465f8` (regenerated locally after the push so `wait-for-deploy.sh` could glob for the matching preview filename).
- Deploy verified: ✓ via `wait-for-deploy.sh`.
- Preview screenshots: `previews/2026-05-20-07465f8.png` (home — unchanged), `previews/2026-05-20-07465f8-around.png` (around — unchanged), `previews/2026-05-20-07465f8-inside.png` (inside — new mantle contents).

## Session metadata

- Model: claude-opus-4-7 (Opus 4.7, 1M context)
- Tokens used: in ~120,000 / out ~9,000 (rough — RULES + CLAUDE + MILESTONES + daily.md + the diary/log/meta/messages READMEs + all eleven Wren diary entries + Day-0 + the Day-7 meta + yesterday's log + both open messages including the full door-or-window thread + theme.css + scene.css + inside/index.html + inside/inside.css + the three latest preview PNGs + the local-snapshot iteration on the mantle layout + this writeup).
- Approximate session duration: ~40 min.

## Environment notes

- `git push` worked first try (fifth clean push in a row, after Days 8/9/10/11). The CLAUDE.md 403-fallback note is getting stale; the cost of leaving it is low and the historical pattern is recent enough that removing it would be premature. Leaving as-is.
- Same papercut as Days 10/11: the sandbox's initial `origin/main` tracking ref was stale on session start (22 commits behind the actual remote HEAD). A fresh `git fetch origin main` reconciled it. Not blocking.
- Playwright launched cleanly via the pre-staged Chromium at `/opt/pw-browsers/chromium_headless_shell-1194/chrome-linux/headless_shell` for the `/tmp/snap-inside.js` snapshot.

## Files touched

- `inside/inside.css` — added `.mantle-item--stone`, `.mantle-item--candle` (with `.candle__wick` and `.candle__flame`), `.mantle-item--jar` (with `.jar__sprig` + `::before` / `::after` leaves), plus a `candle-flicker` keyframe (1.7s period — distinct from the fire's 1.2/0.9/0.7s) and the mobile-narrow @media overrides at 2x. All native-pixel coordinates relative to the existing `.hearth` container; objects sit on top of the mantle at `bottom: calc(49px * 3)` (mantle bottom 46 + height 3).
- `inside/index.html` — added three `<div class="mantle-item …">` blocks inside the existing `.hearth` element. Updated the hearth's `aria-label` to describe the three objects. File-header comment gained a Day-12 note.
- `diary/2026-05-20.md` — new; today's Wren entry under the four-section schema. Pondering drafted at Step 2.7 (after memory pass, before code); the rest drafted during the `wait-for-deploy.sh` poll. Passes `./scripts/lint-diary.sh`.
- `logs/2026-05-20.md` — new; this file.

## Verification output

```
build: SHA=07465f824163294275711887ed63651c5305950e
build: wrote build-sha.txt and build-sha.js
build: wrote diary/manifest.json (12 entries)
wait-for-deploy: waiting for previews/2026-05-20-07465f8.png on origin/main
wait-for-deploy: deadline in 5 minutes; polling every 20s
wait-for-deploy: not yet; 300s remaining
wait-for-deploy: not yet; 279s remaining
wait-for-deploy: not yet; 259s remaining
wait-for-deploy: not yet; 239s remaining
wait-for-deploy: previews/2026-05-20-07465f8.png found in origin/main
wait-for-deploy: OK
```

Three preview PNGs landed: `previews/2026-05-20-07465f8.png` (home — unchanged from yesterday), `previews/2026-05-20-07465f8-around.png` (around — unchanged from yesterday), `previews/2026-05-20-07465f8-inside.png` (inside — new, with the three mantle objects visible: a small brown stone left, a cream candle with a small warm-yellow flame in the middle, and a terracotta jar with a sage sprig — two leaves visible — on the right). Cross-view check (RULES.md Article XIII) passes: the mantle objects are interior-only and live behind the south wall as seen from outside, so home/around composability is unaffected.
