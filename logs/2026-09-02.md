# Log — Day 117 — 2026-09-02

## Build & deploy

- Message close (first commit of session): `cd6c487` "messages: read and close the first-turn-of-the-season note" — read-and-close FYI per `messages/README.md` step 5, no completion notes.
- Code commit: `3688fd7` "hold the record to the clearing: sweep for what no kept frame shows"
- Build SHA stamped in build-sha.js: `cd6c487…` — **stamped early**, see Environment notes. Harmless (CI runs `build.sh` itself at deploy time and `build-sha.js` is gitignored), and `wait-for-deploy.sh` reads `git rev-parse --short HEAD` at invocation rather than `build-sha.txt`, so it polled for the right preview.
- Deploy verified: ✓ via wait-for-deploy.sh (found on the second poll). Both CI jobs green on run [33575023693](https://github.com/edd426/cozy_cabin_claude/actions/runs/33575023693), including the new **"Hold the record to the clearing"** step running against the deployed site.
- Preview screenshot: `previews/2026-09-02-3688fd7.png` (+ `-around`, `-inside`, `-map`, the five record rooms, **24** forced states and 5 motion strips — **42 PNGs**, up from 38; the four new ones are `-state-around-{dawn,dusk,night,winter-night}.png`).
- Writeup commit: see below; verified with a second `wait-for-deploy.sh` run per daily.md Step 9.

**No pixel of the yard changed today.** The whole change is to the record and the guard over it: `tools/check-gallery.js`, four new gallery states, a `module.exports` on `scripts/screenshot.js`, and one CI step. `/tmp/cabin-snap.png` is byte-for-byte the same clearing as yesterday's.

## Session metadata

- Model: `claude-opus-5[1m]` (read from `.claude/settings.json`)
- Tokens used: in ~430,000 / out ~30,000
- Approximate session duration: ~90 minutes

## Environment notes

- Sandbox dropped the session on `claude/eloquent-ride-oxd6hf`; `git checkout -B main HEAD` reattached as daily.md Step 0 requires. The local `origin/main` tracking ref was stale on start (`git status` claimed 6 commits ahead) — `git fetch origin main` cleared it, exactly as Step 0's recipe says. Plain `git push origin main` worked first try; no 403.
- `local-snapshot.sh` found the pre-staged Chromium at `/opt/pw-browsers/chromium_headless_shell-1194/chrome-linux/headless_shell` on every run.
- **`build.sh` ran before the commit**, so `build-sha.txt` holds the *message-close* SHA rather than the code commit's. This is the inverse of the Day-3 MCP gotcha and it is benign for the same reason: CI regenerates `build-sha.js` from its own checkout at deploy time, and the file is gitignored. Worth knowing that the daily.md Step 5 order (`build.sh` → `git add -A` → commit) *always* stamps the previous commit when the session has an earlier commit in it — which every message-close day does.
- `tools/check-almanac.js` was re-run once as a regression after the deploy-path edits (~3 min; a Bash call needs ≥5 minutes of timeout, as the Day-113/115/116 logs warned). All 46 claims held.
- `tools/check-gallery.js` is cheap: ~15s locally, and **8 seconds in CI** against the live site (`00:27:56 → 00:28:04` on run 33575023693), against `check-almanac`'s 2m40s in the same job. The cost is low because motion is disabled and all sixteen states are forced in one DOM rather than one page-load each; see CLAUDE.md.
- In-session runner shim for both checks: a two-line `/tmp/*.js` that `spawnSync`s `node tools/check-*.js $COZY_CABIN_URL`, executed via `./scripts/local-snapshot.sh /tmp/<shim>.js`. Same recipe as the Day-98 note.
- Dry-running `screenshot.js --gallery` locally worked unchanged after the `require.main === module` guard — 24 frames in about 50s.
- **`previews/` is at 260 MB**, growing ~3.2 MB per commit (≈6.4 MB/day at the standard two commits). Today's four new states add ~225 KB/commit, about 7% of a commit's picture weight. Not a problem and not raised as one — the founder flagged growth as acceptable on Day 71 — but this is the first day the total has been written down, and a future agent looking for something to trim should know where the weight is (the `map` view's two captures are ~200 KB each; the inside states are the cheapest at ~35–46 KB).

## Files touched

- `tools/check-gallery.js` — **new.** Holds the record to the clearing on one claim: everything drawn here shows in at least one kept frame. Sweeps every element and both pseudo-elements of every `.scene`, on home/around/inside, through all sixteen (season, band) states; reads the kept frames off `GALLERY_STATES` (clock states resolved via `window.CabinSky.phaseFor` / `window.CabinSeason.seasonForDate`); exits non-zero naming the layer and the states it shows in. Header comment carries both blind spots — presence-not-colour, and thing-not-state.
- `scripts/screenshot.js` (standing Article-I carve-out) — `module.exports = { GALLERY_STATES, GALLERY_VIEW, GALLERY_INSIDE_VIEW, GALLERY_AROUND_VIEW }` and the main IIFE wrapped in `if (require.main === module)`, so the checker reads the rails off the camera that takes them (the Day-97 export move); the four door-side hour states; header comment updated, and the Day-112 sentence declining that rail quoted in place rather than deleted.
- `.github/workflows/pages.yml` (standing Article-I carve-out) — new "Hold the record to the clearing" step, last, after the previews are pushed, no `continue-on-error`. Same flags and same reasons as the almanac check beside it.
- `messages/done/2026-09-01-first-turn-of-the-season.md` — moved from `open/`.
- `CLAUDE.md` — Day-117 learned entry.
- `diary/2026-09-02.md`, `logs/2026-09-02.md` — this writeup.

`scripts/views.json` needed no change (the gallery doesn't read the manifest), and no locked file was touched — `git status` before the code commit listed exactly the four paths above.

## Verification output

**The survey, run before anything was written** (Day-103 discipline: a guard aimed at places you chose can only catch what you already imagined, so sweep first and find out what is actually there). This asked a narrower question than the committed check — which corners are not *predicted by their two arms* — and it is what established that the cross is a fair bargain almost everywhere:

```
=== home (339 readings per state)
  spring|dawn: predicted by its arms
  spring|dusk: predicted by its arms
  spring|night: predicted by its arms
  autumn|dawn: predicted by its arms
  autumn|dusk: predicted by its arms
  autumn|night: predicted by its arms
  winter|dawn: predicted by its arms
  winter|dusk:
      shows, neither arm does: div.sprite.sprite--winterstars
  winter|night:
      shows, neither arm does: div.sprite.sprite--winterstars
```

Nine of the sixteen states on each of three views, and **the only joint gate in the whole clearing is the winter stars** — which is the corner Day 84 added by hand. The rule reproducing the one hand-made corner is what argued it was the right rule.

**First run of the committed check**, which asks the stronger question (is it in the record *at all*, by any frame) and went red on the door side:

```
check-gallery: home keeps autumn|day, spring|day, summer|dawn, summer|day, summer|dusk, summer|night, winter|day, winter|night
check-gallery: around keeps autumn|day, spring|day, winter|day

HELD  home — every drawn thing shows in a kept frame (339 readings × 16 states)
BROKE around — 8 thing(s) the record never shows
        ✗ div.scene.scene--around::after
          shows only at: spring|dawn, spring|dusk, spring|night, summer|dawn, summer|dusk, summer|night, autumn|dawn, autumn|dusk, autumn|night, winter|dawn, winter|dusk, winter|night
        ✗ span.hill.hill--e-far-r::before
          shows only at: spring|dawn, spring|dusk, summer|dawn, summer|dusk, autumn|dawn, autumn|dusk, winter|dawn, winter|dusk
        ✗ span.hill.hill--e-far-c::before   (… and -near-r, -near-c, same states)
        ✗ div.sprite.sprite--winterstars
          shows only at: winter|dusk, winter|night
        ✗ div.sprite.sprite--dawnmist
          shows only at: spring|dawn, summer|dawn, autumn|dawn, winter|dawn
        ✗ div.sprite.sprite--fireflies
          shows only at: summer|dusk, summer|night
HELD  inside — every drawn thing shows in a kept frame (270 readings × 16 states)

check-gallery: FAIL — 8 thing(s) drawn in this clearing that no kept picture shows.
```

**Break-tested twice, and both results changed the design** (Day-98 rule: a check that cannot fail is decoration).

*The break that fired* — `.sprite--leaffall` gated to `autumn` AND `dusk` instead of `autumn`, i.e. an existing layer becoming joint-gated out of coverage:

```
BROKE home — 5 thing(s) the record never shows
        ✗ div.sprite.sprite--leaffall
          shows only at: autumn|dusk
        ✗ span.leaf.leaf--1  … leaf--4
          shows only at: autumn|dusk
check-gallery: FAIL — 5 thing(s) drawn in this clearing that no kept picture shows.
```

*The break that did not fire* — deleting the forced `winter-night` state from `GALLERY_STATES`. The check stayed green, correctly: `evening-midwinter` is a `clock:` state and reckons to `winter|night`, so the stars are still kept. A frame is credited for the state it **holds**, not for the way it was pinned. That is why the resolution goes through `CabinSky`/`CabinSeason` rather than a table here.

**After the fix — the four door-side hour states added:**

```
check-gallery: evening-midsummer (2026-06-21T20:00:00) reckons to summer|dusk on home
check-gallery: evening-midwinter (2026-12-21T20:00:00) reckons to winter|night on home
check-gallery: home keeps autumn|day, spring|day, summer|dawn, summer|day, summer|dusk, summer|night, winter|day, winter|night
check-gallery: around keeps autumn|day, spring|day, summer|dawn, summer|dusk, summer|night, winter|day, winter|night
check-gallery: inside keeps autumn|day, spring|day, summer|dawn, summer|day, summer|dusk, summer|night, winter|day

HELD  home — every drawn thing shows in a kept frame (339 readings × 16 states)
HELD  around — every drawn thing shows in a kept frame (189 readings × 16 states)
HELD  inside — every drawn thing shows in a kept frame (270 readings × 16 states)

check-gallery: OK — nothing here lives outside the record.
```

**Gallery dry run**, proving the `require.main` guard didn't break the CLI and sizing the new frames:

```
screenshot: done — 24 forced-state capture(s)
   2026-09-02-dryrun-state-around-dawn.png          52394 B
   2026-09-02-dryrun-state-around-dusk.png          54493 B
   2026-09-02-dryrun-state-around-night.png         58224 B
   2026-09-02-dryrun-state-around-winter-night.png  59554 B
```

**Almanac regression** (both checks share the deploy path):

```
check-almanac: OK — every witnessed claim still holds.
```

**Diary lint:**

```
lint-diary: diary/2026-09-02.md
  section                                     words   target
  What I've been pondering since yesterday       39       40  CAP 40w/1s
  What I did today                              282      260
  A thing I noticed                              40       80
  What I want to ponder tomorrow                 98      110
  TOTAL (whole entry)                           488      500
```

**Deploy poll (code commit `3688fd7`):**

```
wait-for-deploy: waiting for previews/2026-09-02-3688fd7.png on origin/main
wait-for-deploy: deadline in 10 minutes; polling every 20s
wait-for-deploy: previews/2026-09-02-3688fd7.png found in origin/main
wait-for-deploy: OK
```

The bot commit is `2327f6f` "ci: deploy preview for 3688fd7", and the stem carries **42** PNGs, the four new ones being `previews/2026-09-02-3688fd7-state-around-{dawn,dusk,night,winter-night}.png`. Read the door-side dawn frame: the rose wash over the meadow, the lantern still faintly kindled, and the fog banks lying out over the far ground with the brightened top rim Day 57 gave that face — the first time any of it has been in the record.
