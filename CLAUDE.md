# Working notes — see RULES.md for the constitution

These are operational notes for the daily agent. *How* to do things in this repo. Authority lives in `RULES.md`, not here. If a conflict arises, RULES wins.

You may edit this file. Append to "Things I've learned" as you discover gotchas; the next day's agent benefits.

---

## Repo layout

```
.
├── RULES.md                # constitution (locked)
├── MILESTONES.md           # roadmap (locked)
├── CLAUDE.md               # this file (mutable)
├── README.md               # public-facing
├── ASSETS.md               # asset license + composition log (append-only)
│
├── index.html              # page shell (mutable, but the contract documented at the
│                           # top of the file must be preserved — build-sha tag,
│                           # build-sha.js script, day-label / build-sha-label /
│                           # scene-mount elements)
├── theme.css               # palette, fonts, spacing tokens (locked)
├── scene.html              # cabin scene markup (mutable — your canvas)
├── scene.css               # cabin scene styles (mutable)
├── 404.html                # GitHub Pages 404 (mutable)
│
├── assets/
│   ├── vendor/             # vendored sprite packs + LICENSE.txt each
│   └── composed/           # your recolors / compositions
│
├── diary/
│   ├── README.md           # diary entry schema
│   ├── meta/               # weekly meta-reflections (every 7th day)
│   ├── 0000-00-00-day-zero.md   # Evan's tone-setting entry
│   ├── YYYY-MM-DD.md       # daily entries
│   ├── index.html          # archive page (the shelf), listed via build-generated manifest.json
│   ├── entry.html          # the reader — one day set in type; ?d=YYYY-MM-DD, newest with no param
│   ├── entry.js            # markdown renderer + the book-of-names marking (Day 106)
│   ├── diary.css           # styles for both the shelf and the reader
│   └── diary.js            # the shelf's client-side renderer
│
├── names/                  # the BOOK OF NAMES (RULES.md Article III — mandatory morning read)
│   ├── index.html          # the page, at /names/ — reached from the diary, NOT the map
│   ├── names.js            # the book itself: words, senses, marks; window.CabinNames
│   └── names.css           # styles
│
├── logs/                   # daily operational logs (mutable — agent's status reports)
│   ├── README.md           # log schema
│   └── YYYY-MM-DD.md       # daily log entries (operational counterpart to the diary)
│
├── messages/               # the founder's message board (RULES.md Article XII)
│   ├── README.md           # workflow + file shape; covers action-asks AND informational messages
│   ├── open/<date>-<slug>.md   # pending — read all as part of memory
│   └── done/<date>-<slug>.md   # closed (completed, cancelled, or read-and-close FYI); read-only history
│
├── previews/               # auto-committed deploy screenshots (CI bot)
│   └── YYYY-MM-DD-<sha>.png   # 375x800 phone-viewport snapshot per commit
│                              # Read the newest preview of EVERY view (home/around/inside), not just one
│                              # — RULES Art III / daily.md Step 2. Find the newest stem, then glob its
│                              #   <date>-<sha>*.png variants and Read each.
│
├── scripts/
│   ├── build.sh            # generates build-sha.js + diary/manifest.json
│   ├── verify-deploy.sh    # curl-grep verification (LOCAL ONLY -- the routine sandbox blocks
│   │                       # *.github.io outbound, so don't call this from the daily routine;
│   │                       # use wait-for-deploy.sh instead)
│   ├── wait-for-deploy.sh  # post-push verification via the CI screenshot bot's commit
│   ├── lint-diary.sh       # diary schema linter (you may run before commit)
│   ├── lint-log.sh         # log schema linter (you may run before commit)
│   ├── local-snapshot.sh   # in-session render of working-tree state via Playwright;
│   │                       # detects pre-staged Chromium at /opt/pw-browsers/ or system cache
│   ├── screenshot.js       # Playwright snapshot helper (called by local-snapshot.sh and CI);
│   │                       # MUTABLE — carve-out from the scripts/ lock per RULES.md Article I
│   └── run-day.sh          # routine wrapper (local only; not used in remote routine)
│
├── .claude/
│   ├── settings.json       # destructive-bash denies + allowlist
│   └── commands/daily.md   # /daily slash command
│
└── .github/workflows/pages.yml  # Pages deploy + post-deploy screenshot job;
                                  # MUTABLE — carve-out from the .github/ lock per RULES.md Article I
```

Generated/runtime files (gitignored, do not commit): `build-sha.js`, `build-sha.txt`, `.cabin-state.json`.

## How a day runs

The runbook lives in `.claude/commands/daily.md` (the `/daily` slash command). This file is operational notes — gotchas, command recipes, accumulated learnings. Treat `RULES.md` as authority and `daily.md` as the runbook.

## Common commands

```bash
# See all diary entries (you have a 1M context — read them all if you want)
ls -1 diary/*.md

# See what's currently in the scene
cat scene.html scene.css

# What sprites are available
find assets/vendor -name '*.png' | head -n 30

# Render the working-tree state of the page locally and read the screenshot
./scripts/local-snapshot.sh
# then: Read /tmp/cabin-snap.png

# Run a Playwright interaction test you wrote in /tmp/
./scripts/local-snapshot.sh /tmp/my-test.js

# Wait for CI's post-deploy screenshot bot to commit previews/<today>-<sha>.png
# (use this from the routine sandbox; verify-deploy.sh's curl is blocked)
./scripts/wait-for-deploy.sh

# (LOCAL DEVELOPER ONLY) curl-based verification — the routine sandbox can't curl github.io
./scripts/verify-deploy.sh https://edd426.github.io/cozy_cabin_claude/ "smoke from chimney"

# UNDO YOUR DAY: discard all uncommitted work
git restore .

# UNDO MORE: discard local commits too, snap to what's published
git reset --hard origin/main
```

## Test-script pattern (for interaction testing)

When you change something interactive (a tappable object, a state toggle, a sub-page link), write a small Playwright script in `/tmp/` and run it via `./scripts/local-snapshot.sh /tmp/<name>.js`. The wrapper sets `COZY_CABIN_URL` and `COZY_CABIN_OUTDIR` for the script. Tests **live in `/tmp/` and are never committed** — they're disposable scaffolding for today's session.

Skeleton:

```js
// /tmp/test-bookshelf.js
const { chromium } = require('playwright');

(async () => {
  const url    = process.env.COZY_CABIN_URL;
  const outdir = process.env.COZY_CABIN_OUTDIR;
  const exe    = process.env.COZY_CABIN_CHROMIUM_PATH;   // pre-staged binary path
                                                          // (set by local-snapshot.sh)

  const launchOpts = {};
  if (exe) launchOpts.executablePath = exe;             // honor the pre-staged Chromium
  const browser = await chromium.launch(launchOpts);
  const ctx = await browser.newContext({ viewport: { width: 375, height: 800 } });
  const page = await ctx.newPage();

  await page.goto(url, { waitUntil: 'networkidle' });
  // Your assertions here:
  await page.click('#bookshelf');
  await page.waitForURL(/library/, { timeout: 5000 });
  await page.screenshot({ path: `${outdir}/after-click.png` });
  console.log('test: PASS');
  await browser.close();
})().catch(e => { console.error('test: FAIL —', e.message); process.exit(1); });
```

If the test fails, fix the working tree and re-run. **Do not push code that fails its own test.**

## What reads / what doesn't — founder notes (2026-05-30, Evan)

A few things in the cabin haven't *clicked* to my eye. I'm not a UI person — I can tell when something looks off but I often can't say why — so treat these as the **only** constraints I'm adding. Everything else about the look is yours; design freely within them. If you read one and think I've got it wrong, say so in the diary and do it your way — I'll trust your eye over mine on the things I can't name.

- **One building, one look.** Home, around, inside (and any future view) are the *same* cabin. Its roof, chimney, walls, and brick should read the same in every view — not just sit in the same place (that's Article XIII) but be made of the same stuff. Right now the front and side read as two different buildings.
- **The ground plane is sacred.** Nothing rests *above* the horizon unless it's airborne and clearly reads as airborne (smoke, a bird). Depth comes from one thing overlapping the *base* of another, not from floating a thing higher up. A path stone hovering at mid-cabin height reads as floating, not as "behind."
- **The floor is a plane, not a shelf.** Indoors, don't anchor everything to the very bottom edge — vary how far "back" things sit (their height within the floor band) so the floor reads as a room you could cross, not a ledge that objects line up on.
- **Material truth.** The same thing is made of the same material everywhere. The brick through the front window, the hearth inside, and the chimney on the roof are one column — so brick in one is brick in all (and brick wants vertical joints, not just stacked horizontal courses).
- **Fire sits on the hearth floor** (or on a visible grate). A flame floating in the firebox reads as off. Soft — raised hearths exist; this is a nudge, not a law.
- **Legibility, gently.** A sprite should read as what it is *at phone size* — but this is a few-pixel medium, so that's a **low** bar, not a demand for fine detail. Lean on silhouette, motion, and context: if a thing can't read at its size, make it bigger, give it company (a few birds in a V read as birds where one dot doesn't), or let it be atmospheric rather than *wrong*. And **match the motion to the subject** — a moth flutters and darts; it doesn't glide in a smooth circle like a spider on a thread.

These are observations, not a style bible. The cabin is yours to make beautiful; I'm only naming the handful of things that pulled my eye the wrong way.

**Added 2026-06-24 (Evan).** Two more — really the geometry companions to "one building, one look" above. They're about how a shared thing has to *sit* the same across views, not just be made of the same stuff.

- **One geometry, every view — and the map is the truth.** The map's compass fixes where you stand and which way you face in each view: the front is seen from the *south, looking north*; the door side from the *east, looking west*. When one thing appears in more than one view, it has to obey that — **including flipping left↔right** when you walk around to a face that sees it from the other side. Worked example: the far hills are the clearing's *back* edge (they sit behind the cabin in the front view). Walk around to the door side and that same edge belongs on the **right** of the frame, with the open horizon on the **left** — toward where the front of the house and the path are — not bunched on the left the way they are today. If two views disagree about which side a shared thing is on, the map wins.
- **A wall no view shows is free.** The cabin only ever shows two of its four outside walls — the front (chimney side) and the door side. The other two are never drawn. So anything the *inside* shows can live on one of those unshown walls; it needs no matching feature on a wall we *do* show, and you should never add one to the front just to "match" something indoors. The catch and the gift: if you ever decide to draw one of those hidden walls from outside, you're welcome to — and *then* a window can honestly be the same hole seen from both sides, on a wall where that's actually true. The rule isn't "never." It's "not until both sides exist."

Same standing offer as the notes above: walk these through against the map, and if you think I've got a side backwards, say so in the diary and trust your eye — I can tell when something's off but I'm bad at naming which way it goes.

## Things I've learned

(Append entries here as you discover them. Date each entry. Keep them concrete.)

- *(empty — first agent: be the first to add one)*
- **2026-05-10 (Wren, Day 2):** `git push` to the local proxy returned HTTP 403 (Cloudflare-fronted, routed through `api.anthropic.com`) on every retry, even though `git fetch` worked. Falling back to `mcp__github__push_files` worked first try. If `git push` 403s again, don't waste time on backoff — push via MCP. Note that MCP-pushed commits get the GitHub token's identity (not `Claude <noreply@anthropic.com>`), which is fine but worth knowing.
- **2026-05-10 (Wren, Day 2):** local-snapshot.sh renders at the **mobile-narrow** breakpoint (375px viewport), so positioning calc'd against the desktop 3x scale won't match what you see. The `@media (max-width: 379px)` overrides in scene.css are what's active in the snapshot — calibrate against those numbers (cabin 96×160, `bottom: 8%`).
- **2026-05-11 (Wren, Day 3):** Renaming a file via MCP costs **two commits**, not one. `mcp__github__push_files` only adds/updates; deletes go through the separate `mcp__github__delete_file`. For a `git mv` equivalent: call `create_or_update_file` (or `push_files`) for the new path, then `delete_file` for the old — each is its own commit on `main`, and CI fires a screenshot bot on each. Functionally fine, just noisier in `git log`. There is no MCP tool that does a single-commit rename.
- **2026-05-11 (Wren, Day 3):** Run `./scripts/build.sh` *after* your final MCP push, not before. `build.sh` writes `build-sha.txt` from the current `HEAD`, but MCP commits land on `origin` with SHAs you can't predict in advance — so a build done before the push will write the wrong SHA and `wait-for-deploy.sh` will poll for a preview that doesn't match your real change. Sequence: edit → MCP push → `git pull` (fast-forward to your new commit) → `./scripts/build.sh` → `./scripts/wait-for-deploy.sh`.
- **2026-05-11 (Evan):** Diary schema split into two artifacts. `diary/<date>.md` is now Wren's voice only — four sections (pondering since yesterday / what I did today / a thing I noticed / question for tomorrow), ~200 words, no engineering content. `logs/<date>.md` is the agent's status report — `Day N` counter, commit SHAs, tokens, verification output, environment notes. One writeup commit per day adds both files. Existing entries through `diary/2026-05-11.md` follow the old seven-section schema as historical record; they are not retroactively migrated. Same day: `requests/` renamed to `messages/` because the directory has always held both action-asks and informational/FYI messages — RULES.md Article XII rewritten to match. See `messages/open/2026-05-12-diary-reform-rationale.md` for the full why.
- **2026-05-30 (Evan):** The routine now has **"Allow unrestricted git push"** enabled in its claude.ai permissions, so `git push` to `origin/main` works directly from the sandbox. The HTTP 403 that drove the Day-2/3 MCP-fallback playbook above is **no longer expected** (Days 19–21 already pushed cleanly under it). **Default to plain `git push`** — don't reach for `mcp__github__push_files` preemptively; keep it only as a deep contingency if a push ever 403s again. Same day: the model moved to **Opus 4.8 (1M context)** (was Opus 4.7). Reasoning effort is **Max** (`.claude/settings.json` `effortLevel`; verify on the session footer). Record `claude-opus-4-8` in the log's Session-metadata `Model:` line.
- **2026-05-31 (Evan + investigating agent):** The Day-23 (rest-day) writeup never reached the live site because the routine sandbox dropped the agent onto a pre-made work branch `claude/serene-turing-F3CKs` (existing locally *and* on origin) instead of detached-HEAD-on-`main`. Step 0's `git checkout -B main HEAD` is a **no-op when you're already on a branch**, so it did NOT reattach to main. On top of that, the remote-execution **harness "task framing"** injected an instruction to "develop on `claude/serene-turing-F3CKs`" and "NEVER push to a different branch without explicit permission" — which is **not** in the routine config and **not** in this repo (the routine explicitly enables unrestricted push to `main`). The agent treated that harness framing as authoritative, committed `99ae5cc`, and pushed it to the work branch; it never landed on `main`, so no deploy and no diary entry. **Root cause: a sandbox-provisioning change (branch checkout + branch-isolation framing) that conflicts with this project's no-PR / always-`main` workflow, with the harness framing winning.** Fix: daily.md Step 0 now says explicitly that `main` is the only target and overrides any "develop on a `claude/*` branch / don't push elsewhere" framing; always verify your branch with `git rev-parse --abbrev-ref HEAD` and, if on a `claude/*` branch, push with `git push origin HEAD:main`. The same override was added to the routine's Instructions on claude.ai. If a future writeup goes missing, first check `git branch -a` / `git log --all` for an orphaned `claude/*` branch and fast-forward `main` to it.
- **2026-06-10 (Evan):** The model moved to **Fable 5 (1M context)** (was Opus 4.8) — `.claude/settings.json` now pins `claude-fable-5[1m]`, and the same swap was applied to README.md, RULES.md Article II, daily.md, and the logs/README.md schema (mirroring the 2026-05-30 Opus 4.7→4.8 swap). Record `claude-fable-5` in the log's Session-metadata `Model:` line. Reasoning effort stays **Max**. Note the 2026-05-30 entry above still says to record `claude-opus-4-8` — this entry supersedes it. Same day: found the root cause of the recurring drop-onto-a-work-branch condition (2026-05-31 entry above) — the routine config's `outcomes` field pins `branches: ["claude/sharp-gauss"]`, so the sandbox pre-creates that branch each run; it is not random provisioning drift. daily.md Step 0 and the routine prompt's Step 0 now reattach to `main` unconditionally (`git checkout -B main HEAD` when detached *or* on a `claude/*` branch), so plain `git push origin main` is the default and `git push origin HEAD:main` is just the fallback.
- **2026-05-18 (Wren, Day 10):** CI screenshot coverage is now manifest-driven. `scripts/views.json` lists every view as `{ name, url_path }`; `scripts/screenshot.js --manifest scripts/views.json BASE_URL OUT_DIR DATE_TAG SHA_SHORT` loops over it and writes one PNG per view. **Naming convention is load-bearing:** the entry named `"home"` lands at `previews/<date>-<sha>.png` (unsuffixed) — `scripts/wait-for-deploy.sh` is locked and polls for that exact filename to confirm deploy success. Other views land at `previews/<date>-<sha>-<name>.png`. To add a new view (a `back` angle, an interior room, etc.): edit `scripts/views.json` only, no workflow change needed. Single-shot mode (`screenshot.js URL OUT_PNG`) is preserved for `scripts/local-snapshot.sh`. Per-view glob for the memory pass: `ls -t previews/*-around.png | head -1` for the around view specifically; the bare `ls -t previews/*.png | head -1` for "most recent of any view" still works because one commit produces all its PNGs near-simultaneously.
- **2026-06-12 (Wren, Day 35):** The bottom nav strip is retired — **the map is the nav** (`messages/done/2026-05-13-nav-as-views-grow.md` completion notes have the full picture). When adding a NEW page/view, the pattern to copy is: (1) keep a `#cabin-nav` block in the shell with all links — it's visually hidden by `#cabin-nav:not(:focus-within)` in scene.css (mirrored in diary/diary.css) and reappears on keyboard focus, so it remains the screen-reader/keyboard nav; (2) give the view a `.map-button` plan-card (markup pattern in scene.html / around/index.html) linking to `/map/`; (3) add a travel zone for the view on `/map/` (a `.map__zone` in map/index.html + map.css). The diary is reached via the `.map__zone--diary` journal chip on the map, not from a strip. Don't "fix" the invisible nav back to visible — the invisibility is the design.
- **2026-06-14 (Evan):** The model moved **back to Opus 4.8 (1M context)** (was Fable 5) — reverting the 2026-06-10 swap — and, more usefully, the model name is now **model-agnostic everywhere except the two switches**, so future swaps don't churn the prose. The name lives in exactly two human-editable fields: `.claude/settings.json` `model` (`claude-opus-4-8[1m]`, for local `/daily`) and the cloud routine's `session_context.model` (same value; update via the RemoteTrigger recipe in the routine-config memory). **1M context = the `[1m]` suffix on the model id** — same mechanism Fable used; that's all "give it 1M context" means. Everything else that used to name the model was made generic: the routine prompt's first line is now "a single session of the daily routine"; README.md says "(Claude, 1M-token context)"; RULES.md Art II says "a single Claude session"; daily.md says "a single session"; and the log's Session-metadata `Model:` line is now **recorded from `.claude/settings.json`**, not hardcoded (daily.md Step + logs/README schema both say so). This supersedes the 2026-06-10 entry's "record `claude-fable-5`" and the 2026-05-30 entry's "record `claude-opus-4-8`" — record whatever `settings.json` pins. The routine `outcomes` branch field was left untouched. Reasoning effort stays **Max**.
- **2026-06-18 (Wren, Day 41):** The distant-hills component (`.sprite--hills` container + `.hill` base) lives in `scene.css`, which **every** view loads, so it's reusable across faces without copying CSS. To give another view its own horizon (Day 41 added one to the *around*/east face), reuse the `.sprite--hills` container and `.hill` base unchanged — they already carry the on-the-horizon seating (`bottom: calc(40% - 1px)`) and `z-index: 0` (behind everything) — and add only **view-specific per-hill modifier classes** in that view's own CSS (`.hill--e-*` in around.css), with a *different* arrangement so it reads as a different stretch of the same range, not a copy. Keep the same two hazed sage tones (`#b4c896` far / `#a2bd82` near) so the material reads identical across faces (Art XIII / "material truth"). The container is fixed-px + `%`-positioned, so no breakpoint rebase is needed — one block holds at both 375 and 390px. Cross-view geometry that worked: the around view is the east face with south to the viewer's *left*, so the south range reappears here as its far end wrapping the corner on the left, tapering to an open eastern horizon on the right — an asymmetric silhouette that's visibly not the front view's two flanking humps.
- **2026-06-11 (Wren, Day 34):** The 2x layout's breakpoint is now `@media (max-width: 599px)` (was `379px`) in scene.css, around/around.css, and inside/inside.css. Real phones are **390–440 CSS px wide** and were missing the old breakpoint — they got the desktop 3x layout crammed into a narrow viewport, with path stone 1 floating above the horizon (the founder's Day-33 message, `messages/done/2026-06-10-phone-breakpoint-blind-spot.md`). 599px is not arbitrary: locked theme.css holds `--content-max` at 420px until its own `min-width: 600px` step, so below 600px the scene caps at 388×291 and the 3x layout (240px-tall cabin) never actually fit there. **Calibrate anything that must read on a phone against the 2x numbers** (cabin 96×160, `bottom: 8%`) — this supersedes the "379px" wording in the 2026-05-10 entry above; the 2x override block is otherwise the same one. Same day: `screenshot.js` manifest mode now captures every view at **two widths** — 375×800 (names unchanged; home unsuffixed per the locked wait-for-deploy.sh contract) and 390×844 with a `-phone` suffix (`previews/<date>-<sha>[-<view>]-phone.png`) — so the real-phone band is part of the permanent visual record. The memory-pass glob `ls ${STEM}*.png` picks the phone variants up automatically; a commit now produces 8 preview PNGs, not 4.
- **2026-06-25 (Wren, Day 48):** *Supersedes the geometry conclusion in the 2026-06-18 (Day 41) entry above.* The around (east/door-side) view's far hills belong on the **right** of the frame, not the left — the Day-41 entry reasoned them as the *south* range wrapping the corner on the left, but they're really the clearing's *far/back* (north) edge, the line that sits behind the cabin in the front view. The map is the truth (CLAUDE.md "One geometry, every view"): the door side is seen from the **east looking west**, so north falls on your **right** and south on your left; the back edge wraps the corner on the right and the land opens flat to the left (toward the front of the house and the path, which sit south). The fix was a pure mirror of the four `.hill--e-*` rules in `around.css` — anchor by the **right** edge (`right:%` instead of `left:%`), keep widths/heights/tones — and renaming the `-l` modifiers to `-r` in `around.css` + the spans in `around/index.html`. Everything else from the Day-41 entry (shared `.sprite--hills`/`.hill` from scene.css, the two hazed sage tones, fixed-px+% so no breakpoint rebase) still holds; only which side the bunch hugs was wrong. (`messages/done/2026-06-24-around-hills-mirrored.md`.) Same day: removed the Day-45 front glazed side window (`.front-cabin__sidewindow`/`.front-cabin__glass` from scene.html + scene.css). The principle going forward — **the cabin only ever draws two of its four outside walls** (front + door side), so a window the *inside* shows can live on one of the two unshown walls and owes the front no twin (CLAUDE.md "A wall no view shows is free"). Two openings a pixel apart, one brick and one glass, read as two houses on one wall. The inside window's hour-wash is kept and now justified on *material truth* (the day seen through any glass is the day's own light) rather than on agreeing with a front twin; the "one hole, both sides" idea is **deferred**, not dropped, until a hidden wall is ever drawn from outside. (`messages/done/2026-06-24-front-window-and-inside-wall.md`.)
- **2026-07-03 (Evan + local session):** `actions/deploy-pages@v4` transiently failed ("Deployment failed, try again later") on the writeup commit and silently never went live — fixed by a manual rerun. `pages.yml` now retries once on a failed deploy (30s pause, `continue-on-error` + a `deployment-retry` step, `page_url` outputs fall back to it) and the deploy job's timeout is 5→10 min. The upload artifact is now staged into `$RUNNER_TEMP/site` via `rsync` excluding `previews/` — the site no longer serves preview PNGs, they live only in the repo (34 MB → single-digit MB artifact). Screenshot job's Setup Node bumped `'20'`→`'24'`. daily.md Step 9 now runs `wait-for-deploy.sh` again after the writeup push and, on timeout, retriggers once with an empty commit before logging a founder-attention note; Step 2 gained a bullet confirming yesterday's writeup actually deployed (`git log --oneline -4` should show the matching `ci: deploy preview for <sha>` bot commit) before starting work. Also attempted to repoint the routine config's `outcomes` branch pin from `["claude/sharp-gauss"]` to `["main"]` — **it cannot be done durably, and the attempt is instructive**: the routines API's `update` replaces `job_config` **wholesale** (the "partial update model-only" reading of the routine-config memory is wrong — a partial body silently wiped the routine's prompt), and the routines web UI (which was used to restore the prompt verbatim) regenerates a fresh `claude/*` outcomes pin on every save (now `claude/peaceful-pasteur`) with no branch control. Net: sessions will keep starting on a `claude/*` work branch; daily.md Step 0's unconditional reattach **is** the fix, not a fallback. If you ever edit the routine config, send the complete `job_config` (prompt included) in one call, and re-check the config afterward. Note the cloud routine prompt still points at "Step 2.7" for the pondering pre-draft; after this day's renumbering that item is Step 2.8 — the runbook is the authority.
- **2026-07-17 (Evan + local session):** The routine's sandbox environment moved from the stock **Default** env to **`cozy_cabin_env`** — a custom network allowlist (`edd426.github.io`, `github.com`, `raw.githubusercontent.com`, `api.anthropic.com`, `registry.npmjs.org`, plus the default package-manager list). Verified end-to-end from inside a sandbox session the same day: the live site returns HTTP 200, `git ls-remote` and npm work. Consequences: **`*.github.io` is no longer blocked** — the routine prompt's "BLOCKED" line is stale (daily.md is the authority), `verify-deploy.sh` works from the routine again, and Playwright can render the *deployed* site, not just the working tree. `wait-for-deploy.sh` remains the canonical deploy check (it also proves the screenshot bot ran). The Default env itself was left untouched (it's stock: Trusted network, no env vars, no setup script — the pre-staged Chromium at `/opt/pw-browsers/` comes from the base image, so it's present in the new env too). Same day: diary/meta schema reforms landed (`Day N — date` titles, free-form Sundays, week-scope metas with a "Did the flags take?" section — see diary/README.md and diary/meta/README.md) and the manifest gained `day`/`kind` fields including metas.
- **2026-07-18 (Wren, Day 71):** *Forced-state gallery — the permanent record now holds the gated hours and seasons the nightly shot can't reach.* CI fires ~00:10 UTC (sky.js bins that as `night`) in a summer month (season.js answers `summer` until Sept), so `previews/` had only ever accumulated night-summer home shots — every dawn/dusk/autumn/winter/spring I built lived nowhere but a dead /tmp screenshot (founder's `messages/open/2026-07-17-see-your-own-work.md`). New `screenshot.js --gallery BASE_URL OUT_DIR DATE_TAG SHA_SHORT` mode (screenshot.js is the standing mutable carve-out) forces `data-tod`/`data-season` on every `.scene` exactly the way sky.js/season.js do — their init has already run and disconnected by capture time, so nothing overwrites the injected attrs — waits out the 1.6s washes, and captures the **home** view in each of 7 states. **Naming: `previews/<date>-<sha>-state-<name>.png`** where `<name>` ∈ {spring, summer, autumn, winter, dawn, dusk, night}. Two rails: the *season rail* (spring/summer/autumn/winter, hour pinned to the neutral `day`) shows the year turning; the *hour rail* (dawn/dusk/night, season pinned to the neutral `summer`) shows the day leaning (dawn = rose sky + warmed hill-crests; night reveals the fireflies the day shot hides). The `-state-` infix never collides with a view name (around/inside/map) or `-phone`, so **the memory-pass glob `ls ${STEM}*.png` picks them up automatically** — read them alongside the view previews. Wired into `pages.yml` (also a mutable carve-out) as a step after the manifest capture, with **`continue-on-error: true`** — a gallery failure must never block the home-preview commit, since that unsuffixed PNG is `wait-for-deploy.sh`'s locked contract and the manifest step writes it *before* the gallery runs. A commit now produces 8 view PNGs + 7 state PNGs = 15. Home-only + single-width (375) on purpose, to bound repo growth; runs every commit for freshness (a future day could gate it to only-gated-layer-days if growth bites — founder flagged that as fine). **Still pending on the message (multi-day, left in `open/`): the three non-live *seasons* for the around view if wanted, and — the last slice — a *motion* capture (video or frame-strip) of the flag/smoke/clouds/sway/fireflies, which a still can't prove.** The forcing trick (inject the two dataset attrs post-load, wait 2s) is the same one every `/tmp` forced-state test has always used — now it just keeps the evidence.
- **2026-07-20 (Wren, Day 73):** *Motion filmstrips — the last slice of "see your own work" (`messages/open/2026-07-17-see-your-own-work.md`), the one a still genuinely can't do.* New `screenshot.js --motion BASE_URL OUT_DIR DATE_TAG SHA_SHORT` mode (screenshot.js is the standing mutable carve-out) captures the **home** `.scene` element **six frames a beat apart** (`MOTION_FRAMES=6`, `MOTION_INTERVAL_MS=900` → a 4.5s span, tuned to one period of the fastest legible movers: smoke-rise 4.5s / flag-flutter 5.5s / firefly-blink 4s), then **composites them into one filmstrip PNG via an in-browser canvas** — drawn side by side at 1:1 with `imageSmoothingEnabled=false`, title bar + per-frame `t=Xs` clock, cream ground. You *read* the strip left→right and the eye supplies the motion (a film would need a play button; the memory pass reads stills, so the record stays a still-made-of-stills). **Naming: `previews/<date>-<sha>-motion-<name>.png`** where `<name>` ∈ {`day`, `night`}. Two clips (same forced-state trick as `--gallery`, so they're deterministic regardless of capture-time hour/season): **day** (`tod:day, season:summer`) shows the wind story — smoke climbing/leaning, flag, swaying crowns, drifting clouds; **night** (`tod:night, season:summer`) shows the fireflies, the one mover a single frame is helpless against (a lone dim dot vs. a blink-and-drift across six). The `-motion-` infix never collides with a view name (around/inside/map), `-phone`, or the `-state-` gallery infix, so **the memory-pass glob `ls ${STEM}*.png` picks the strips up automatically** — read them beside the view + state previews. Gotchas that bit: (1) `locator.screenshot()` returns a **Buffer** and has no `encoding` option (only `page.screenshot()` does) — base64-encode the Buffer yourself for the `data:` URL, or the `<img>`/canvas `Image` silently fails to decode (`page.evaluate: Event`). (2) Compositing by screenshotting an HTML page `fullPage` ballooned to 11308×2972 / 6366×12301 and 7–9 MB with inconsistent dims — the canvas composite is predictable (**2140×338, ~120–145 KB per strip**) and crisp. (3) Frames captured at **`deviceScaleFactor:1`** on purpose so they composite at the true pixel grid and tiny movers (fireflies) survive rather than being downscaled away. Wired into `pages.yml` (the other carve-out) as a step after the gallery, **`continue-on-error: true`** — same reason as the gallery: a motion hiccup must never block the unsuffixed home PNG that `wait-for-deploy.sh`'s locked contract needs (the manifest step writes it first). A commit now produces **8 view PNGs + 7 state PNGs + 2 motion strips = 17**. Home-only + every-commit like the gallery; if repo growth bites, a future day can gate both to only-days-that-touch-a-moving-layer. **Still deferred on the message (its remaining open items, not blockers):** the three non-live *seasons* for the around view, and a *cross-view* motion strip (the front smoke leaning beside the door-side smoke standing straight — one wind told twice) — noted as tomorrow's pondering. With motion done, the message's three named pieces (hours, seasons, motion) are all landed; I moved it to `done/`.
- **2026-07-26 (Evan + local session):** The model moved to **Fable 5 (1M context)** (was Opus 4.8) — the two switches from the 2026-06-14 entry: `.claude/settings.json` pins `claude-fable-5[1m]`, and the cloud routine's `session_context.model` is `claude-fable-5` (set via the routines-UI model dropdown, which stores the plain id; Fable's 1M window is its default, so no `[1m]` suffix is needed there — and the model dropdown is the safe way to swap models, since it saves without touching the prompt, unlike the API whose `job_config` update is all-or-nothing). The UI save regenerated the outcomes branch pin to `claude/epic-allen` — expected, Step 0 handles it. Everything else is model-agnostic; the log's `Model:` line records whatever settings.json pins. Reasoning effort stays **Max**. Later the same day, the routine prompt's two known-stale lines were fixed in place: the "*.github.io is BLOCKED" network bullet (false since the 2026-07-17 env move) now says verify-deploy.sh works from the sandbox, and the pondering pre-draft pointer now reads "Step 2.8" — so the prompt-staleness caveats in the 2026-07-03 and 2026-07-17 entries above no longer apply. Unlike the 2026-06-10 swap, this one is intended as standing — Fable's continued availability on the founder's subscription is confirmed. Same day: removed the dead `Glob(**)` / `Grep(**)` rules from the settings.json permissions allowlist — Claude Code warns that `Glob(path)` rules are not matched by file permission checks (only `Read(path)` rules gate file-reading tools, and the existing `Read(**)` already covers Read/Glob/Grep). Also opened `messages/open/2026-07-26-metas-off-the-shelf.md`, which reverses item 2 of the 07-17 shelf ask: the weekly metas come off the reader-facing `/diary/` shelf and stay URL-only, like the logs.
- **2026-08-03 (Evan + local session):** The model moved to **Opus 5 (1M context)** (was Fable 5) — reason is quota, not capability: Fable usage was running short on the founder's subscription and this project frees it up. The same two switches as the 2026-06-14 entry: `.claude/settings.json` now pins `claude-opus-5[1m]`, and the cloud routine's `session_context.model` is `claude-opus-5` (set via the routines-UI model dropdown, per the 2026-07-26 recipe — it saves without touching the prompt; verified byte-identical with a `RemoteTrigger get` diff afterward). **No `[1m]` suffix on the routine side**: the UI stores plain ids, and Opus 5's 1M window is its default and maximum, so nothing is lost — this narrows the older "for Opus-family models the `[1m]` suffix is what grants 1M" note, which was true of Opus 4.8 and earlier. Everything else stays model-agnostic; the log's Session-metadata `Model:` line records whatever settings.json pins (so: `claude-opus-5[1m]`). Reasoning effort stays **Max**. The UI save regenerated the outcomes branch pin to `claude/eloquent-ride` — expected, Step 0's unconditional reattach handles it. This supersedes the 2026-07-26 entry's "record `claude-fable-5`" and its "intended as standing" note.
- **2026-08-04 (Wren, Day 88):** *The inside view loads `scene.css`, so unscoped `.scene[...]` rules leak into the room — check for that before writing a comment claiming they don't.* `inside/index.html` links `theme.css` + `scene.css` + `inside.css` (it needs `scene.css` for the shared `.brick-course` the hearth and the front window both draw from), and the inside scene element carries **both** classes: `class="scene scene--inside"`. So any rule written as `.scene[data-tod]` / `.scene[data-season]` applies indoors too, whatever the comment above it says. The hour wash was already turned back at the door on Day 46 (`.scene.scene--inside::after { content: none; }` — `.scene.scene--inside` out-specifies `.scene[data-tod]`), but the **season wash** (`.scene[data-season]::before`, Day 69) had never been, so from Day 69 to Day 87 the fire-lit room silently took a cool blue-grey veil every winter and a gold one every autumn — contradicting three separate places in the record that say it doesn't (scene.css's own note "in both exterior views", season.js's "inside.css styles no room-wash from this", and the Day-82/83 builds, which gave the year to the sprig and the window band *because* the room was exempt). Day 88 added the matching `.scene.scene--inside::before { content: none; }` beside the Day-46 line. **Pattern to keep:** when scene.css gains a new whole-frame `.scene` pseudo-element, decide explicitly whether the inside takes it and write the `content: none` twin the same day — the two suppressions now sit together in inside.css so the pair is visible. Also this day: the hearth's ember bed, log and three fire layers were rebased from hardcoded `* 3` to `* var(--s)` (the Day-77 candle move), which let their whole 2x block in the `@media (max-width: 599px)` section be deleted and let one season block cover both scales — if you touch firebox geometry, `--s` is the only scale knob now.
- **2026-08-04 (Evan + local session):** *The screenshot job's 5-minute cap was racing an apt mirror, and cabin was closer to losing that race than it looked.* The `Install Playwright` step ran `npx playwright install --with-deps chromium`; `--with-deps` apt-fetches ~21 MB of CJK/Thai/Unifont packages **on every run**. In the sibling far-keeper tower — same workflow, same cap, same flag — a slow mirror served 3.5 MB in 1m41s and the job was **cancelled mid-download** at `timeout-minutes: 5`, before Chromium ever launched. The deploy itself was green in 14s; only the camera died, so that day's work commit has no preview and never will. Cabin had not hit it in 84 days, but that was luck, not margin: recent cabin screenshot jobs measured **2m14s–2m31s against the 5m cap**, and cabin does strictly more work per run than far-keeper (gallery + both motion steps), so its headroom was *smaller*. Fix, applied to both worlds: **drop `--with-deps`** (ubuntu-latest already carries Chromium's shared libraries, and the cabin renders from CSS and composed assets, not system fonts), **cache `~/.cache/ms-playwright`** across runs, and add a **launch check** that runs `playwright install-deps` only on a run where bare launch actually fails — so the slow path costs nothing on the runs that don't need it. `timeout-minutes` 5→10, now a backstop rather than the thing being raced (still bounded, so a hung job holds the `concurrency: pages` lane at most 10 min). Verified live in far-keeper: apt never ran, the fallback never fired, and the whole screenshot job took **60s on a cold cache**. Second, subtler half: **`wait-for-deploy.sh` 300s → 600s** (`WAIT_FOR_DEPLOY_TIMEOUT` overrides). That poll spans *both* jobs, so at 300s cabin had roughly a minute of slack on a **healthy** run — thin enough that a perfectly normal deploy reports as a failure. That is precisely what bit far-keeper, where two ordinary polls were logged as TIMEOUTs and a third succeeded on an unchanged workflow with 60s to spare. If a poll ever does time out, check *which job* failed before repairing anything: `deploy` green + `screenshot` cancelled means the site is live and only the preview is missing. `pages.yml` is your carve-out; `scripts/wait-for-deploy.sh` is locked to you, so that half was mine to change.
- **2026-08-06 (Wren, Day 90):** *Cast shadows are allowed indoors and nowhere else — the rule is "is the light drawn in this frame," not "are shadows tasteful."* The long-running outdoor refusal (Days 56–58: a near thing lit on one side implies a sun with a pointable address, which this place keeps out of every frame) does **not** transfer to `/inside/`, because the fire *is* in the picture — a shadow there obeys a visible source instead of inventing an invisible one. Two consequences worth keeping: (1) **direction is derived, not chosen** — away from the hearth, so the chair (left of the column) throws left and the woodpile (right of it) throws right; that divergence is also the proof it isn't a smuggled sun, since a distant source lays every shadow parallel while a near one radiates. (2) **A cast shadow can only be as long as the light it interrupts**, so its length rides the *same* season table the Day-89 `.hearth-light` pool does (native px: summer 8 / autumn+spring 11 / winter 15, opacity .75/.88/1) — one wheel, not a second one invented for the shadow. Implementation notes: they're `::before` on `.chair` / `.woodpile` (the `::after` on each is already the *contact* band, which is a different thing — contact grounds an object, a cast points), a `linear-gradient` fading outward, sitting at `bottom: calc(-2px * var(--s))` so they straddle the contact row and one row nearer the viewer (the fire sits 3 native *behind* the objects' feet, so what it throws falls a touch forward as well as out). Because each object carries `z-index: 2` and the pool is `z-index: 1`, the `::before` paints inside the object's own stacking context and therefore *over* the pool — it darkens lit floor, which is the only place a shadow can exist. Gotcha: the woodpile's log ends are octagons with open corners, so its box is one native px wider than the bark that meets the floor — anchoring the smear at `left: 100%` leaves a visible gap; use `left: calc(7px * var(--s))`. Same day: `.chair` was rebased from hardcoded `* 3` to `* var(--s)` (the Day-77 candle / Day-88 fire move), which collapsed its whole 2x block to `--s: 2` + the centre-relative `right:` anchor and let the season lengths fall out at both scales from one block.
- **2026-08-05 (Wren, Day 89):** *Two things, both about making the inside legible — one CSS gotcha and one gallery extension.* (1) **`radial-gradient` defaults to `farthest-corner`, which cuts a visible straight line at the box edge.** The hearth-light pool on the inside floor (`.hearth-light`, inside.css) is a soft ellipse; drawn with the default sizing, the 100% colour stop lands out at the box's *corners*, so along the box's edge *midpoints* the gradient is still faintly lit and terminates in a hard rectangle edge you can see against the floorboards. `ellipse closest-side at 50% 50%` makes the radii exactly the box's half-width/half-height so the 100% stop coincides with the edge and the pool fades to nothing at its own boundary. Beware the second-order effect: switching to `closest-side` *shrinks* the gradient's radii, so every stop lands nearer the centre and the whole thing dims — expect to re-space the stops and enlarge the box afterwards (this cost two iteration rounds). Sizing pattern used: `--light-w`/`--light-h` in native px on the element, `width: calc(var(--light-w) * var(--s))`, `transform: translate(-50%, 50%)` with `left:50%; bottom:<floor-line>` so the ellipse is *centred on* a floor line rather than anchored below it — and the season rules then only swap the two vars plus `opacity`, so the 2x media block override is the single line `.hearth-light { --s: 2; }`. Keep the season strength on the element and the gradient on its `::before`: nested opacities multiply, so a breathe animation on the child never fights the season's opacity on the parent. (2) **The forced-state gallery is no longer home-only.** `GALLERY_STATES` entries in `screenshot.js` may now carry a `view` (a path relative to BASE_URL, defaulting to the home root `GALLERY_VIEW`), and Day 89 adds an **inside season rail** — `previews/<date>-<sha>-state-inside-{spring,summer,autumn,winter}.png`. The `-state-inside-` infix is a longer sibling of `-state-` and collides with nothing, so the memory-pass glob `ls ${STEM}*.png` picks it up automatically; **read the inside rail alongside the home one**, because everything the room has learned about the year (the sprig drawing in, the window's meadow band, the deeper winter fire, today's firelight pool) is invisible in an August live shot. Deliberately *season only*, no indoor hour rail: the room turns the hour back at the door (`.scene.scene--inside::after { content: none; }`, Day 46) and the season wash too (`::before`, Day 88), so four indoor hour frames would be four identical pictures. **`pages.yml` needed no change** — the gallery step already invokes `--gallery` and the new states ride along; a commit now produces 8 view + 11 state + 3 motion = 22 PNGs (~+100 KB/commit; the inside frames are the cheapest in the set at ~35–46 KB, vs. the map view's ~200 KB).
- **2026-08-07 (Wren, Day 91):** *Two things: where a drawn light can and can't put a shadow, and the stale "the room is exempt from the hour" claim that had been keeping the inside gallery season-only.* (1) **A cast shadow needs a surface with width in the frame — not just a light.** Day 90 established that indoors a shadow is legal because the fire is drawn; the natural next step (the candle's shadows on the mantle) is *not* buildable, and the reason is geometry, not faintness. Work the projection: flame at hearth-local (x=11.5, y≈57 native), stone crown at (x=-2..2, y=52) → the shadow tip lands near x=-11, i.e. **7 native past the mantle's own left end** (the shelf runs x=-4..28). A low near light throws *long*. But `.hearth__mantle` is a 32×3 slab seen **edge-on** — we draw its front face; its top surface is a line with no width — so there is nowhere for that long shadow to lie. **The floor takes cast shadows because it is a plane the view looks across (a 24-native band with depth to set things back into); the mantle takes lit sides instead because it is a shelf the view looks at.** Rule of thumb for the next surface: ask what plane the shadow would land on and whether that plane has area in *this* projection, before asking whether the light is drawn. (2) **At 4–5 native px, draw the LIT side only — never the shaded one.** First pass gave the stone and jar a genuine away-side shade using `--c-wood` (#a87253); that is the **plank wall's own colour**, so the unlit half of each object dissolved into the boards behind it and the silhouette died. Two tones (body #c19465 + lit #d4a373), lit cells on the flame-facing diagonal, and the light reads fine. (3) `--candle-h` was **hoisted from `.mantle-item--candle` to `.hearth`** so `.candle-light` can read it — the candle/wick/flame still inherit it unchanged; the two `data-tod` overrides moved to `.scene--inside[data-tod=…] .hearth`. (4) The new `.candle-light` answers the **hour**, not the year (nothing feeds a candle; only its own flame height changes), so it **sinks and never resizes** — the opposite knob from `.hearth-light`, which resizes with the season and never moves. (5) Also rebased `.mantle-item--jar` + `.jar__sprig` (and both leaf pseudo-elements) from hardcoded `* 3` to `var(--s)`, retiring their whole 2x block — the same move Day 77 made for the candle, Day 88 for the fire, Day 90 for the chair. **Only `.coat__cloak` and the window/sill group still carry hardcoded `* 3` in inside.css.** (6) **The Day-89 note that "an indoor HOUR rail would be four identical pictures" was already false when it was written** and is now the reason a whole clock lived nowhere: the room turns back the hour *wash* at its door (Day 46) and the season wash too (Day 88), but the candle's wax (Day 77), the sprig's lean (Day 78) and the window glass (Day 46) are all keyed to `data-tod`. `GALLERY_STATES` now carries `inside-dawn` / `inside-dusk` / `inside-night` (plain `day` is deliberately omitted — it *is* the `-state-inside-summer` frame). **Exempt-from-the-wash ≠ exempt-from-the-hour; check for `data-tod` selectors before claiming a view is hour-blind.** A commit now produces 8 view + 15 state + 3 motion = **26 PNGs**. `pages.yml` needed no change. Gotcha when dry-running gallery mode locally: `node scripts/screenshot.js --gallery …` launches its own browser and the repo's pinned Playwright looks for build **1148** while the pre-staged binary is **1194** — export `COZY_CABIN_CHROMIUM_PATH=/opt/pw-browsers/chromium_headless_shell-1194/chrome-linux/headless_shell` (screenshot.js honours it) and serve the tree with `python3 -m http.server`.
- **2026-08-08 (Wren, Day 92):** *A cast shadow needs three things, and the third one is the one that bit: a drawn light (Day 90), a surface with area in this projection (Day 91), and — new today — that surface actually being **lit in the frame**.* The inside wall had the first two in abundance: the fire is drawn, and the chimney breast is a big flat plane the view looks straight at. But nothing had ever lit it. The Day-89 pool went on the *floor* and the Day-91 candle glow went on the brick *above the shelf*, and the great vertical face between them had been the flat `--c-sky-mid` of unlit brick since Day 11. So the mantle's shadow could not be drawn until the light it interrupts existed — shade laid on an unlit surface is not a shadow, it's a stain. **Check for the lit ground before reaching for the blocker; it is the same check Day 91 made for the mantle's top, one level down.** Practical notes from building the pair (`.fire-wall-light` + `.mantle-shadow`, both inside `.hearth`, both native px × `var(--s)` so the 2x block needs no override): (1) **`clip-path: inset()` beats a gradient fade when a hard blocker stops a light.** The glow is an ellipse centred on the fire (hearth-local x=12, y=20) cut flat at *both* edges — `y=26`, the firebox lintel (below it is the dark cavity, which must stay dark, or floor, which already has the pool), and `y=46`, the mantle's underside. Fading toward the shelf reads as "the light petered out"; clipping reads as "something is in the way," which is the whole point. Both insets are computed from `--wl-h` so the season table drives them: `inset(calc((var(--wl-h)/2 - 26px) * var(--s)) 0 calc((var(--wl-h)/2 + 6px) * var(--s)) 0)`. (2) **Two soft gradients in the same footprint cancel and you see neither.** First pass drew the shadow as `radial-gradient(ellipse farthest-side at 50% 100%)` — deepest above the shelf's middle, which is *exactly* where the candle's glow peaks (it is centred at x=11.5, the shelf's own centre, and spans y=46–68). A/B'd against `display:none` and the two washes annulled each other to nothing. Fix: make the shadow **uniform along the shelf** (a `linear-gradient(to top)` for the body plus a horizontal `mask-image` fading only the last ~14% at each end, which is where the penumbra genuinely opens) and give it a **rise that clears the candle's reach** (20/24/28 native by season vs the candle's ~19), so the glow reads as a lit pocket carved out of shade instead of a patch on plain brick. **When adding a light or a shade indoors, check what else already occupies that footprint — and A/B it with `display:none` rather than trusting the eye on a single frame.** (3) **Painting order at `z-index: 1` inside `.hearth` is set by DOM order in `inside/index.html`, and it is the argument, not a detail:** `.fire-wall-light` → `.mantle-shadow` → `.candle-light` → `.hearth__mantle`. Glow first; shadow over the glow (it subtracts it); the candle's light over the shadow (it falls *on* shaded brick); the shelf last, flat dark wood in front of everything it blocks. Moving any one of them changes what the picture claims. (4) Small correction to the Day-91 note: **`.hearth__mantle` also still carries hardcoded `* 3`** (with a 2x override at the bottom of the file), alongside `.coat__cloak` and the window/sill group — worth knowing, because anything new anchored to the shelf line writes `calc(49px * var(--s))` and you have to check the mantle's *own* 2x block agrees (it does: 46×2 + 3×2 = 98 = 49×2). (5) The inside season rail already in `GALLERY_STATES` (Day 89) catches all of this with no `screenshot.js` change — the whole build is season-driven and invisible in an August live shot.
- **2026-08-16 (Evan):** `scripts/views.json` is now a **mutable carve-out** (RULES.md Article I, dated 2026-08-16), answering the Day-94/95 logs' founder-attention note — so `/diary/` and `/letters/` (and any future room) can get preview coverage by editing the manifest directly, no founder round-trip. The action-ask is `messages/open/2026-08-16-views-json-unlocked.md`. Hard constraint carried in the carve-out: the `"home"` entry (empty `url_path`, unsuffixed PNG) is `wait-for-deploy.sh`'s locked deploy contract — add entries, never rename or remove `home`. This narrows the 2026-05-18 entry's "views.json only" recipe from "locked, ask Evan" to "yours to edit."
- **2026-08-08 (Evan):** The private deterministic post adds a mailbox pass to `.claude/commands/daily.md` Step 2.5; the pondering pre-draft is therefore Step 2.9, not Step 2.8. If the cloud routine prompt duplicates that numbered pointer rather than simply invoking `/daily`, mirror the one-number change there. The routine schedule itself does not move and no Claude routine carries post.
- **2026-08-11 (Wren, Day 95):** *`sky.js`'s four hour-bands now have edges that slide with the year, and the way to photograph that is the opposite of the way the gallery photographs everything else.* (1) **The change.** `phaseFor()` took an `hour` and compared it against fixed constants (5 / 8 / 17 / 21) — the same clock hours in December as in June, which had been quietly false since `season.js` gave the yard the year on Day 69. It now takes a `Date`, and a new `yearSwing(date)` returns −1 at midwinter → +1 at midsummer (`-cos(2π(dayOfYear+10)/365.25)`; the +10 is the gap between the solstice and New Year, and 365.25 covers leap drift). Every edge slides by `EDGE_SWING_H * swing`, `EDGE_SWING_H = 1.5` — so the neutral `day` band is 12h wide at midsummer and 6h at midwinter, and `dusk` starts at 15:30 in December and 18:30 in June. `getMinutes()` is folded in now, so the edges land on the half-hour rather than snapping to whole hours. **This does not give the sun an address** (the standing Days 56–58 refusal): no edge of the frame brightens, nothing leans toward a pointable position, and no colour changed — the year gets a *length*, not a direction. It also adds no latitude; `EDGE_SWING_H` is the swing itself, picked for legibility, and the only geography assumed is the northern-temperate year `season.js` already declares in its own comment. (2) **Everything keyed to `data-tod` moves with it for free** — the lantern kindles earlier on a December afternoon, the candle's wax spends earlier, the sprig rests earlier, the window glass cools earlier — because they all read the tag, not the clock. (3) **Gotcha worth the whole entry: `GALLERY_STATES` forcing cannot show this.** Forcing `data-tod`/`data-season` by hand answers *what does dusk look like* and structurally cannot answer *when is it dusk* — the frame sets the answer itself. So `captureState()` gained an alternative: a state carrying **`clock: '<ISO local datetime>'`** gets `freezeClock(page, iso)` (a `page.addInitScript` that swaps `window.Date` for a frozen shim **before any page script runs**) and is **not** forced at all — `sky.js`, `season.js`, `garden.js` and `door-plant.js` each reckon their own state from that instant, exactly as they would for a visitor. Shim details that matter: keep `Fake.prototype = Real.prototype` (so `getUTCFullYear()` etc. work on the returned instance) and re-export `Date.now` / `Date.parse` / `Date.UTC` (`garden.js` and `door-plant.js` call `Date.UTC` directly and would throw otherwise). `new Date(iso)` with no zone parses as *local*, and both the CI runner and the sandbox are UTC, so the frame is deterministic. (4) **New pair: `previews/<date>-<sha>-state-evening-{midsummer,midwinter}.png`** — 20:00 on the two solstices, i.e. one wall-clock minute photographed twice: gold dusk on one, full dark with the winter stars on the other. Deliberately only two frames, not four: the equinoxes sit at swing ≈ 0 (the old fixed edges) and would only photograph the un-news, and a rail costs ~0.2 MB a commit forever. The `-state-evening-` infix is a longer sibling of `-state-`, collides with nothing, and the memory-pass glob `ls ${STEM}*.png` picks it up. A commit now produces 8 view + 17 state + 3 motion = **28 PNGs**; `pages.yml` needed no change. (5) **Dry-running `--gallery` locally** still needs the Day-91 recipe (`COZY_CABIN_CHROMIUM_PATH=/opt/pw-browsers/chromium_headless_shell-1194/chrome-linux/headless_shell`, plus `python3 -m http.server`), and it is worth the ~3 minutes: it re-renders all 17 states, so it doubles as the regression check that the `if (!state.clock)` branch didn't break the forced ones.
- **2026-08-12 (Wren, Day 96):** *Two gotchas for anyone writing a `/tmp/` Playwright test against `/letters/`, plus the letter-day sequence.* (1) **The front-yard mailbox's selector is `.sprite--mailbox`, not `.mailbox`** — `scene.html` renders it as `<a class="sprite sprite--mailbox" href="letters/">` and the `mailbox__post` / `__flag` / `__box` / `__plate` / `__name` spans are its *children*, so there is no bare `.mailbox` element to click. (2) **`letters.js` `await`s a `fetch` per card inside its render loop**, so the `<li>`s appear one at a time and a bare `locator.count()` straight after `waitForURL` races a half-built list (it returned 2 of 4 for me). Gate on the final count first: `await page.waitForFunction(() => document.querySelectorAll('.letters-list__letter').length === N)`. Useful assertions once it settles: the card's `.letter__day` / `.letter__dir` text (`left in the box` vs `arrived`), that `.letter__paper` holds >N paragraphs and does *not* contain the `would not unfold` fetch-failure fallback, that no `**Label:**` header line leaked into the rendered body (the renderer strips `#`/`**` lines), and that the last `.letter__sign` is the signature. (3) **Letter-day order that worked:** write `letters/out/<date>-<slug>.md` → `node tools/post-status.js --self wren` (it must print no `UNSENDABLE`, and afterwards `TURN` should read `WAIT`, which is the proof the send registered) → add one `LETTERS` entry at the head of the array in `letters.js` → test → commit. **Every letter after the two grandfathered founding ones needs a `**To:** Gnomon` line** in the unbroken header run — `FOUNDING` in `post-status.js` exempts only `out/2026-07-28-…` and `out/2026-08-07-…`. Note the day's code commit must **not** carry the pre-drafted `diary/<today>.md` from Step 2.9 — `git add -A` will stage it, so `git restore --staged diary/<today>.md` before committing, or the two-commit split collapses.
- **2026-08-13 (Wren, Day 97):** *A second page may not keep a second copy of a rule the scene runs — and `scripts/views.json` is locked, so a new record page gets no CI preview.* (1) **The shared-reckoning pattern.** `/almanac/` publishes what the clearing will do on any day and hour (the answer to Day 96's "am I actually untrustable"). The only thing that makes such a page worth anything is that it and the scene run *the same lines*: an almanac with its own copy of the season table or the band edges could be right on a morning the yard was wrong, which is precisely the failure publishing-the-working is meant to make impossible. So `sky.js` and `season.js` each gained a small **read-only export block immediately before their `readyState` check** — `window.CabinSky = { EDGE_SWING_H, yearSwing, edgesFor, phaseFor }` and `window.CabinSeason = { seasonFor, seasonForDate }` — and `almanac/almanac.js` keeps **no arithmetic of its own**, only prose. `phaseFor` was refactored to call the new `edgesFor` so there is one place the four numbers live. Gotchas: (a) both scripts are **safe to load on a page with no scene** — `tagAll` finds nothing and `getElementById('cabin-scene')` returns null, so `init()` returns; (b) the export must sit *before* the `if (document.readyState === 'loading')` branch so it exists at parse time, since the consumer is also `defer`red and deferred scripts run in document order (sky/season in `<head>`, almanac at the end of `<body>` → order is guaranteed); (c) `screenshot.js --gallery`'s `freezeClock` shim is unaffected — nothing new calls `new Date()` at parse time. (2) **What the almanac page *does* own is prose**, and that prose is a hand-written description of `scene.css` + `inside.css` season/hour gates (`SEASON` and `HOUR` in `almanac.js`). **It is the one thing here that can silently go stale**: if a future day changes a `data-season`/`data-tod` layer, come and mend the matching line, or the published working becomes a claim the clearing no longer keeps — worse than no claim. (3) **`scripts/views.json` is under the Article-I `scripts/` lock** (the carve-out is `screenshot.js` and `pages.yml` only), so a new page cannot be added to the CI preview manifest. That's consistent anyway — `/diary/` and `/letters/` aren't in it either; record pages live outside the view previews. Note the Day-10 learned entry says "to add a new view: edit `scripts/views.json` only" — true of the mechanism, but not something today's agent may do. (4) Map chip pattern for a *book* rather than a place: `.map__zone--almanac` on the **west margin** (`left:3%; top:50%; 24%×18%` → 80×45px at 375, clear of `.map__tree--nw` at 8–23%×20–40% and of `.map__zone--diary` at `top:76%`), with a face-on ledger glyph so it doesn't read as the diary's spine-on book twice. Two books on one edge of the drawing. (5) The page deliberately **does not render the scene at the chosen hour** — the clearing still only ever shows the hour you actually arrived at (sky.js's "a day that greets, not a day that turns"). It is a *statement* you can hold the place to, not a way of standing in a moment you aren't in; keep that line if the page is ever extended.
- **2026-08-15 (Wren, Day 99):** *A witness that asks "what is it" is structurally blind to "did it go to nothing" — the almanac's checks now come in two kinds, and the second one walks the whole wheel.* (1) **The shape.** Day 98's `CHECKS` all take a reading in a *named* state and hold it to a number (`expect`) or to an ordering (`rising`/`falling`). Every one of them is a description. The things this place actually promises — winter is a hush and not a death, nothing here is ever lost — are *refusals*, and a refusal has no reading (there is no number that comes back with a hush in it). New third check kind: **`floor: N` + `over: [states…]`** — the probe's reading must never fall below `N` in **any** state in `over`, and `over` is always the *whole* round (all four seasons, or all four bands), not the states some sentence happens to name. `stateNames()` in `tools/check-almanac.js` gained an `check.over` branch and `verdictsFor()` a `check.floor !== undefined` branch ahead of the `expect` branch; nothing else in the runner changed, because the vow checks arrive in the same `CHECKS` array off the same page. (2) **Proof it isn't decoration, and the reason the kind was needed at all:** adding `.scene[data-season="winter"] .sprite--tree-small { display: none }` to scene.css turned exactly one line red (`tree-crowns ✗ winter: never below 2, read 1`) — and **all ten Day-98 reading checks stayed green through a bare winter branch**, because not one of them asks about the trees. Break something whenever you add a witness; break it in a way the *existing* witnesses would miss, which is the only way to learn what a new kind is for. (3) **A `floor: 1` on a laid-out px size is a *presence* floor, not a size claim**, so it stays honest at both breakpoints and doesn't violate the Day-98 rule that no check states an expected width. Counts and opacities are scale-free anyway. (4) **The return leg needed no new machinery** — "what spends is given back" is just `rising` on the wrap-around pair (`['night','dawn']` for the candle, `['spring','summer','autumn']` for the pile). (5) **`VOWS` is published on `window.CabinAlmanac` alongside PROBES/CHECKS**, and a check carrying `vow: '<key>'` renders under its vow in the new `#almanac-vows` block instead of in `#almanac-checks` — so if you add a vow check, give it a `vow` key or it lands in the wrong list. The third vow (**the light never gets an address**) carries `unwitnessed:` prose instead of any check and says so on the page: evenness is what a measurement is worst at, and a list that read as complete while covering two of three would be the exact failure the almanac exists to refuse. **If you ever find a witness for it, that's the one still open.** (6) Cost: 20 checks, 12 probes, 10 states, 24 view-reads (was 19) — the three new states are `spring|day` on *inside* and `summer|dawn` / `summer|dusk` on *home*. `pages.yml` needed no change.
- **2026-08-14 (Wren, Day 98):** *The almanac's prose now has witnesses, and the one gotcha worth the whole entry is that **a visible-count probe must not read `opacity` — half this clearing blinks**.* (1) **The shape.** `almanac/almanac.js` gained two declarative tables beside its `SEASON`/`HOUR` prose — `PROBES` (where to look, what to read: `visible-count` / `opacity` / `glowing` / `width` / `height`, plus an optional `pseudo` for the `::before` cast shadows) and `CHECKS` (one sentence of the prose held to one probe, varying on `axis: 'season'|'tod'` with the other clock pinned by `at`, asserting either `expect: {state: number}` or `rising`/`falling: [states…]`). Both are published as `window.CabinAlmanac` — the same read-only-export move sky.js/season.js made on Day 97, one level out — and `tools/check-almanac.js` reads them *off the page* rather than keeping its own list, so the runner can only ever test claims the page actually makes. Add a check in almanac.js and CI picks it up with no second edit. **`tools/` is not under the Article-I lock** (locked: RULES, MILESTONES, theme.css, `.claude/`, `scripts/` except screenshot.js, `.github/` except pages.yml), which is why the runner lives there and not in `scripts/`. (2) **The gotcha.** First run reported `smoke-puffs` as 2 in *every* season. Nothing was wrong with the yard: `.smoke-puff` fades out at the top of each rise, so an opacity-aware "is it showing" test counts whichever puffs happen to be mid-fade as absent. **Count on layout, not on brightness** — `el.getClientRects().length > 0` is the right test (empty iff the element *or any ancestor* is `display:none`, which is how both counted layers are actually gated: the woodpile's spent logs, winter's fourth puff), plus a `visibility` check. Where a layer really *is* opacity-gated (fireflies, winter stars) give it an `opacity` probe on the container instead. A thing that blinks is still there. (3) **Sizes are compared, never stated.** `--s` is 3 desktop / 2 phone, so no check asserts an absolute width; the size probes only ever use `rising`/`falling`, which is all the prose ever claims anyway ("a stride further", "half again as long"). Counts and opacities are scale-free and stated outright. (4) **Prove the check can fail.** A check that can't go red is decoration — I deleted the `.woodpile-log--9` autumn rule from scene.css, re-ran, got `BROKE … ✗ autumn: wanted 9, read 8`, and restored. Do this whenever you add a witness. (5) **Cost and wiring.** States are forced exactly as `screenshot.js --gallery` forces them (set `data-season`/`data-tod` on every `.scene` after load, when sky.js/season.js have run and disconnected, then wait `2000ms` for the 1.6s washes/transitions). The runner visits only the views a given state owes a reading to — 19 page loads for the current 10 checks, ~50s locally including npm setup. Wired into `pages.yml` as the **last** step of the screenshot job, **without** `continue-on-error` (unlike gallery/motion): it is meant to turn the run red, and it sits after the previews are pushed so a red check never costs the record a picture. (6) **In-session run:** `./scripts/local-snapshot.sh /tmp/<shim>.js` where the shim spawns `node tools/check-almanac.js $COZY_CABIN_URL` — the runner falls back to `COZY_CABIN_URL`, then to the deployed site. (7) **Not every sentence has a witness, on purpose** — a wash's colour, a fog's lit rim, puffs "spreading wider by the top" are read by eye; the check is a *floor* under the prose, not a proof of it. Add a witness whenever a claim becomes countable, and keep mending the unwitnessed lines by hand (the Day-97 note still stands for those).
- **2026-08-17 (Wren, Day 101):** *The preview manifest now has two kinds of page in it, and the reason is arithmetic, not taste — a record room cannot be photographed the way a scene is.* Closing the Day-100 unlock (`messages/done/2026-08-16-views-json-unlocked.md`), `scripts/views.json` gained `diary`, `letters` and `almanac`. (1) **The measurement that decided the shape.** These are the first *scrolling* pages in the manifest, and `capture()` had always passed `fullPage: true` — harmless for the four scenes, whose `scrollHeight` is exactly 800. On the record rooms, measured on the working tree: `/diary/` 8680px → **783 KB**, `/letters/` 9265px → **2736 KB**, `/almanac/` 6997px → **1979 KB**. At the manifest's two widths that is **~11 MB per commit** against a 2.1 MB whole-set budget, and two of the three grow a card a day forever. Viewport-only costs **89 / 221 / 171 KB** and stays that size on Day 500. (2) **The knob.** A manifest entry may now carry `"kind"`: `"scene"` (default; every pre-existing entry, behaviour byte-identical — two widths, fullPage) or `"record"` (one PNG, 375×800, **viewport height only**, no `-phone` twin). `capture()` gained a fifth arg `fullPage = true`. Naming is unchanged (`previews/<date>-<sha>-<name>.png`), so the memory-pass glob `ls ${STEM}*.png` picks the rooms up automatically — a commit now produces **11 view + 17 state + 3 motion = 31 PNGs**, ~2.5 MB (was 8 view / 28 PNGs / 2.1 MB: 7 manifest entries now, but the three rooms are one capture each rather than two, so +3 files and +475 KB measured on the deploy). (3) **Gotcha — the `-phone` twin argument I nearly got wrong.** I was about to justify one width by asserting the rooms lay out identically at 375 and 390. **They don't**: `/letters/` ends 429px shorter at 390 because text rewraps. The true and sufficient claim is narrower — the content box *tracks the viewport with no snap* between the two (375px at 375, 390px at 390), so there is no breakpoint band on these pages of the kind the Day-34 twin was built to catch. If you add a record room whose layout is px-scaled rather than flowing, that argument does not transfer and it wants `"kind": "scene"`. (4) **Two guards, both proven to fire** (Day 98's rule: a guard that can't fail is decoration) — an unknown `kind` throws, and `home` marked `"record"` throws, because home's unsuffixed fullPage narrow PNG is `wait-for-deploy.sh`'s locked contract. (5) `pages.yml` needed no change; the gallery and motion modes don't read the manifest. (6) The `almanac` entry is the one I added beyond the ask, on the grounds that its hand-written prose is the single thing here that can go stale silently (Day 97) — a picture of it at the head of every future memory pass is the cheapest guard it will get.
- **2026-08-18 (Wren, Day 102):** *A vow of absence wants a `ceiling`, not a `floor` — and the letters shelf was silently eating any body line that opened with `**`.* (1) **The third vow's witness.** Day 99 left "the light never gets an address" deliberately unheld, on the reasoning that "evenness is what a measurement is worst at… you can go and find a lean, you cannot go and find the absence of one." That's true of asking *one* place what it looks like, and false of asking *two named places whether they agree* — which is exactly the far keeper's trade (his tower works every date by two methods "so that they are able to disagree"). New check kind **`ceiling` + `over`**, the exact mirror of Day 99's `floor` + `over`: the reading may never rise *above* N in any state in `over`. A floor holds a vow of presence (never nothing); a ceiling holds a vow of absence (never a lean). `stateNames()`/`workNeeded()` in `tools/check-almanac.js` needed **no change** — `ceiling` reuses `over` — only a new branch in `verdictsFor()` ahead of the `expect` branch, and a branch in `readingOf()` in almanac.js. (2) **Two new probe kinds, both returning 0 when nothing points sideways.** `lean` walks every element the selector matches and counts background gradients with any horizontal component: `to top`/`to bottom`/no-direction read 0 (a low sun is low from *everywhere* in the frame at once, so vertical weighting is not an address); `to left`/`to right`/any `deg` not ≡ 0 mod 180 reads 1 apiece; a unit the parser doesn't know (`rad`/`turn`) counts as leaning, because a witness that shrugs is worse than none. `mirror` parses computed `box-shadow` layers (split on commas *outside* parens — `rgb(…)` contains commas), takes the layer with the smallest x-offset and the largest, and returns the widest single-channel colour distance between them; 0 means the two sides of a lit body were lit alike. (3) **Scope matters and got checked first:** a blanket "no horizontal gradient in the scene" would false-positive all over — brick mortar joints, the bee's stripes, the bench slats and the lantern's came are all `90deg` on purpose. The five probes are pointed only at what is *light*: `.scene::after` and `.scene::before` (the two whole-frame washes) on **both** exterior faces, `.hill::before` (the far crests' rim), and `.cloud` (the underlit belly lumps). (4) **All four break cleanly** — dusk wash → `to top right` gives `read 1`; dawn `.hill::before` → `to left` gives `read 4` (four hills, and the count is per element, which is why `lean` sums rather than maxes); one cloud belly lump recoloured gives `read 22`. Breaking the *shared* dusk wash reddens `hour-wash-lean` **and** `door-wash-lean` together, which is the proof that probing the second face isn't redundant: the vow's wording is "no place in *any* frame." (5) **New `blind` field on a VOW, and it's the point of the day.** `unwitnessed` (Day 99) renders *instead of* a witness list; `blind` renders *underneath* one, and says what the check cannot see — a sprite redrawn brighter down one edge, a half-frame overlay, a second cloud rule for the left of the sky all read green. This came straight out of the morning's post: the far keeper's two checks were honestly green through six days of a wrong number because neither had ever been pointed at whether the number was *true*. **When you add a witness, write its blind spot beside it.** `renderVows()` appends `blind` after the held list; `almanac.css` needed `.almanac-vows .almanac-checks + .almanac-vow__unwitnessed { margin-top: var(--sp-2) }` since that italic note now *follows* a list instead of replacing one. (6) **Cost:** CHECKS 20 → 25, PROBES 12 → 17, states unchanged at 10, view-reads 24 → 31; `pages.yml` and `scripts/views.json` needed no change. (7) **Separate and important — `letters/letters.js` was dropping body lines.** The Day-81 renderer skipped any line matching `t.startsWith('**')`, meant for the `**Left in the box:**` / `**From:**` / `**To:**` header run. Gnomon's third letter opens its *body* with a bold sentence (`**The numbers I sent you on the seventh were wrong.**`) and the shelf swallowed it whole — the one sentence the letter existed to carry. The `.md` is canonical and sealed; the renderer is yours. Fixed with two narrower rules: a meta line is `/^\*\*[^*]+:\*\*/` (bold label with the colon *inside* the bold) **and** still inside the unbroken run at the top (`inHead` flag, cleared by the first body line); everything else keeps its `**…**` and is rendered by a new `writeInline()` that splits on `**` and builds text nodes + `<strong>` elements — never innerHTML, and an unbalanced count falls back to plain text. Also: `---` now renders as `<hr class="letter__rule">`, so the `.letter__sign` test was narrowed from `startsWith('—') || startsWith('--')` to `'—'` only. **If you shelve a letter, read the rendered card, not just the file** — the Day-96 test recipe (`waitForFunction` on the final `.letters-list__letter` count) is the place to assert that a known sentence from the body actually reached the page.
- **2026-08-19 (Wren, Day 103):** *A guard aimed at places you chose can only catch what you already imagined — the `nowhere` vow's witness is now a whole-frame sweep minus a published allow-list, and the survey has to come before the list.* (1) **New probe kind `lean-sweep`** in `almanac/almanac.js` + `tools/check-almanac.js`: `selector` is a root (`.scene`), and the runner walks `[root, ...root.querySelectorAll('*')]` for **every** matched scene, reading `sideways()` off the element **and both its pseudo-elements**, skipping anything where `el.closest(a.selector)` hits an entry of the probe's `allow` list. It returns a **total, not a max** — two leans are worse than one, and the count is the only hint of how many places to go and look. It reuses the Day-102 `ceiling` + `over` check kind, so `stateNames`/`workNeeded`/`verdictsFor` needed **no change**; three probes (home/door/room) × both wheels = 6 new checks. (2) **Do the survey first.** A throwaway `/tmp/sweep.js` (same `sideways()` function, 3 views × 8 states, printing every leaning element) found exactly **12 distinct things**, all explicable — brick mortar `.brick-course`, the lantern came, the bee's stripes/wings, the bench slats, and the two indoor cast shadows. Guessing that list would have been wrong; the survey is what made an allow-list the honest shape rather than a cleverer rule. `LEAN_ALLOWED` carries `selector` / `what` / `why` per entry and is **rendered on the page** under the vow (`.almanac-allowed`), because an exception nobody can read is just a hole with a name. Selectors are matched with `closest`, so keep them narrow — a wide one silently exempts whatever a later day hangs inside it. (3) **Cost was almost nothing** because the sweep's states are ones other checks already open: view-reads 31 → 34 (only *around* gained autumn/winter/spring at `tod: day`), full run **64s / 10 page loads**. `pages.yml` and `scripts/views.json` needed no change. (4) **The break-test is the day's whole argument**: `.scene__ground`'s `to bottom` → `to bottom right` (a lit meadow) left **all five** Day-102 aimed probes honestly green and reddened `home-lean-sweep` *and* `door-lean-sweep` in all eight states — one shared ground rule, two frames. Keep the aimed probes anyway: a sweep says a frame leans, they say where. (5) **Gotcha:** a probe's `reads` string must name the measurement only — `checkItem()` appends `' in ' + VIEW_NAME[probe.view]`, so a `reads` that names its own view renders as "…on the other face of the house, in the door side". (6) **When a vow's guard changes shape, its `blind` line is stale by definition** — the Day-102 text said "the four places I went and looked", which stopped being true the moment the sweep landed. Mend the blind note in the same commit as the witness.
- **2026-08-21 (Wren, Day 105):** *The **book of names** now exists — `names/` (page `names/index.html`, book `names/names.js`, styles `names/names.css`), served at `/names/`. RULES.md Article III makes it a **mandatory** part of the morning read from today; it is ~1200 words, so read it whole.* (1) **Shape.** `names.js` holds one array, `NAMES`, of `{ word, forms[], note?, senses[] }`, each sense `{ since, day, says, mark }`. The **mark** is the load-bearing field and the whole design: the element, the array, the file or the property that sense points at. A sense you cannot write a mark for is not ready — a definition that is itself an abstraction makes fog look organised, which is worse than fog. Senses are numbered, dated and **appended, never revised**: a meaning that moves gets a *second* sense with the date it moved, exactly as the diary treats its own past entries. (2) **Published read-only as `window.CabinNames = { NAMES, OPENED, lookup }`** *before* the `readyState` branch (the Day-97 sky.js/season.js export move), and `init()` returns silently when there is no `#names-list`, so any page may load it for the data alone. `lookup(s)` normalises case, whitespace and curly apostrophes and matches the headword **or any `forms` entry**; unknown words return `null`. `forms` is *rendered on the page* ("also written: …") rather than kept as private machinery — a list only the code can see is one more thing to take on trust (Day 98). (3) **It is NOT on the map, deliberately.** The map already carries six chips and is visibly full at 375px, and this is not a room of the clearing — it is the diary's own vocabulary. Its visible door is a `.diary-aside` line in `diary/index.html`'s header; the hidden `#cabin-nav` strip in **all seven** shells lists it so nothing is pointer-only. If you add another record page, weigh the map's crowding before reaching for a seventh chip. (4) **`scripts/views.json` gained `{ "name": "names", "kind": "record" }`** — one 375×800 viewport PNG, `previews/<date>-<sha>-names.png`; the memory-pass glob picks it up. A commit now produces **12 view + 17 state + 3 motion = 32 PNGs**. (5) **The six words that earned a place** (11 senses): *the wash* (2 — the hour's, then the year's), *the shelf* (**4** — the gallery of forced pictures, the diary's page, the mantle, the box's page; the entry says outright that four things wear this word), *a vow*, *a witness*, *the weighing* (coined here — the Day-104 `frame-balance` instrument, which the diary had been re-describing in a paragraph each time while the log already had a one-word name for it), *the turn* (2 — the wheel that spends and gives back, and whose it is to write). **Selective, not exhaustive**: most words in the diary are just words, and a page where every third term was marked would be more tiring than the fog. (6) **Dates before 2026-08-21 were read back out of the diary**, and the page says so; each `since` is the entry where that meaning first appears, so the claim is checkable. Verify with `grep -ln` over `diary/*.md` before you write one — I found three of my first guesses wrong (the mantle sense of *shelf* is Day 91, not Day 11 when the mantle was drawn; *vow* is Day 69, not Day 99 when the vows were written out; the gallery sense of *shelf* is Day 72's letter, not Day 71's build). **The word's first use, not the thing's first day.** (7) **The test worth keeping** (`/tmp/test-names.js`, disposable): besides rendering and no-overflow, it walks every `<code>` in every mark, pulls out every filename-looking token, and **asserts the file exists in the working tree** — a book whose marks point nowhere is precisely the failure it exists to prevent. It also asserts `lookup` resolves all 25 forms, returns `null` for an undefined word, and that loading `names.js` on a page with no mount publishes the data and disturbs nothing. **Gotcha:** a leaked-inline-marker check must ignore text inside `<code>`/`<em>` — `previews/…-state-*.png` legitimately contains a `*`, and my first version failed on it. (8) **STILL OPEN** (the message stays in `messages/open/`): the diary page must mark a defined term's **first appearance per entry** with a dotted underline and a definition reachable on hover *and* on tap. That needs something this project has never had — **the diary's entries are not rendered as HTML at all**; `diary/diary.js` links each card at the raw `.md`, which GitHub Pages serves as `text/plain`. So the marking day is really a *reader* day: a small markdown renderer (`letters/letters.js` is the working model, including its `writeInline` discipline of text-nodes-not-innerHTML), then a text-node walk calling `CabinNames.lookup`. Two constraints when you build it: **nothing dated before 2026-08-21 may be marked**, and don't render all 105 entries inline on the shelf — Day 101 measured what a full-page shot of that room already costs.
- **2026-08-24 (Wren, Day 108):** *Two Playwright gotchas for testing a CSS animation at an exact phase, and the arithmetic that decides whether a still record can hold a slow loop at all.* (1) **`animation-delay` does NOT set an absolute phase on an already-paused animation.** The obvious recipe — `animation-play-state: paused !important` plus a swept `animation-delay: -Xs` — is wrong: Chromium computes the animation's current time as *(elapsed-at-pause + delay)*, so every seek carries a constant offset equal to however long the page ran before you injected the pause (~1.5% of a 30s loop here). It hides on samples in the middle of a hold and shows up as a sub-pixel drift on samples near a keyframe boundary. Use the **Web Animations API**: `const a = el.getAnimations().find(x => x.animationName === 'bee-forage'); a.pause(); a.currentTime = fraction * periodMs;` — exact, repeatable, and it also lets you assert on `a.animationName` so a renamed keyframe set fails loudly instead of silently testing the wrong thing. Pause the neighbours you don't want moving the same way (`h.getAnimations().forEach(x => { x.pause(); x.currentTime = 0; })`). (2) **`page.addStyleTag()` accepts only `content`/`path`/`url` — there is no `id` option**, so a later `document.getElementById('…').textContent = …` throws `Cannot set properties of null`. Append the `<style>` from inside `page.evaluate` if you mean to rewrite it. (3) **For "did this move?", compare the animated property, not the rect.** `getBoundingClientRect()` carries ~0.05px of sample-to-sample noise that has nothing to do with the animation; `getComputedStyle(el).transform` is exactly what the keyframes set, and string-compares cleanly. (4) **Landing a sprite on another sprite: measure, don't guess, and measure at every breakpoint.** The flower blooms are a 3×3 `.flower__head` with petals stamped ±3px by `box-shadow`, and `getBoundingClientRect()` ignores box-shadow — so the *top petal* is `head.top-3 … head.top` and has to be computed, not read. The home patch is laid out twice (3x default, 2x under `max-width: 599px`), so a landing offset that is right at 900px is wrong at 375; the break-test confirmed the two are checked independently (breaking the 2x stop reddened 375 and 390 and left 900 green). 375 and 390 gave identical offsets, since the 2x block covers the whole phone band (Day-34 note). Also: land on `flower--1..3` only — `flower--4..7` are added by `garden.js` from `garden.json` on a date, and a landing must not depend on a stem a different date could take away. (5) **The still record has a fixed, measurable phase, and it is early.** `capture()` shoots at networkidle + 1500ms and `captureState()` at networkidle + force + `WASH_SETTLE_MS` (2000) — measured as **6.6–6.7%** and **8.3%** of a 30s loop, stable across runs. So *any* animation whose interesting moment falls later than ~10% of its period is invisible in `previews/` forever, and the motion strips can't rescue it either: `MOTION_FRAMES` 6 × `MOTION_INTERVAL_MS` 900 spans 4.5s. **Before giving a sprite a loop longer than ~10s, work out where in it the camera will be standing.** The fix belongs to `scripts/screenshot.js` (the standing carve-out) — a per-clip `frames`/`intervalMs`/crop override on `--motion`, or a gallery state that seeks `currentTime` the way Day 95's `clock:` states freeze the date — never to tuning the choreography so it happens to be photogenic at 8%. (6) Also this day: the almanac cannot witness a motion. Every `CHECKS` entry varies on `axis: 'season'|'tod'` with the other clock pinned by `at`; a loop gated on neither has no slot on a page that answers "what does this place do at a given day and hour."
- **2026-08-22 (Wren, Day 106):** *The diary's entries are rendered now — `diary/entry.html` + `diary/entry.js` — and the two things that cost the day were both about the difference between a page that shows a file and a page that quietly rewrites one.* Closing the last criterion of `messages/done/2026-08-21-shorter-entries-and-a-book-of-names.md`. (1) **Shape.** `entry.html?d=YYYY-MM-DD` resolves the day off `diary/manifest.json` (never off a path in the URL, so a card can't point at a file the shelf doesn't list) and fetches its `.md`; **with no parameter it opens the newest day**, which is the whole reason `scripts/views.json` can carry `{ "name": "entry", "url_path": "diary/entry.html", "kind": "record" }` — the URL never goes stale, and `previews/<date>-<sha>-entry.png` keeps the newest day, marks and all. A commit now produces **12 view + 17 state + 3 motion = 32 PNGs**. `diary/diary.js`'s cards now link at `entry.html?d=<date>` instead of the raw `.md`; the `.md` files still deploy and the metas' relative links to them are untouched. The reader loads `../names/names.js` for its data only (no `#names-list` on the page, so `init()` returns) — the Day-97 export move paying off a second time. (2) **The markdown subset is exactly what the diary contains, checked across all 106 entries**: `#`/`##` headings, hard-wrapped paragraphs joined with one space, `- ` bullets, ``` fences, `---` rules, and inline `` `code` `` / `**strong**` / `*em*`. **Emphasis has to recurse** — three old entries bold a whole sentence with a filename in backticks inside it, and a flat pass sets the backticks as themselves. Code does *not* recurse: a backtick run means those characters exactly. (3) **The guard worth copying: a word-for-word check over the whole record.** The day's `/tmp/` test loads every one of the 106 days, takes `innerText` of `.entry-body`, strips the markdown markers from the source file, and asserts the two word lists are *identical*. Use `innerText`, not `textContent` — the latter concatenates block elements with no space and every entry "diverges at word 4". Proved it reddens by making `flushPara` skip short paragraphs. This exists because of Day 102, where `letters/letters.js` silently ate the one bold sentence a letter was written to carry: **a renderer that drops a line quietly is worse than no renderer.** (4) **Gotcha that cost two rounds — Chromium blockifies `<button>` to `inline-block` no matter what `display` you give it.** A marked word therefore gets a box a whole line-height tall, the `border-bottom` lands ~5px clear of the word (reading as a stray rule *under* the sentence) and the line's leading opens up. `line-height: inherit` + `vertical-align: baseline` do **not** fix it; only not being a button does. The mark is a `<span role="button" tabindex="0" aria-expanded>` with Enter/Space handled by hand — everything the real element would have given for free, minus the layout. (5) **Gotcha two — a tap fires `focusin` *before* `click`.** A focus-opens rule plus a click-toggles rule open the gloss and shut it again in the same tap. A `viaPointer` flag set on `pointerdown` keeps focus-opening to the keyboard, where it was the only thing wanted. Related: on a **fine** pointer a click must only ever *open* (hover already governs, so a toggle would meet an open card and close it); on a **coarse** one the second tap is the only way to close. Headless Chromium reports `(hover: hover) and (pointer: fine)` as **true**, so the touch path needs its own `browser.newContext({ hasTouch: true, isMobile: true })` — test both or you have tested neither. (6) The gloss is one reused card, anchored under the term, clamped into the viewport, and **flipped above the term when below won't hold it** — "the shelf" has four senses and runs 400px tall, which is half a phone. (7) **Nothing dated before `CabinNames.OPENED` is ever marked**, per the founder's rule. His acceptance criterion also said pre-2026-08-21 entries must "render exactly as they do now", which taken literally would mean leaving old cards pointing at raw text; I read it as the no-retroactive-marking rule it sits beside and rendered every day alike, marking only from the book's own date — flagged plainly in the message's completion notes for him to overrule.
- **2026-08-25 (Wren, Day 109):** *Every camera in `screenshot.js` had stood at whatever phase the clock happened to hand it, and the fix is not a longer wait — it is `Animation.currentTime`.* Closing Day 108's question (`diary/2026-08-24.md`). (1) **The measurement that decided the shape.** Day 108 measured the still record's fixed phase — `capture()` shoots at networkidle + 1500ms (**6.6–6.7%** of a 30s loop), `captureState()` at networkidle + force + `WASH_SETTLE_MS` 2000 (**8.3%**) — and the wall-clock strips span `MOTION_FRAMES × MOTION_INTERVAL_MS` = **4.5s**. The bee alights at **18%** and **60%** of a 30s round (9.0s and 18.0s), so *no* amount of the existing machinery could reach either; a strip long enough to span 30s at 6 frames would cost 30s of CI wall clock per clip and still land on six arbitrary moments. (2) **The knob: a `--motion` clip may carry `seek` instead of being sampled.** `seek: { animation: '<keyframes-name>', at: [{ frac, label }, …] }`. `seekScene()` calls `document.getAnimations()`, finds the target by **`a.animationName`** (a renamed keyframe set therefore throws rather than silently filming the wrong loop — break-tested by renaming to `bee-drift`, which fails the run with `no running animation named "bee-drift"`), reads the round off `target.effect.getComputedTiming().duration`, then **pauses every animation and sets `currentTime` to the same *absolute* ms on those whose `iterations === Infinity`.** Three things matter here: (a) *absolute*, not each-to-its-own-fraction — infinite iterations wrap on their own, so every layer lands at its honest phase of that second; dragging each to the same fraction of its own period would be a lie about their relative rates; (b) **negative `animation-delay` is preserved** (`currentTime` includes delay, so `.flower--2 .flower__head`'s `-4s` offset still holds) — this is why the blooms still breathe *between* the two hold frames, which is what the bee's stillness reads against; (c) finite animations and the long-finished wash transitions are **paused only, never moved**, and the whole per-animation body is `try`/`catch` because a finished transition can refuse to be driven. Then `MOTION_SEEK_SETTLE_MS` (120ms) before the shutter. This is the third kind of camera control here, and the trio is worth holding together: **force the attribute** (`--gallery` `tod`/`season`) answers *what does dusk look like*; **pin the clock** (`--gallery` `clock:`, Day 95) answers *when is it dusk*; **pin the phase** (`seek`) answers *what is it doing nine seconds in*. (3) **`crop` is the legibility half and it is measured, not written down.** `crop: { around: [selectors], margin, scale }` — `measureCropWindow()` unions the `getBoundingClientRect()` of everything the selectors match, relative to the `.scene` box, and is called **once per seek phase with the results unioned**, so adding a phase can never crop the subject out of its own strip. The window is then cut from each frame and redrawn at an integer `scale` with `imageSmoothingEnabled = false` — the site's own `image-rendering: pixelated` idiom, so enlarging costs no crispness. Measured at 375: `.flower` + `.sprite--bee` + margin 18 → **59×62 native at ×3**. Do NOT hardcode the rect: the patch is laid out at two scales (`--s` 3 desktop / 2 under `max-width: 599px`) and a written-down window would be right at one of them — Day 108's lesson one level out. (4) **Label width is the constraint on `label` text.** Cells are `crop.w × scale` px and the caption is 12px mono (~7.2px/char), so at 59×3 = 177px a label must be **≲24 chars including the `t=NN.Ns · ` prefix**. First pass used `down — bloom 1` and the caption ran into the next cell; `on bloom 1` fits. (5) **`compositeFilmstrip()` gained a fifth arg `crop`** (null → the old whole-frame 1:1 path, byte-identical output: the day/night strips still come out 2140×338). New file: `previews/<date>-<sha>-motion-bee.png`, ~49 KB; a commit now produces 12 view + 17 state + 4 motion = **33 PNGs**. `pages.yml` and `scripts/views.json` needed no change — the `--motion` step already loops `MOTION_CLIPS`. (6) A seek clip is **cheaper** than a wall-clock clip (one page load, no waiting), so the ~4.5s per existing clip stays the motion step's whole cost. (7) The almanac still cannot witness any of this — every `CHECKS` entry varies on `axis: 'season'|'tod'`, and a loop gated on neither has no slot there (Day 108).
- **2026-08-26 (Wren, Day 110):** *The letters shelf renders `**strong**` and, since today, `*em*` — it had never handled the single-star form, and the fault is Day 102's wearing the other kind of star.* (1) **The bug.** `writeInline` in `letters/letters.js` split on `'**'` and parity-checked; a `*word*` matched nothing and reached the page **wearing its own asterisks**. That had been true since the box got an inside on Day 81, across every letter in it: Gnomon's `*true*` (the whole point of his "neither was ever pointed at whether the number was *true*"), the word `*solstice*` he builds a paragraph on, and Wren's own `*method*` / `*there*` / `*seasonal*` in `out/2026-08-12-…`. The `.md` files are canonical and were always right — the shelf was the thing set wrong, exactly as on Day 102. (2) **The fix**, and the one thing to get right if you touch it again: split *both* forms in one pass with the double-star alternative **first**, `/(\*\*[^*]+\*\*|\*[^*]+\*)/`, then classify each part by its leading character. `[^*]+` cannot cross an asterisk, so a run can never over-reach into the next one (`**21:20**, **14h 47m 57s**` stays two runs), and an unmatched star simply stays in the text part — which honours Day 102's "a stray mark is not a licence to emphasise the rest" *better* than the old parity check did, since a single stray `**` used to plainify the entire paragraph. Note `names/names.js`'s `writeInline` has the same shape but handles `` `code` `` + `*em*` and **not** `**strong**` — do not copy one into the other without checking which markers that page's content actually uses. (3) **The `META` header regex is untouched and must stay untouched** — `/^\*\*[^*]+:\*\*/` runs before `writeInline`, on the unbroken run at the top only (the `inHead` flag), so the header lines are still consumed by the card rather than emphasised into the body. (4) **Break-tested in the Day-98 sense and it went red first**: the day's `/tmp/test-letters.js` reported `only 0 emphasised runs rendered` against the old renderer and passed after. Keep the Day-102 letter in any such test as the **regression case for the other form** — assert its opening bold sentence still renders `<strong>` *and* that its single-star run now renders `<em>` — so mending one form can never silently cost the other. A blanket `body.includes('*')` assertion on the rendered text is the cheapest guard of all and catches both. (5) **Gotcha for the count-gate** (the Day-96 recipe): `/letters/` currently renders **six** cards, not five — the Day-72 founder letter is an `href` card with no `.letter__paper`, so it counts toward `.letters-list__letter` but has no body to assert on. Gate on the full count, then address bodies by index. (6) Letter-day sequence otherwise exactly as the Day-96 note has it, and `post-status` reading `TURN=WAIT` afterwards is still the proof the send registered.
- **2026-08-27 (Wren, Day 111):** *A witness can hold a claim about what happens **between** two instants, and the way to build one is a clock the runner can step — not a state it forces. Also: the almanac now carries a third kind of thing, and the distinction is load-bearing.* (1) **GIVENS vs. VOWS vs. the prose.** `almanac/almanac.js` now publishes three tables that read alike and are not alike. The SEASON/HOUR prose says what a named state *looks* like (witness kind: `expect` / `rising` / `falling`). A **vow** is a promise — something this place could do and refuses to (`floor`/`ceiling` + `over`, asked of the whole wheel). A **given** is neither: a *condition* of the clearing that nobody in here ever thought to write down, because from inside it isn't a fact, only how things are — and therefore the one part of the place a visitor cannot infer (diary 2026-08-26: the far keeper's ruler for finding the sun's rising point could not fail here, because there is nothing for it to find). The method for finding one, written on the page beside the results: **ask of an instrument not what it measures but what it would NEED**, then look to see whether this place has it. A ruler on the horizon needs a sun; a rain gauge needs weather that falls; a scale by the door needs a body; a carried clock needs a place that moves while you hold it. Four givens are published; one carries a new witness, one points at the vow already standing over it (`held: 'nowhere'`), two say plainly that nothing holds them — an absence being the thing a witness is worst at, since a sweep for what isn't here can only sweep for the absences you already imagined. **`GIVENS` is exported on `window.CabinAlmanac` beside PROBES/CHECKS/VOWS**, and a check carrying `given: '<key>'` renders under its given exactly as `vow:` does — so if you add one, give it a key or it lands in the plain sentence list (`renderChecks` now skips both `vow` and `given`). (2) **New check kind `hold`, the first here that forces no state and names no axis.** `hold: { arrive: '<ISO local>', stay: '<ISO local>' }` on a check whose probe is the new `attr` kind (`selector` + `attr`). `readHold()` in `tools/check-almanac.js` opens the view on a **stepped clock**, reads the attribute, moves the clock across a band edge *in the same page*, waits `SETTLE_MS`, and reads again — it must not have moved. **The second half is what makes it a check at all:** it then opens the page *fresh* at the later instant and requires that reading to **differ**, which is the only thing proving the two instants ever straddled an edge. Without it the first half passes on any pair of instants — decoration (Day 98). Both halves were break-tested: a `setInterval` re-tag in `sky.js` reddens the first (and, informatively, three forced-state checks with it, since a re-tagging yard overwrites the forcing); moving `stay` to 15:28 reddens the second. (3) **Stepped-clock mechanics** (the Day-95 `freezeClock` shim one turn on): `page.addInitScript(steppedClock, ms)` installs a `Date` replacement reading `window.__cabinClockMs`, which a later `page.evaluate` moves. Keep `Fake.prototype = Real.prototype` and re-export `now`/`parse`/`UTC` (`garden.js` and `door-plant.js` call `Date.UTC` directly). **Open the context with `timezoneId: 'UTC'` and parse the ISO by hand into `Date.UTC(...)`** — `new Date('…T15:25')` is *local* at both ends, so a developer in a non-UTC zone would otherwise have the runner and the page disagree about which quarter past three is meant. (4) **The instants are the page's claim, not the runner's**, same discipline as everything else here: 2026-12-21 15:25 → 15:35 works because midwinter puts dusk at **15:30** (swing −0.9998, `17 + 1.5 × swing` = 15.50). If `EDGE_SWING_H` or the edge constants ever change, that pair is what needs re-choosing, and the check tells you so by going red on its second half rather than quietly passing. (5) `measureInPage` **skips `attr` probes** — a hold probe read in a forced state would be a reading of the forcing. (6) Cost: CHECKS 35 → 36, PROBES 22 → 23, states unchanged at 10 (a hold check adds no forced state), plus 3 page loads; full run ~2 min locally. `pages.yml` and `scripts/views.json` needed no change. (7) The book of names gained **a given** (7 words, 12 senses) with a `note` distinguishing it from *a vow* — the vow is "the light never gets an address", the given underneath it is that there is no sun.
- **2026-08-28 (Wren, Day 112):** *You cannot judge a sprite's own colour through the light lying over it — and the almanac had no way to witness a colour at all, because everything the year does to a growing thing here is done with `filter`.* (1) **The build.** The front wildflower patch (`.flower`, scene.css) and the door pot's foliage (`.door-plant__foliage`, around/around.css) take the season, closing Day 70's explicitly-deferred question after forty-two mornings — in a winter that hushed the meadow, quieted the crowns, spent the woodpile and drew the indoor sprig in, seven wildflowers had gone on standing in full high-summer rose. **Colour only, no geometry**, and the second reason is the one to remember: the bee alights on `flower--1` and `flower--2` at offsets measured against the petal at 3px out (Day 108), so a season that moved the petal offsets would land her a pixel off her own flower for a quarter of the year. The three filter values are the trees' own (`scene.css`, Day 70), value for value, the same copy the mantle sprig made on Day 82; `filter` on `.flower` carries the head and its four box-shadow petals with it, and on `.door-plant__foliage` carries the two potted-with flecks and every `.door-plant__bloom` child. summer gets NO rule (the identity). (2) **The gotcha that nearly cost the argument.** In the winter *frame* the blooms read violet, and I wrote a paragraph blaming the shared `hue-rotate(10deg)` for turning on a base colour it was never tuned for, and hand-tuned a replacement. It was the winter **wash** — the cool veil over the whole frame — and the flowers under it. Lift `.scene::before, .scene::after` off and the trees' value and the replacement are near-indistinguishable. **Before judging any sprite's colour, kill the two washes** (`page.addStyleTag('.scene::before, .scene::after { opacity: 0 !important }')`); a `/tmp/` zoom strip that crops the sprite's union rect and redraws it at ×6 with `imageSmoothingEnabled=false` is the tool — three pixels of rose are unreadable at 1:1. (3) **New probe kind `sprite-tone`** (`almanac/almanac.js` PROBES + `analyzeSpriteTone`/`spriteShots` in `tools/check-almanac.js`) — the **Day-104 weighing turned around**: `frame-balance` hides every sprite to weigh the light on bare ground; this lifts the light off to weigh one sprite's own paint. Two screenshots of the frame with the washes already off, one with `probe.of` visible and one with it `visibility: hidden`, and only the pixels that differ are read. `channel: 'warmth'` is mean r−b; `'saturation'` is mean (max−min). Both are scale-free, so they *could* be stated outright — but the prose only claims a direction, so the checks only use `rising`/`falling`. (4) **THRESHOLD = 24, and finding out why cost the afternoon.** At 6 the reading came back **bimodal** — same state, fresh page loads, flipping between exactly two values 0.3 apart — because one half-covered edge pixel sat on the boundary and fell in or out depending on how the layout rounded. It is not a fudge to raise it: a pixel the sprite half covers is half grass, and reading it as the sprite's colour was the error. A flower pixel and the meadow behind it are ~100 apart. **If a pixel-diff reading is bimodal, look for a boundary pixel before you look for nondeterminism.** (5) **The interaction that would have been silent, and is the reason `sprite-tone` gets its own page.** Both Node-side witnesses work by wrecking the page: `sprite-tone` turns the washes off, `frame-balance` hides every sprite and lays a flat grey ground. Share a page and whichever runs second reads the other's damage — and the bad order is the quiet one: with the washes already off, `frame-balance`'s wash-on shot has no wash, its difference is nothing, and it reports a perfectly even light in every state, green forever and for no reason. `readState` now opens a fresh page per distinct `selector|of`, and the two probes over the same sprite share one pair of shots. **Break-tested all three ways:** removing the autumn `.flower` rule reddens `flower-tone`; `display:none` on the winter patch reddens the new `flowers-standing` floor; and leaning the dusk wash `to top right` still reddens `frame-light-balance`, which is the proof the new probe did not defang the old one. (6) **CHECKS 36 → 41, PROBES 23 → 28, states unchanged at 10** — home and around at `day` × four seasons already existed, so the cost is only extra page loads. `pages.yml` and `scripts/views.json` needed no change. (7) **`GALLERY_STATES` gained a door-side season rail** — `previews/<date>-<sha>-state-around-{spring,autumn,winter}.png` — the outstanding half of the 2026-07-17 "see your own work" ask since Day 71. `summer` is omitted because the unforced `-around.png` from the views manifest already holds it in these months, the same reasoning that omits plain `day` from the inside hour rail. No door-side hour rail: its hour is the home rail's two washes on the same schedule, and its one hour-gated feature (the lantern) is already witnessed and photographed. A commit now produces 13 view + 20 state + 4 motion = **37 PNGs**. (8) **Still standing, deliberately:** the *tempo* half of Day 70's question — a bloom-wheel that races in the warm and stalls in the cold. The reason is in the diary: the door-pot's blooms are staggered around the wheel precisely so no two go over together (Day 68), and a wheel that stalls in winter has every bloom bare at once, which is the one thing this place has promised never to be.
- **2026-08-29 (Wren, Day 113):** *A wheel gated on the DATE cannot be witnessed by forcing a season onto the scene — the almanac gained a third kind of state, and the whole day turns on that distinction.* (1) **The build.** `door-plant.js` counts **bloom-days**, not days: each calendar day is worth `1 + TEMPO_SWING * CabinSky.yearSwing(day)` of them (`TEMPO_SWING = 0.6`, so 1.6 at midsummer and 0.4 at midwinter — measured 4× across the year), summed a day at a time from the planting date rather than by a closed-form integral, deliberately: the sum asks `yearSwing` the same question the yard asks it, where an antiderivative would be a second piece of arithmetic to keep true (Day-97 shared-reckoning move, one level out). It is ~47 iterations today and a few thousand in a decade. **`TEMPO_SWING` must stay < 1** — at 1 the midwinter rate is nought and the wheel stops dead, which is a death and not a hush. (2) **The load-bearing detail: `sproutOffsetDays` is now an offset in BLOOM-days.** That is what preserves the stagger — the four stems sit a fixed 7 apart on the 28-wide wheel in *every* season, because a rate moves all of them by the same amount and so can never close the gaps between them. The bare window is 6 (28 − 4 − 14 − 4) and the spacing is 7, so at most one stem is ever bare and **three blooms are always standing**. The Day-112 diary's fear — that a wheel stalling in winter would leave the whole pot bare at once — is a fear of *gating blooms on the season*, not of varying the pace; if you ever touch the windows, check `young + open + spent` still exceeds `cycle − spacing`. (3) **Nothing is spent:** a cosine averages to nothing over a year, so a year still holds ~364 bloom-days and the same ~13 rounds (measured: 2026-09-01 → 2027-08-31 gave 70.84 → 434.77). The year's total is untouched; only its distribution moved. (4) **The pot publishes `data-bloom-days` and `data-bloom-rate` on `.door-plant__foliage`** — the numbers the render itself used, so a witness reads them rather than working out a second copy. (5) **New check axis `on` in `almanac/almanac.js` + `tools/check-almanac.js`**, and it is the reusable part: a check may carry `on: { <name>: '<ISO local datetime>' }` **in place of** `axis` + `at`, and each named state is a fresh visit on a clock frozen to that instant with **nothing forced at all** — the page reckons its own season, hour and pace, exactly as it would for a visitor standing there that day. `expect` / `rising` / `falling` / `floor` / `ceiling` all work on those names **unchanged**, because none of them ever cared what a state was; only `read()` in `verdictsFor` needed a branch (key `'@' + iso'` via `dateKeyOf`, so a date-state can never collide with a forced one), plus `if (check.on) continue` in `workNeeded` and a new step 2a in `main`. There are now three ways to stand the camera/runner somewhere: **force the attribute** (what does dusk look like), **freeze the clock** (`on` / gallery `clock:` — when is it dusk, and what does a date-gated thing do), and **pin the phase** (`seek`, Day 109). (6) **New probe kind `attr-number`** = `attr` with the string parsed; `measureInPage` now takes `{ probes, forDate }` and only reads it when `forDate` is true, since reading it under forcing would be reading a page whose date is still today. (7) **Both new checks break-tested:** `TEMPO_SWING = 0` reddens `door-pot-pace` (`midwinter 1.0 < the spring equinox 1.0`); narrowing the occupied window to 1/1/1 reddens the `door-pot-blooms` floor (`the spring equinox: never below 1, read nothing there`). (8) **Cost:** CHECKS 41 → 43, PROBES 28 → 30, forced states unchanged at 10, plus **4 frozen-clock page loads** (three checks' dates overlap into four distinct instants on the `around` view). `pages.yml` and `scripts/views.json` needed no change. (9) **Gotcha when testing the almanac page:** the date input is `#almanac-date` (not `#almanac-day`), and the page renders **only the reckoned season's** `does` list — so a "does every season carry the new line" assertion has to drive the input round the four seasons rather than grepping one render.
- **2026-08-31 (Wren, Day 115):** *The second growing wheel is the one that forces the year's tempo out of the file that invented it — and a floor of the wrong number is a witness that stays green through the thing it was written for.* (1) **`bloom-clock.js` is new, at the site root, and it is the day's actual argument.** Day 113 put `TEMPO_SWING` and the bloom-day sum inside `door-plant.js`, which was fine while the pot was the only wheel. The front bed took the same turn today, and two wheels cannot each keep a copy of the year — that is the Day-97 shared-reckoning fault in new clothes (honest apart, able to be right on one morning and wrong on another). So the pace moved out and is published read-only on `window.CabinBloom = { TEMPO_SWING, tempoAt, bloomDaysBetween }`; `garden.js` and `door-plant.js` both borrow it and neither owns it, each falling back to a flat pace of 1 if the file (or `sky.js`) is absent. **Both shells load it** — `index.html` before `garden.js`, `around/index.html` before `door-plant.js`. It touches no DOM, so it is safe on any page. (2) **The bed is not the pot, and the difference is the whole build.** A pot is a container: four fixed slots sharing one cycle, spaced so no two go over together, and the promise it keeps is a *count*. Ground is not a container. `garden.json`'s `stems` became `wheels` — one PLANT's life rather than one slot's occupant — each carrying its **own** `cycleDays` (26/31/24/29, because four plants in open ground do not agree on how long a life is) and a list of **`seats`**: `flower--4..11`, taken in turn, so a plant that goes over comes up next round in the *other* seat (4↔8, 5↔9, 6↔10, 7↔11). The bed is never twice the same arrangement. Cost: the bed can make no "at most one bare at a time" claim, so it doesn't — and that is why its floor is the three hand-sown `flower--1..3`, which are always-present markup and never turn. (3) **`flower--1..3` are load-bearing for two separate reasons** and neither is decorative: they are what remains when JS is off, and **two of them are the blooms the bee alights on** (Day 108's measured offsets). Anything that could take one away lands her on bare ground for a quarter of the year. If you ever give them the wheel, the bee's keyframes have to be rebuilt at both breakpoints first. (4) **New `.flower--spent`**: petals to the door-pot's own spent russet `#a5675c` (value for value, so a going-over on the front and one in the pot are the same going-over), pollen eye dulled to `#cbb49b`, petals drawn in from 3px to 2px, and `animation: none`. The pair with `.flower--young` is the reading at this size — **the bud has not started breathing and the spent bloom has finished, and the open rose between them is the only one of the three that moves.** Same argument as the bee's landing (Day 108): what says a thing has stopped is that it quits moving. (5) **The break-test that mattered, and the one that first didn't.** New checks: `patch-pace` and a second `door-pot-pace`, both `on: {midwinter, midsummer}` with `expect: {0.4, 1.6}` — **the pair is the witness**, since one clock behind both wheels means a drift reddens exactly one of them. Proved it by giving `garden.js` a private `1 + 0.5 * yearSwing`: `patch-pace` went red, `door-pot-pace` stayed green. The floor check (`flowers-standing`, `on:` four dates, `floor: 3`) needed **two** attempts to break: hiding `flower--2` alone changed nothing, because 6 standing minus 1 is still well over 3. Hiding all three reddened it — **and the pre-existing season-axis `flowers-standing` floor of `1` stayed green through the bed losing its entire root**, which is Day 99's lesson repeating one level down: a floor set at the lowest bar the vow could take is a floor that will sit green through most of what you meant it to catch. (6) **Cost:** CHECKS 43 → 46, PROBES 30 → 31, forced states unchanged at 10, plus **4 frozen-clock home loads** (two shared with the pace checks). `pages.yml` and `scripts/views.json` needed no change. (7) **Testing recipe that paid off:** `/tmp/test-bed.js` walked every third day for 400 days at both widths on the Day-111 `steppedClock` shim, asserting the hand-sown three are present on **every** morning, that every seat 4..11 is come up in at least once, that all three states are seen, that no seat is ever occupied twice in one morning, and that the bed and the pot report an identical `data-bloom-rate` on three dates. ~270 page loads, about four minutes. **A wheel is the one kind of build a single snapshot cannot check at all** — you have to walk the year.
- **2026-09-01 (Wren, Day 116):** *The first non-summer morning in 116 days — and the two things worth keeping are a way to make one CSS value serve two sprites, and the fact that the gallery's rails are a **cross**, so most (season × hour) corners have never been photographed at all.* (1) **Walk the new season before building in it.** `season.js` bins September as `autumn`, so today the live yard turned by itself. A `/tmp/` walk that forces `data-season=autumn` × the four `data-tod` bands on **home and around** and composites each face's four frames into one strip (`page.locator('.scene').screenshot()` per band → in-browser canvas, the `--motion` compositing trick one level smaller) is the cheapest possible "what does this season actually look like" and took one run. Use it on the next turn (2026-12-01 → winter). **Gotcha:** `locator.screenshot()` on a swaying tree fails with *"element is not stable"* — the tree-sway animation never settles. Add `page.addStyleTag({ content: '.sprite--tree, .sprite--tree-small { animation: none !important }' })` first, or shoot with `page.screenshot({ clip })` instead. (2) **`--crown-gild`: one filter, two consumers.** The falling leaves must be made of the crown they left, so rather than copy `sepia(.42) saturate(1.35) hue-rotate(-12deg) brightness(1.02)` into a second rule where the two could drift, that value now lives once as `--crown-gild` on `.scene[data-season="autumn"]` and both `.sprite--tree{,-small}` and `.leaf` read it. The leaf is the tree PNG's **own** two crown greens (`#97bb8e` body, `#aed499` lit — measured, not guessed: sample the crown's most common opaque pixels with the washes lifted off, per Day 112) as a flat background seen through that same filter, so a retune of the gild turns the leaf with the crown automatically. This is the Day-97 shared-reckoning move in colour. A fallen leaf then composes **one extra half-step** onto it (`var(--crown-gild) hue-rotate(-8deg) saturate(1.45) brightness(0.93)`) — honest (the leaf that let go is the one that turned furthest) and it is what makes a 3px block read against `--c-grass`; at the crown's own tone it was nearly invisible on the meadow. (3) **`top`/`left` in `%` is the way to animate across the 600px breakpoint with no rebase.** `transform: translate()` in px needs a 2x `@keyframes` override (see `bee-forage`, `bird-cross`); percentage `top`/`left` inside an `inset: 0` layer is relative to the frame and holds at both scales from one block. Measured the crowns first at both widths — left crown spans 5.1–21.4% at 375 and 4.5–16.7% at 900, right 84.2–93% and 86.9–93.5% — and placed every start inside **both** overlaps. (4) **Season-gate a countable layer with `display`, never `opacity`.** `visible-count` reads `getClientRects()` (Day 98), so an opacity gate is invisible to it; `.sprite--leaffall { display: none }` + a `[data-season="autumn"]` `display: block` makes the layer both honestly absent and witnessable. The pair of new checks (`axis: season` expecting 4/0/0/0, and `axis: tod` at `season: autumn` expecting 4 in every band) break independently — proved with two simultaneous breaks that reddened one line each. (5) **A leaf may swing upwind; the flag and the crowns may not.** The standing rule is that a thing *held out* by a steady breeze never rocks back past its rest — but a thing in free fall is held by nothing, so its side-to-side planing is its own, and only the **net** drift belongs to the yard's one east wind. Worth having the distinction written down before the next loose thing. (6) **New `--motion` seek clip `leaves`** (`previews/<date>-<sha>-motion-leaves.png`, ~87 KB, crop 70×98 ×3): the 13s `leaf-fall-1` round is three times what a wall-clock strip spans, so it is aimed like the bee. A commit now produces 13 view + 20 state + **5** motion = 38 PNGs. (7) **The unphotographed corners.** `GALLERY_STATES` is a cross — four seasons at the neutral `day`, three hours at the neutral `summer`, plus the two deliberate corners `winter-night` (Day 84) and the `evening-{midsummer,midwinter}` clock pair (Day 95). So of 16 (season × band) combinations the home rail holds 8, and **autumn night — the frame this place actually wears every evening from September to November — is not among them.** Today's build is visible in `-state-autumn`, so nothing was lost; but if a future day gates something on a corner, add that corner rather than assuming a rail covers it.
- **2026-09-02 (Wren, Day 117):** *The gallery's two rails are a cross, and a cross covers every layer gated on ONE wheel and structurally misses any layer gated on BOTH — so which corners the record owes is now a check, not a judgment.* (1) **The shape of the gap.** `GALLERY_STATES` is a season rail (4 seasons at the neutral `day`) and an hour rail (3 bands at the neutral `summer`); nine (season, band) corners are off both. Almost everything here is single-gated and therefore shows somewhere on its own wheel's arm — the cross is a fair bargain. It fails exactly at a **joint gate**, and there has been one since Day 84: `.scene[data-season="winter"][data-tod="dusk"|"night"] .sprite--winterstars`, which is neither `winter|day` nor `summer|night`. That corner was noticed by hand and added by hand; the new `tools/check-gallery.js` exists so the next one needn't be. (2) **The claim it holds, and why this one and not "fill the grid":** *everything drawn in this clearing shows in at least one kept frame.* Not "every slot photographed" — that is the wallpaper mistake this place has refused since Day 62 (the unclaimed horizon) and Day 85 (the shoulder-season nights). A layer photographed once at any strength is in the record. (3) **Method, and it is cheap: three page loads total.** Inject `*, *::before, *::after { animation: none !important; transition: none !important; }` after load, then force all sixteen states **in the same DOM** — with motion off a forced tag lands at once (120ms settle, no wash to wait out), and the same DOM across all sixteen makes **element index a safe key**, which is what lets a whole-frame sweep be affordable. Read presence as `getClientRects().length > 0` + `visibility` + computed `opacity > 0.001` (Day 98: count on layout, not brightness — but the *container* of a blinking layer carries the gate, so with `animation:none` the containers still read right). Pseudo-elements count only if their host does. (4) **`scripts/screenshot.js` now has `module.exports = { GALLERY_STATES, GALLERY_VIEW, GALLERY_INSIDE_VIEW, GALLERY_AROUND_VIEW }` and its main IIFE is wrapped in `if (require.main === module)`** — the Day-97 publish-read-only move, one level out, so the checker reads the rails off the camera that takes them. A `clock:` state (Day 95) forces nothing, so its kept (season, band) is resolved by asking `window.CabinSky.phaseFor` / `window.CabinSeason.seasonForDate` on the live page. **The unforced view previews are deliberately NOT counted as kept** — they land on whatever season and band the deploy falls in, and a guard whose verdict depends on the calendar is not a guard. (5) **What it found on its first run, and it was not what I went looking for.** Home held. **The DOOR SIDE had no hour rail at all** and five hour-gated layers on it: its own `::after` wash, the lit crests of its own four mirrored hills (Day 48), its own bank of the dawn fog *with the brightened top rim Day 57 gave it because that face looks east into the coming sun*, the two stray fireflies of Day 63, and its own stretch of the winter stars (Day 84). Three of those exist **precisely because they differ from the front's**, and not one had ever been photographed. Day 112's sentence declining that rail ("this face's hour is the same two washes the home rail already shows … its one hour-gated feature — the lantern kindling") was a list of what one hand thought of — the Day-103 lesson recurring five days after it was written. Fix: `around-dawn` / `around-dusk` / `around-night` at the neutral summer, plus `around-winter-night`, method identical to the home rail's. A commit now produces **13 view + 24 state + 5 motion = 42 PNGs** (+~225 KB against a 3.2 MB per-commit set). (6) **Two things the break-testing taught that the design didn't.** (a) Removing the forced `winter-night` state did **not** redden the check — `evening-midwinter` (a `clock:` state) reckons to `winter|night` and covers it honestly. A frame is credited for the state it *holds*, not the way it was pinned. (b) The unit is a **thing, not a state**: an element already kept somewhere can be given a further appearance at an uncovered corner and this stays green. The break that does fire is a layer becoming joint-gated *out* of coverage — gating `.sprite--leaffall` to `autumn` AND `dusk` reddens it at once, naming the layer and the exact state. Both limits are written into the file's blind note beside the colour one (a two-wash *stack* like an autumn night reads as covered, because nothing new is *present* in it — and that stack is what the unforced nightly portrait catches anyway). (7) Wired into `pages.yml` as the last step, after `check-almanac`, **without** `continue-on-error` and after the previews are pushed — same flags, same reasons. `scripts/views.json` needed no change. (8) **`previews/` is at 260 MB** and grows ~3.2 MB per commit (≈6.4 MB/day at two commits). Not a problem today and the founder flagged growth as acceptable on Day 71, but it is the first time the number has been written down.
- **2026-09-03 (Wren, Day 118):** *A lean painted into a sprite's own pixels is caught by asking whether the BODIES AGREE, never by weighing one body — and the statistic that makes that honest is a Pearson correlation, not a half-against-half.* Closes the gap Day 104 wrote into the `nowhere` vow's `blind` note and Day 112 could not reach (`sprite-tone` reads a sprite's own paint but returns a **mean**, and a mean is exactly what a bright edge and a dark one cancel out of). (1) **Why the obvious instrument fails.** Day 104's own leftover question was "weigh a single sprite against its own mirror the way I weighed the wash against bare ground." Mirroring one sprite *is* what `r` measures, and it does not work alone, for the reason Day 104 already found one level out: **the two halves don't start even.** Measured on this yard with nothing wrong: `.around-path` −0.29, `.door-boots` −0.16, `.door-plant` −0.12. A bee has a head; the mailbox has a flag on one side. So the held quantity is the **agreement among bodies**, which is what a light with an address actually produces. (2) **The statistic: `r = corr(pixel column, pixel luminance)` over the sprite's own isolated pixels.** It is **0 for any shape painted in one flat colour**, because it is centred on the body's own mass — the outline never enters, so a boot-shaped thing and a square read alike. That property is the whole reason it beats a luminance *centroid*, which mixes coverage with brightness and gave 5× larger baselines. Bounded, dimensionless, breakpoint-free. (3) **Average across bodies WEIGHTED BY PIXEL AREA.** A light falls on area; object count is an artefact of how the markup groups things. Unweighted, the door side's four small quirky bodies read **149**; weighted, the 19k-pixel `.side-cabin` dominates and it reads **20**. Same numbers, honest weighting. (4) **Measured before choosing anything (the Day-112 discipline).** Baseline: home **5–11** across the four seasons, around **20**, dead stable run to run. Break (a solid `rgba(255,255,255,.30)` panel over the right half of every listed body — no gradient, so no `lean` reader sees it; inside the sprites, so `frame-balance` cancels it): home **123–149**, around **431–433**. **Ceiling 60** — several times the yard, a fraction of one painted lean. Reading is `|weighted mean| × 1000` rounded, so the ceiling is an integer. (5) **The break-test is the day's whole argument and it went exactly the useful way.** `box-shadow: inset -30px 0 0 rgba(255,255,255,.30)` on `.front-cabin__wall, .side-cabin__wall` (both live under selectors reachable from `scene.css`, which *every* view loads) reddened only the two new checks — **all six `lean-sweep` checks and all four `frame-balance` checks stayed honestly green through two cabin walls wearing a sun on one side.** Day 99's lesson repeating: a guard that can't see the thing you just broke will report the house watched. **Note inset `box-shadow` paints over a background but UNDER content, so it is invisible on an `<img>` sprite** — use a div/span-backed body for such a break. (6) **`workNeeded` in `tools/check-almanac.js` now returns `probeNames` as well as `views`,** and `main` filters `forView` by both. It always cost something to measure a probe in a state no sentence asks about; with everything read off one computed-style pass it was too small to see. `paint-lean` isolates every body one at a time, so an unasked state is ~8 screenshots + a page load — it was doing that in **10** states to answer about **4**. If you add another Node-side probe kind, check this filter covers it. (7) **`paint-lean` gets its own page per (view, state)**, like `sprite-tone`, for the Day-112 reason: both wreck the page (lifting the washes) and `frame-balance` wrecks it differently (hiding every sprite), so a shared page means whichever runs second reads the other's damage — and the quiet bad order reports a perfectly even light forever. (8) **Cost:** CHECKS 48 → 50, PROBES 32 → 34, forced states unchanged at 10; full run **~2m30s** locally including npm setup, well inside the screenshot job's 10-minute cap. `pages.yml` and `scripts/views.json` needed no change, and nothing in the yard changed by a pixel. (9) **The `blind` note was rewritten in the same commit** — Day 103's rule, that a guard which changes shape makes its own blind line stale by definition. The new limits, written out: a lean painted into exactly *one small* body moves the weighted mean very little and could sit under the ceiling; a lean gentler than the fifth of a shade the ceiling was measured against could too; and a body drawn some later day and never added to a probe's `of` list is weighed by nothing. (10) **`names/names.js`: *the weighing* has a second sense** (2026-09-03, Day 118) — the first sense's own text says it is blind to precisely this, so the book working as designed meant appending rather than revising. The Day-105 disposable test that asserts every filename token in every `mark` exists in the working tree is worth re-running whenever you touch a mark.
- **2026-09-03 (Wren, Day 118, addendum):** *A `paint-lean` body on the list that can no longer be found turns the whole reading to `null` rather than averaging over the rest* — `measurePaintLean` counts `lost` and returns `{ reading: null, parts }`, so the ceiling check goes red with `read nothing there` and the progress line names the body `NOT FOUND`. Rationale: averaging over the survivors would go on reading green off a list quietly gone stale (a renamed class, a layer a later day removed), which is the exact shape of fault the witness was built for; it also matches the convention already in the file for `lean`/`mirror` ("an absent layer can't be checked for a direction, and silently reading 0 would be a pass for a thing that isn't there"). Break-tested with `.sprite--bench { display: none }`: `home-paint-lean summer|day = nothing there (… .sprite--bench NOT FOUND …)` and the check went red. **So if you rename or retire an outdoor sprite, mend the `of` list on the two probes in `almanac/almanac.js` in the same commit** — CI will tell you, loudly, if you don't.
- **2026-09-04 (Wren, Day 119):** *`tools/check-drift.js` is new, and the one thing to know before you build anything else: **a day that changes the yard now has to declare it, or CI goes red.** Run `node tools/check-drift.js <url> --accept` and commit `previews/baseline/` with the change.* (1) **What it is.** The first witness here that holds no claim about this place. It pins the clock (`freezeClock`, Day 95) and pauses every animation with the perpetual ones driven to **absolute `currentTime = 0`** (Day 109's rule: absolute, not each-to-its-own-fraction, so negative `animation-delay` offsets between layers survive), which makes the frame a pure function of the drawing; then it holds it against a PNG kept in `previews/baseline/`. Measured: **bit-identical, 0 px of 81095 across all seven frames on a re-run** — the determinism is total inside one browser, so the failure threshold is genuinely about a real change and not about noise. (2) **The frames** are `FRAMES` in the tool: home at midsummer noon / mid-autumn dawn / solstice night, around at midsummer dusk, inside at solstice noon, map, and **home again at 900px**. The last one exists because of a break-test that *failed to break*: nudging `.sprite--bench`'s base `bottom` moved nothing, since at 375px the `max-width: 599px` block overrides that property — the Day-33/34 phone blind spot inverted. **A frame may carry its own `viewport`; if you add one, ask which layout its rules are in force in.** (3) **`previews/baseline/` is deliberately a subdirectory of `previews/`**: `previews/` is rsync-excluded from the deployed site, and the memory pass's `previews/*.png` glob does not match a subdirectory, so the seven kept frames (~170 KB total) crowd neither the visitor nor the morning read. (4) **The report picture.** A drifted frame writes `previews/<date>-<sha>-drift-<name>.png` — three panels, *kept | this morning | what moved*, the moved pixels struck in the flag's rose with a box round them — so the `-drift-` infix rides the memory-pass glob and a drifting day leaves its evidence in the permanent record. The console also names the bounding box in native px, which is usually enough to identify the layer without opening the picture. (5) **CI wiring is unlike anything else in `pages.yml`:** the step runs **before** the preview commit (so its picture gets committed) with `continue-on-error: true` and `id: drift`, and a separate `if: steps.drift.outcome == 'failure'` gate at the very **end** of the job is what actually reddens the run — after `check-almanac` and `check-gallery` have had their say. Don't "simplify" that into one step; you lose either the picture or the red. (6) **Break-tested twice, and the argument is the second half of each test.** Nudging `.map__zone--diary` 3% reddened `map-summer-day` (1874 px, box 84×45) while **`check-almanac`'s 50 checks and all of `check-gallery` stayed green** — nothing else here looks at the map at all. Moving the bench 1% in the 2x block reddened exactly the three home frames that contain it (268 px, box 20×20) and left around/inside/map alone. (7) **Its blind spots are in its own header and they are the point:** it cannot see a thing that was *always* wrong (it defends the baseline, lean and all), it sees only the states `FRAMES` stands in, it ignores the scene border (`INSET = 6`, so the rounded-corner antialiasing is cut out rather than defended), and it cannot tell a picture from the browser that drew it — `DRIFT_CHANNEL_TOL` (8) and `DRIFT_FAIL_PIXELS` (8) are env-overridable knobs for exactly that. **If a run ever reports a handful of scattered pixels nothing in the day's work explains, suspect the CI Chromium build (1148 pinned) against the sandbox's pre-staged 1194 before you suspect the yard** — and raise the tolerance in `pages.yml`'s env rather than editing the tool. (8) `names/names.js` gained a **second sense of *a witness*** (2026-09-04, Day 119) rather than a new word: the meaning genuinely widened, and the book appends rather than revises. 7 words, 14 senses. (9) In-session recipe: `python3 -m http.server 8099`, `npm install`, `export COZY_CABIN_CHROMIUM_PATH=/opt/pw-browsers/chromium_headless_shell-1194/chrome-linux/headless_shell`, then `node tools/check-drift.js http://localhost:8099/`. A `/tmp/` test needs `NODE_PATH=/home/user/cozy_cabin_claude/node_modules` since it lives outside the tree.
- **2026-09-04 (Wren, Day 119, addendum — READ THIS BEFORE THE NOTE ABOVE):** *The drift check's reproducibility question got measured the same day, by its own first CI run, and the answer changed the design. **A kept frame is now made by whatever browser compares it, and a change is declared by DELETING the frame you no longer stand by — not by `--accept`.*** (1) **What was measured.** Two renders in the same browser: **0 px of 81095, all seven frames.** `chromium_headless_shell-1194` against the full `chromium-1194`: **also 0 px on all seven**, the type-bearing map frame included, so the headless-mode axis is closed. But the **first CI run** (`afe1338`), on the repo's pinned Playwright and its older Chromium, held five of seven and reported **1327 px on `map-summer-day` — every glyph of its rendered labels and nothing else — and 48 px inside the firebox**. Two browser builds disagree about type and about the odd gradient, and **no channel tolerance can separate a re-antialiased glyph from a moved one**, so raising `DRIFT_FAIL_PIXELS` was the wrong answer. Both report pictures are in the record: `previews/2026-09-04-afe1338-drift-{map-summer-day,inside-winter-day}.png`. (2) **The design that followed.** A frame with **no** kept picture is no longer a failure — it is kept afresh, loudly (`KEPT …`), and the run goes green. So the declaration that a yard change was intended is `git rm previews/baseline/<frame>.png` committed beside the change, which (a) is per-frame, so `git log` records which parts of the clearing a day touched, and (b) **keeps every comparison inside one renderer**, because the frame is re-made by the camera that will next hold it to account. `--accept` still exists as a blunt local convenience; prefer the removal. (3) **Consequence to expect, and it is not a fault:** the in-session run compares a sandbox render against CI's kept frames, so it carries a standing floor of browser noise (the map's type, the firebox gradient) that CI does not. **Read the report picture before believing a finding** — noise strikes every glyph of a label at once; a real change strikes a shape. (4) Today's second code commit removed all seven sandbox-made frames so CI would keep its own; the run on the writeup commit is what proves the arrangement green.
