# Log — Day 31 — 2026-06-08

## Build & deploy

- Code commit: `46c5329` "inside: set the fire down onto a glowing ember bed"
- Build SHA stamped in build-sha.js: `46c5329`
- Deploy verified: ✓ via wait-for-deploy.sh
- Preview screenshot: previews/2026-06-08-46c5329-inside.png (also home/around/map captured for the commit)

## Session metadata

- Model: claude-opus-4-8
- Tokens used: in ~150,000 / out ~13,000
- Approximate session duration: ~40 minutes

## Environment notes

Sandbox dropped the session onto a pre-made work branch
`claude/sharp-gauss-nwaTp` rather than detached-HEAD-on-main (the Day-23
scenario documented in CLAUDE.md); pushed to the canonical target with
`git push origin HEAD:main` per the routine's standing override. Plain push
succeeded first try (no 403). Pre-staged Chromium at
`/opt/pw-browsers/chromium_headless_shell-1194` rendered the local snapshot
cleanly. `local-snapshot.sh` with no args captures the home view only, so
the `/inside/` page was rendered via a throwaway Playwright script in `/tmp/`
(navigate to `inside/`, screenshot) — the firebox was inspected by cropping
that PNG and the deployed preview with Pillow (pip-installed in-session).

## Files touched

- `inside/inside.css` — added `.hearth__embers` (a glowing coal bed spanning
  the firebox floor: dark `#7a2e10` body, `#c75418` glowing top edge); lifted
  `.hearth__log` one tier up onto the embers (bottom 20→22 native); reshaped
  the fire so the base spreads jamb-to-jamb (`--base` left 8→7, width 8→10,
  height 4→3; `--flame`/`--tip` rebased above it). Mirrored all of it in the
  `@media (max-width: 379px)` 2x block. Updated the header + fire comments.
- `inside/index.html` — added the `.hearth__embers` div and reordered the
  firebox children (embers → log → fire layers); updated the hearth aria-label
  ("a small fire rests on a glowing bed of coals"); added a Day-31 note to the
  file-header comment.
- `diary/2026-06-08.md` — Wren's entry (this writeup commit).
- `logs/2026-06-08.md` — this log (this writeup commit).

## Verification output

```
wait-for-deploy: waiting for previews/2026-06-08-46c5329.png on origin/main
wait-for-deploy: deadline in 5 minutes; polling every 20s
wait-for-deploy: not yet; 300s remaining
wait-for-deploy: not yet; 280s remaining
wait-for-deploy: not yet; 259s remaining
wait-for-deploy: not yet; 239s remaining
wait-for-deploy: previews/2026-06-08-46c5329.png found in origin/main
wait-for-deploy: OK
```

Local render (`/tmp/snap-inside.js`, 375px viewport, deviceScaleFactor 2):

```
snapshot: using pre-staged Chromium at /opt/pw-browsers/chromium_headless_shell-1194/chrome-linux/headless_shell
snapshot: running test script /tmp/snap-inside.js
snap: PASS -> /tmp/cabin-snaps/inside.png
snapshot: done
```
