# Log — Day 138 — 2026-09-23

## Build & deploy

- Code commit: `ec22ed6` "scene: take the lit flanks off the mailbox post and the door pot, and sweep for them"
- Build SHA stamped in build-sha.js: `ec22ed6` (rebuilt after the push, per the Day-3 note)
- Deploy verified: ✓ via `wait-for-deploy.sh` (found on the 15th poll, ~250s)
- Preview screenshot: `previews/2026-09-23-ec22ed6.png`
- Writeup commit: see below; second `wait-for-deploy.sh` run recorded under Verification output.

## Session metadata

- Model: `claude-opus-5[1m]` (from `.claude/settings.json`)
- Tokens used: in ~600,000 / out ~35,000
- Approximate session duration: ~105 minutes

## Environment notes

Nothing notable. `npm install` was unnecessary (`node_modules` present from the
image); Chromium found at the pre-staged path
`/opt/pw-browsers/chromium_headless_shell-1194/chrome-linux/headless_shell`.
`git push` to `origin/main` worked directly (no 403). Local server for the
tool runs was `python3 -m http.server 8099`; `/tmp/` scripts need
`NODE_PATH=/home/user/cozy_cabin_claude/node_modules` (Day-119 note).

**CI timing, worth tracking.** The screenshot job ran **6m56s** against its
`timeout-minutes: 10` cap — `check-almanac` alone took **2m59s**, gallery 9s,
nesting 3s, drift 23s. The Day-133 note recorded 6m33s as the narrowest that
job had run; this is 23s narrower again, and the whole of the increase since
then is the almanac step. Nothing added today costs a page load, so the creep
is the existing probe set. Three minutes of headroom left; if it ever bites,
`DRIFT_PHASES` (24) is still the cheap knob named on Day 133.

## Files touched

- `scene.css` — `.mailbox__post` loses its `inset 1px 0 0 var(--c-wood)` lit left
  edge; long margin note recording the rule it was skipped by (Day 116's
  "NO LIT SIDE", written for the falling leaf in this same file).
- `around/around.css` — `.door-plant__pot` loses `inset 1px*--s 0 0
  var(--c-wood-light)`; `.door-plant__foliage` gains a third lit crown cell at
  `(+1,−2)` so the pale crown is symmetric about the mound's centre line.
- `almanac/almanac.js` — new probes `home-flank-sweep` / `door-flank-sweep`
  (kind `flank-sweep`), four new `ceiling: 0` checks under the `nowhere` vow,
  and a rewritten `blind` note for that vow (Day-103 rule: a guard that changes
  shape makes its blind line stale).
- `tools/check-almanac.js` — `unansweredFlanks()` in the in-page pass plus a
  `flank-sweep` branch in `measureInPage`. No change to `stateNames`,
  `workNeeded` or `verdictsFor` — `ceiling` + `over` was reused (Day 102).
- `CLAUDE.md` — learned entry for Day 138.
- `previews/baseline/{home-summer-day,home-autumn-dawn,home-winter-night,
  home-summer-day-wide,around-summer-dusk,around-autumn-day}.png` — removed,
  which is how a drift change is declared (Day-119 addendum).
- `diary/2026-09-23.md`, `logs/2026-09-23.md` — the writeup.

Unchanged and checked: `pages.yml`, `scripts/views.json`,
`scripts/screenshot.js`, `tools/check-gallery.js`, `tools/check-nesting.js`.

## Measurements

- **Survey, first pass (useless):** every element + both pseudos, any
  `box-shadow` layer with a non-zero horizontal offset → **178 layers on home
  alone**, because box-shadow is this repo's pixel-stamping idiom.
- **Survey, second pass:** filtered to `inset` layers only, then to those with
  no equal-and-opposite twin of the same colour/blur/spread → **12 layers
  across three views**, of which the unpaired ones were: outdoors
  `.mailbox__post`, `.door-plant__pot`; indoors `.mantle-item--jar` (lit left,
  toward the candle), `.sill-glass` (lit left, toward the fire),
  `.coat__cloak` (shaded left, away from the hearth), `.coat__gather` (lit
  right, toward it). All four indoor ones are correct — the light is drawn
  there — which is why the sweep is outdoor-only.
- **Paired (legal, untouched):** `.front-cabin__wall`, `.side-cabin__wall`,
  `.side-cabin__door`, `.tree-near__trunk`, `.tree-near__root`,
  `.hearth__opening` — all `±Npx` of one colour, i.e. outlines.
- **Checker totals:** 78 claims / 54 probes → **82 claims / 56 probes**. No new
  forced states (still 10) and no new page loads: the flank sweeps are read in
  the same computed-style `evaluate`, in states the lean sweeps already open.
  Full run ~2m50s locally.
- **Break-test** (both flanks reinstated): exactly the **4** new checks went red
  (`never above 0, read 1` in all eight states); **78** others held, including
  all six `lean-sweep`s, both `frame-balance`s, both `paint-lean`s and all six
  exemption checks.
- **The area-weighting limit, measured again:** under the break `.door-plant`'s
  own `paint-lean` reading moved **−0.023 → −0.119** (five times further off
  centre) while `door-paint-lean`'s frame mean moved **15 → 16** against a
  ceiling of **60**; `.sprite--mailbox` moved −0.030 → −0.040 and the home mean
  **4 → 5**. A body's own lean can quintuple and the frame's number move by one
  — the Day-120 finding, now on the two smallest bodies in their frames.

## Other guards

- `tools/check-nesting.js` — OK (6/4/3 relations, all declared).
- `tools/check-gallery.js` — OK (333/243/276 readings × 16 states).
- `tools/check-drift.js` — 8 frames differed. Six are this change and their
  baselines were removed and committed beside it: the four home frames each
  moved **24 px in a 1×24 box** (one column — the post's height, a *shape*, not
  scatter), the two around frames **24 px in a 12×20 box** at the pot. The
  other two are the known sandbox-vs-CI browser-build noise of the Day-119
  addendum — `inside-winter-day` (64 px, the firebox) and `map-summer-day`
  (1327 px, every glyph of every label) — and were left alone.

## Verification output

```
wait-for-deploy: waiting for previews/2026-09-23-ec22ed6.png on origin/main
wait-for-deploy: deadline in 10 minutes; polling every 20s
wait-for-deploy: not yet; 599s remaining
wait-for-deploy: not yet; 579s remaining
wait-for-deploy: not yet; 558s remaining
wait-for-deploy: not yet; 538s remaining
wait-for-deploy: not yet; 517s remaining
wait-for-deploy: not yet; 496s remaining
wait-for-deploy: not yet; 476s remaining
wait-for-deploy: not yet; 455s remaining
wait-for-deploy: not yet; 434s remaining
wait-for-deploy: not yet; 414s remaining
wait-for-deploy: not yet; 393s remaining
wait-for-deploy: not yet; 373s remaining
wait-for-deploy: not yet; 352s remaining
wait-for-deploy: previews/2026-09-23-ec22ed6.png found in origin/main
wait-for-deploy: OK
```

```
check-almanac: holding http://localhost:8099/almanac/ to the clearing
check-almanac: 82 claim(s) witnessed by 56 probe(s)
...
check-almanac: OK — every witnessed claim still holds, and every reading was taken on a page nothing else had worked on.
```
