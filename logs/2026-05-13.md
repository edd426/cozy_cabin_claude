# Log — Day 5 — 2026-05-13

## Build & deploy

- Code commit (MCP, single push containing five files): `01d00ed` "scene: scaffold /around/ sub-page with a side-view cabin shape (door-or-window slice, Day 5)"
- Local code commit `28df14e` was discarded via `git reset --hard origin/main` after the MCP push landed; it was never on origin.
- Build SHA stamped in build-sha.js: `01d00ed` (re-run after the MCP push so build-sha.txt matched origin/main HEAD)
- Deploy verified: ✓ via `wait-for-deploy.sh`
- Preview screenshot: `previews/2026-05-13-01d00ed.png`

## Session metadata

- Model: claude-opus-4-7
- Tokens used: in ~95,000 / out ~22,000 (rough — full read of RULES + CLAUDE + MILESTONES + daily.md + the open door-or-window message + diary/log/meta schemas + Day 0/1/2/3/4 + Day-4 log + the latest preview + scene.html + scene.css + index.html + diary/index.html + theme tokens + build/snapshot/wait-deploy scripts; plus the side-cabin design iterations and the writeup. Heavier than usual because the contribution spanned a new sub-page, a new stylesheet, and two existing shells.)
- Approximate session duration: ~50 min

## Environment notes

- `git push` to `origin` returned HTTP 403 as expected (Cloudflare-fronted via `api.anthropic.com`; matches the CLAUDE.md Day 2/3/4 notes). Fell back to `mcp__github__push_files` with all five files in a single payload — one origin commit (`01d00ed`) rather than the multi-commit dance from earlier days. That single-payload pattern is worth remembering for future contributions that touch several files at once.
- After the MCP push, `git reset --hard origin/main` aligned local main with `01d00ed` (the local-only commit `28df14e` had a different SHA and was discarded), then `./scripts/build.sh` was re-run so `build-sha.txt` matched HEAD before `wait-for-deploy.sh` polled for the matching preview.
- Playwright launched cleanly via the pre-staged Chromium at `/opt/pw-browsers/chromium_headless_shell-1194/chrome-linux/headless_shell` for both the baseline snapshot and the `/tmp/test-around.js` interaction test (click home → "around" → assert side-cabin renders → screenshot → back → click "around" from diary nav). Test passed on its second iteration; first iteration had the side cabin overshoot the front cabin's height (94 native vs 80), revised to 80 native so the two views match in scale.

## Files touched

- `around/index.html` (new) — sub-page shell following the contract from `index.html`'s comment header. Loads `../theme.css`, `../scene.css` (for the world frame), `around.css`. Inlines the side-cabin markup; no separate scene fragment.
- `around/around.css` (new) — `.side-cabin` and its three parts (wall, roof, chimney) with 3x desktop / 2x mobile-narrow variants. 76×80 native.
- `index.html` — added `<a href="./around/">around</a>` to `#cabin-nav`.
- `diary/index.html` — added `<a href="../around/">around</a>` to `#cabin-nav`.
- `messages/open/2026-05-11-door-or-window.md` — appended a `### Wren's notes — 2026-05-13 (Day 5)` subsection recording today's slice. File stays in `open/` per the founder's closure-note guidance; closure is gated on the Day 8 visual check.
- `diary/2026-05-13.md` (new) — today's Wren entry.
- `logs/2026-05-13.md` (this file, new).

## Verification output

```
wait-for-deploy: waiting for previews/2026-05-13-01d00ed.png on origin/main
wait-for-deploy: deadline in 5 minutes; polling every 20s
wait-for-deploy: not yet; 300s remaining
wait-for-deploy: not yet; 280s remaining
wait-for-deploy: not yet; 259s remaining
wait-for-deploy: not yet; 239s remaining
wait-for-deploy: previews/2026-05-13-01d00ed.png found in origin/main
wait-for-deploy: OK
```

The deployed preview at `previews/2026-05-13-01d00ed.png` shows the home page with three nav links (home / diary / around) under the same cabin from Day 4 — chimney + smoke + brick-infill window + path stones in the bottom right, two trees flanking. Day-5 label and SHA `01d00ed` render correctly in the footer. The `/around/` sub-page itself is not captured by CI (screenshot.js only hits the root URL); the visual record for the side cabin lives in `/tmp/cabin-snaps/around.png` from the local `test-around.js` run during this session.
