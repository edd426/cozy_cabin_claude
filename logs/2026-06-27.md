# Log — Day 50 — 2026-06-27

## Build & deploy

- Code commit: 74fdb35 "around: a pair of boots by the door — a sign of a resident without a figure"
- Build SHA stamped in build-sha.js: ca20832 (gitignored runtime file; rebuilt locally pre-push — the deployed SHA is the pushed commit 74fdb35)
- Deploy verified: ✓ via wait-for-deploy.sh
- Preview screenshot: previews/2026-06-27-74fdb35-around.png (+ -around-phone, and the home/inside/map captures, unchanged)
- Note: the CI screenshot ran at ~00:15 UTC — inside the `night`/dim band — so the deployed around-view preview shows the lantern lit gold and the new boots taking the same ambient hour-wash as the grass, confirming the passive object participates in the time-of-day overlay on the live site, not just in-session.

## Session metadata

- Model: claude-opus-4-8[1m]
- Tokens used: in ~145,000 / out ~17,000
- Approximate session duration: ~35 min

## Environment notes

Sandbox dropped the session on branch `claude/sharp-gauss-9mj7h9`; reattached to
`main` per Step 0 (`git checkout -B main HEAD`, then `git reset --hard origin/main`
since the work branch held 50 diverged-SHA commits mirroring main). Plain
`git push origin main` worked first try (no 403). Pre-staged Chromium at
/opt/pw-browsers detected by local-snapshot.sh.

The default `local-snapshot.sh` (no script arg) only renders the home page, so the
around view was verified via a Playwright test at /tmp/boots-check.js: it navigates
to /around/, clears any real-clock `data-tod`, and screenshots the plain-day state,
then forces `data-tod` to `night` and `dusk` (waiting 1.8s past the 1.6s wash
transition each time) to confirm the boots take the ambient `.scene::after` overlay
with the grass — dusk-blue at night, gold at dusk — while staying a passive object
(no glow of their own; the lantern remains the only light source). A second
disposable test /tmp/boots-zoom.js cropped the `.door-boots` bounding box at a high
deviceScaleFactor to check the silhouette reads as a pair of boots and not a crate.
The first silhouette pass set the back boot 1px higher, which read a touch blocky;
flattened both boots onto the same ground line, taller shafts, paler folded cuff at
top + dark sole pressed to the ground, back boot a shade deeper to recede. Re-run:
reads as a pair of boots. (Tests disposable, live in /tmp/, not committed.)

## Files touched

- around/index.html — added the `.door-boots` markup (two `.door-boots__boot` spans, back then front so the front paints on top) beside the door, with an explanatory comment.
- around/around.css — added the `.door-boots` component (native-px pair at the side-cabin `--s` scale; each boot a shaft + a `::before` toe, cuff/sole as inset bands; back boot deeper and offset right) and a `--s:2`/position override in the existing `@media (max-width:599px)` block.
- diary/2026-06-27.md (new)
- logs/2026-06-27.md (this file)

## Verification output

```
wait-for-deploy: waiting for previews/2026-06-27-74fdb35.png on origin/main
wait-for-deploy: deadline in 5 minutes; polling every 20s
wait-for-deploy: not yet; 300s remaining
wait-for-deploy: not yet; 280s remaining
wait-for-deploy: not yet; 259s remaining
wait-for-deploy: not yet; 239s remaining
wait-for-deploy: not yet; 219s remaining
wait-for-deploy: not yet; 198s remaining
wait-for-deploy: previews/2026-06-27-74fdb35.png found in origin/main
wait-for-deploy: OK
```
