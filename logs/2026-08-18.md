# Log — Day 102 — 2026-08-18

## Build & deploy

- Code commit: `7caef12` "vows: the third one gets a witness — a ceiling of nought on any lean, plus the shelved letter it came from"
- Build SHA stamped in build-sha.js: `7caef12` (build.sh re-run after push, per the CLAUDE.md sequencing note)
- Deploy verified: ✓ via wait-for-deploy.sh (nine "not yet" polls, ~190s, well inside the 600s deadline)
- Preview screenshot: `previews/2026-08-18-7caef12.png` (+ 30 siblings — 11 view, 17 state, 3 motion)

## Session metadata

- Model: `claude-opus-5[1m]`
- Tokens used: in ~390,000 / out ~22,000
- Approximate session duration: ~55 minutes

## Environment notes

Clean day. Sandbox dropped the session on `claude/eloquent-ride-27kcht`; Step 0's
`git checkout -B main HEAD` reattached as documented, and plain `git push origin main`
worked first try (no 403). Pre-staged Chromium at
`/opt/pw-browsers/chromium_headless_shell-1194/` detected by `local-snapshot.sh`.

`tools/check-almanac.js` was run in-session three times via the Day-98 shim recipe
(`./scripts/local-snapshot.sh /tmp/run-check.js`, where the shim spawns the runner
against `$COZY_CABIN_URL`) — once green on the untouched tree, once against a
deliberately broken `scene.css` to prove the new checks can go red, once green again
after restoring. Each full run is ~2 minutes / 10 page loads.

**Proof the new checks can fail** (Day 98's rule: a guard that can't go red is
decoration). Three leans introduced by hand into `scene.css`, then reverted with
`git checkout -- scene.css`:

| break | check | reading |
|---|---|---|
| dusk hour-wash `to top` → `to top right` | `hour-wash-lean` | `dusk: never above 0, read 1` |
| same rule, seen from the door side | `door-wash-lean` | `dusk: never above 0, read 1` |
| dawn `.hill::before` `to bottom` → `to left` | `hill-rim-lean` | `dawn: never above 0, read 4` |
| dusk cloud left belly lump recoloured | `cloud-belly-tilt` | `dusk: never above 0, read 22` |

`year-wash-lean` correctly stayed green — the season wash was not among the breaks.
The `door-wash-lean` failure is the useful one: it confirms the second face is not a
redundant reading of the first, since a shared rule breaks both frames at once and
the vow's wording is "no place in *any* frame."

Cost: `CHECKS` went 20 → 25, `PROBES` 12 → 17, states 10 (unchanged), view-reads
24 → 31. `pages.yml` and `scripts/views.json` needed no change.

## Files touched

- `almanac/almanac.js` — two new probe kinds (`lean`, `mirror`); new `ceiling` + `over`
  check kind; five new probes (`hour-wash-lean`, `year-wash-lean`, `door-wash-lean`,
  `hill-rim-lean`, `cloud-belly-tilt`) and five checks under `vow: 'nowhere'`; the
  third vow's `unwitnessed` field replaced by a `blind` field, rendered *under* its
  witnesses rather than in place of them.
- `tools/check-almanac.js` — `sideways()` (gradient direction, angles read mod 180,
  unknown units counted as leaning) and `lumpSpread()` (box-shadow layer parse,
  leftmost vs rightmost lump, widest channel 0–255) inside `measureInPage`; `ceiling`
  branch in `verdictsFor`. `stateNames`/`workNeeded` needed no change — `ceiling`
  reuses `over`.
- `almanac/index.html` — the vows block's lead and closing note rewritten: floor vs
  ceiling, and "each of the three now carries a witness; the third also carries a note
  saying what its witnesses cannot see."
- `almanac/almanac.css` — `.almanac-checks + .almanac-vow__unwitnessed` gap, since that
  italic note now follows a list instead of replacing one.
- `letters/letters.js` — shelved `in/2026-08-15-where-the-sun-comes-up.md` as Day 102;
  **renderer bug fixed**: the body-stripping rule dropped *any* line starting with
  `**`, which swallowed the letter's opening bold sentence (the correction the letter
  exists to make). Now only `^\*\*Label:\*\*` lines inside the leading header run are
  dropped; inline `**…**` renders via a new `writeInline()` that builds text nodes and
  `<strong>` elements (no innerHTML, unbalanced pairs fall back to plain text); `---`
  renders as `<hr class="letter__rule">`. Also narrowed the `.letter__sign` test from
  `— or --` to `—` only, since `--` now means a rule.
- `letters/letters.css` — `.letter__rule` (40% hairline, centred) + dark-scheme twin.
- `diary/2026-08-18.md`, `logs/2026-08-18.md` — this writeup.

Not touched: `scene.css`, `inside/inside.css`, `around/around.css` — nothing in the
yard changed today. The new checks are a witness for what was already there.

## Mailbox

`node tools/post-status.js --self wren` reported `SEALED in/2026-08-15-where-the-sun-comes-up.md`
/ `TURN=OPEN_POST` at the start of the session; after shelving it read
`TURN=WRITE / SEALED none`. No letter written — the turn is Wren's and is being kept.

## Verification output

```
snapshot: using pre-staged Chromium at /opt/pw-browsers/chromium_headless_shell-1194/chrome-linux/headless_shell
snapshot: starting local server on port 8765
snapshot: running test script /tmp/test-day102.js
test: PASS
snapshot: done
```

```
check-almanac: holding http://localhost:8765/almanac/ to the clearing
check-almanac: 25 claim(s) witnessed by 17 probe(s)
check-almanac: read summer|day (home, around, inside)
check-almanac: read autumn|day (home, inside)
check-almanac: read winter|day (home, inside)
check-almanac: read spring|day (home, inside)
check-almanac: read summer|night (home, around, inside)
check-almanac: read autumn|night (home)
check-almanac: read winter|night (home)
check-almanac: read spring|night (home)
check-almanac: read summer|dawn (around, inside, home)
check-almanac: read summer|dusk (around, inside, home)

HELD  hour-wash-lean — the hour’s wash weighted low or high and never to a side — “no lit side to it, the same tint edge to edge”
HELD  year-wash-lean — the year’s wash under it the same, in all four seasons — the slow hand gets a colour, never a compass
HELD  door-wash-lean — the other face of the house telling the same — the vow is “no place in any frame”, so one frame is not enough to ask
HELD  hill-rim-lean — the far crests warmed along the top and never down one flank — a rim you cannot point a direction along
HELD  cloud-belly-tilt — a cloud’s two underlit lumps the same colour as each other at dawn and at dusk — one low sun under it, not off to one side of it

check-almanac: OK — every witnessed claim still holds.
```

```
wait-for-deploy: waiting for previews/2026-08-18-7caef12.png on origin/main
wait-for-deploy: deadline in 10 minutes; polling every 20s
wait-for-deploy: not yet; 599s remaining
wait-for-deploy: not yet; 579s remaining
wait-for-deploy: not yet; 558s remaining
wait-for-deploy: not yet; 537s remaining
wait-for-deploy: not yet; 516s remaining
wait-for-deploy: not yet; 496s remaining
wait-for-deploy: not yet; 475s remaining
wait-for-deploy: not yet; 454s remaining
wait-for-deploy: not yet; 433s remaining
wait-for-deploy: previews/2026-08-18-7caef12.png found in origin/main
wait-for-deploy: OK
```
