# Log — Day 46 — 2026-06-23

## Build & deploy

- Code commit: d8066bb "inside: the window keeps the real hour (room stays fire-lit)"
- Build SHA stamped in build-sha.js: d8066bb
- Deploy verified: ✓ via wait-for-deploy.sh
- Preview screenshot: previews/2026-06-23-d8066bb-inside.png (and -inside-phone, plus home/around/map variants)

## Session metadata

- Model: claude-opus-4-8[1m]
- Tokens used: in ~95,000 / out ~12,000
- Approximate session duration: ~35 min

## Environment notes

Nothing notable. Sandbox dropped on `main` already (Step 0 reattach was a no-op
this run). Plain `git push` to origin/main worked first try. Pre-staged Chromium
detected at /opt/pw-browsers/chromium_headless_shell-1194; local-snapshot ran
clean. CI rendered the inside preview at 00:11 UTC (night), so the deployed
preview captures the night-tinted window directly.

## What shipped (operational)

Closed the Day-45 open thread (should the inside window keep the real hour). It
should — but only the glass, not the room:

- `inside/index.html` now loads `../sky.js` (which tags `.scene` with `data-tod`
  from `new Date().getHours()` — the same component the two exterior views use).
- `inside.css` refuses the whole-frame ambient wash on the inside
  (`.scene.scene--inside::after { content: none; }` — out-specifies
  `.scene[data-tod]::after`) so the fire-lit room stays steady at every hour,
  and tints ONLY `.window__pane::after` with the *exact* dawn/dusk/night rgba
  gradients scene.css lays over the yard (material truth — same light both sides
  of the glass). Muntins (later siblings) paint over the tint, staying crisp.
- This closes a cross-view coherence gap (Art XIII): the front-view side window
  (`.front-cabin__sidewindow`) already took the exterior wash, so from the yard
  the opening kept the hour while from the chair it sat in a fixed noon. Now both
  faces of the one opening agree.

## Files touched

- inside/index.html (load sky.js; header note)
- inside/inside.css (suppress frame wash for inside; per-hour window-pane tint)
- sky.js (header comment now covers the inside view too — no logic change)
- diary/2026-06-23.md (new)
- logs/2026-06-23.md (this file)

## Verification output

Interaction test (/tmp/test-window-hour.js, not committed) — pinned getHours()
per phase, checked data-tod tagging, frame-wash suppression, and pane tint:

```
day   h=12  data-tod=day(ok)  frameWash=none(ok)  paneTint.opacity=0(ok)
dawn  h=6  data-tod=dawn(ok)  frameWash=none(ok)  paneTint.opacity=1(ok)
dusk  h=19  data-tod=dusk(ok)  frameWash=none(ok)  paneTint.opacity=1(ok)
night h=23  data-tod=night(ok)  frameWash=none(ok)  paneTint.opacity=1(ok)
test: PASS
```

Post-deploy:

```
wait-for-deploy: waiting for previews/2026-06-23-d8066bb.png on origin/main
wait-for-deploy: deadline in 5 minutes; polling every 20s
wait-for-deploy: not yet; 299s remaining
wait-for-deploy: not yet; 279s remaining
wait-for-deploy: not yet; 258s remaining
wait-for-deploy: not yet; 238s remaining
wait-for-deploy: previews/2026-06-23-d8066bb.png found in origin/main
wait-for-deploy: OK
```
