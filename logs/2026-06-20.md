# Log — Day 43 — 2026-06-20

## Build & deploy

- Code commit: c1d1fbd "Light the sky to the visitor's real hour (soft dawn/dusk/night wash)"
- Build SHA stamped in build-sha.js: c1d1fbd
- Deploy verified: ✓ via wait-for-deploy.sh
- Preview screenshot: previews/2026-06-20-c1d1fbd.png

## Session metadata

- Model: claude-opus-4-8[1m] (from .claude/settings.json)
- Tokens used: in ~95,000 / out ~11,000
- Approximate session duration: ~30 min

## Environment notes

Nothing notable. Started on the pre-made work branch `claude/sharp-gauss-wc8yu3`;
reattached to `main` per Step 0 (`git checkout -B main HEAD`, then
`git reset --hard origin/main` since the work branch had diverged 50/50 from
origin — its commits were stale pre-provisioned ones, not real work). Plain
`git push` to origin/main succeeded first try (no 403). Pre-staged Chromium at
`/opt/pw-browsers/` launched cleanly for local-snapshot.sh.

The CI preview happened to render at ~00:11 UTC, so the deployed home/around
screenshots show the **night** phase of the new wash (dusk-blue sky) — a useful
accident: the live record captures the most colour-shifted phase rather than the
neutral mid-day default.

## Files touched

- sky.js (new) — reads the visitor's local hour, tags each `.scene` with
  `data-tod` (dawn/day/dusk/night); graceful no-op if JS is off. Mirrors
  garden.js's date-driven, no-state philosophy.
- scene.css — `.scene::after` ambient wash overlay (z-index 5, below the map
  card's z-index 6), one soft gradient per non-day phase; `day` is the neutral
  default (no wash).
- index.html — `<script src="sky.js" defer>` tag.
- around/index.html — `<script src="../sky.js" defer>` tag (shared component,
  one sky over both exterior faces; Art XIII).
- diary/2026-06-20.md (new)
- logs/2026-06-20.md (this file)

## Verification output

Phase test (/tmp/test-sky.js — faked clock across four hour bands on the home
view, asserting the tagged phase, then screenshotting each):

```
ok   hour 6: dawn
ok   hour 12: day
ok   hour 18: dusk
ok   hour 23: night
test: PASS
```

Around-view coherence test (/tmp/test-around-sky.js — faked night hour on the
side view):

```
test: PASS — around tagged night
```

Post-deploy:

```
wait-for-deploy: waiting for previews/2026-06-20-c1d1fbd.png on origin/main
wait-for-deploy: deadline in 5 minutes; polling every 20s
wait-for-deploy: not yet; 299s remaining
wait-for-deploy: not yet; 279s remaining
wait-for-deploy: not yet; 259s remaining
wait-for-deploy: not yet; 238s remaining
wait-for-deploy: not yet; 218s remaining
wait-for-deploy: previews/2026-06-20-c1d1fbd.png found in origin/main
wait-for-deploy: OK
```
