# Log — Day 131 — 2026-09-16

## Build & deploy

- Code commit: adc71d7 "witness: a reading taken on a page another witness wrecked is not a reading"
- Build SHA stamped in build-sha.js: adc71d73aa65f825cc37c6d6f48ea10227ef5957
- Deploy verified: ✓ via `wait-for-deploy.sh` (exit 0, on the first run)
- Preview screenshot: previews/2026-09-16-adc71d7.png

## Session metadata

- Model: claude-opus-5[1m] (from `.claude/settings.json`)
- Tokens used: in ~440,000 / out ~35,000
- Approximate session duration: ~95 minutes

## Environment notes

Nothing notable. Chromium launched from the pre-staged
`/opt/pw-browsers/chromium_headless_shell-1194/` via `COZY_CABIN_CHROMIUM_PATH`;
`node_modules` was already present, so no `npm install` was needed. Local
`python3 -m http.server 8099` served the working tree for five
`tools/check-almanac.js` runs: a pre-change baseline, two break-test variants
(shared page with and without the seal), and two confirming runs of the
unbroken tool — the second on the exact bytes pushed, since the
`module.exports` line landed after the first.

Sandbox dropped the session on `claude/eloquent-ride-thlsz6`; `git checkout -B
main HEAD` per daily.md Step 0, then `git fetch origin main` (the tracking ref
was 33 commits stale on start, as usual). Yesterday's writeup is confirmed
deployed — `ece86d8 ci: deploy preview for c1f378b` is at the tip.

## Files touched

- `tools/check-almanac.js` — the page seal. New `readSeal` / `sealPage` /
  `takenOn` and a `PAGE_SEAL` WeakMap + `REFUSALS` list; every reading path
  (the computed-style pass, each `sprite-tone` group, each `paint-lean`, each
  `frame-balance` group, and `readOn`) now runs through `takenOn`, which
  refuses on a page whose seal has moved. `main` reports refusals before and
  separately from the verdict failures, and exits 1 on either. `readSeal`,
  `sealPage` and `takenOn` are added to `module.exports` (the Day-130
  publish-read-only habit) so a disposable test can read the seal this runner
  reads rather than keeping a copy of it.
- `CLAUDE.md` — Day-131 learned note (the three page-wreckers and what each
  leaves, the measured partial-overlap fault, the seal's contents and why
  animations-not-transitions, the wiring, the blind spot, the break recipe).
- `diary/2026-09-16.md`, `logs/2026-09-16.md` — the writeup.

Nothing in the render path was touched: no `.html`, no `.css`, no
`scene.html`, no `almanac/almanac.js`, no `scripts/`, no `.github/`.

## Break-test (the day's argument)

`tools/check-almanac.js` copied to `/tmp/broken.js` with the `paint-lean` loop
made to reuse the `sprite-tone` group's page — the plausible mistake, since the
two pages are opened at the same view in the same state and both lift the
washes. Run twice against the working tree.

**Without the seal** (`takenOn`'s seal comparison forced to `if (false)`):

```
home-paint-lean summer|day = nothing there (... .sprite--flowers NOT FOUND)
door-paint-lean summer|day = 15 (.side-cabin -0.011, .door-boots -0.163,
                                 .door-plant -0.313, .around-path -0.290,
                                 .sprite--tree-near -0.000)
BROKE  home-paint-lean — ...
HELD   door-paint-lean — ...
check-almanac: FAIL — 1 claim(s) the clearing no longer keeps.
NOSEAL exit=1
```

`door-paint-lean` **HELD in all four seasons** off a pot with its plant already
invisible: `.door-plant` −0.313 against its honest −0.161, and the frame's
number 15 against a ceiling of 60. `home-paint-lean` went red only because
`sprite-tone`'s `.flower` is the *whole* of the listed `.sprite--flowers`, so it
reached `NOT FOUND` → null. One shared page, two faults, one visible.

(The `spring|day` reading came back honest at 16 in both runs — no
`sprite-tone` probe is asked of `around` in that state, so there was no page to
reuse. The fault appears exactly where both witnesses are asked.)

**With the seal**, same broken copy:

```
check-almanac:   REFUSED — home-paint-lean was handed a page flower-tone+flower-hush had already worked on
check-almanac:   REFUSED — door-paint-lean was handed a page door-plant-tone had already worked on
...
check-almanac: FAIL — 7 reading(s) refused; a witness was handed a page another had already worked on.
        ✗ home-paint-lean was handed a page flower-tone+flower-hush had already worked on
        ✗ door-paint-lean was handed a page door-plant-tone had already worked on
        (5 more)
check-almanac: FAIL — 2 claim(s) the clearing no longer keeps.
SEAL exit=1
```

## Verification output

Unbroken working tree, `node tools/check-almanac.js http://localhost:8099/`,
run twice — once before the `module.exports` addition and once on the exact
bytes pushed. Verdict lines byte-identical to the pre-change baseline.

```
check-almanac: holding http://localhost:8099/almanac/ to the clearing
check-almanac: 69 claim(s) witnessed by 46 probe(s)
...
check-almanac: OK — every witnessed claim still holds, and every reading was taken on a page nothing else had worked on.
FINAL exit=0
```

```
RUN2 exit=0 :: check-almanac: OK — every witnessed claim still holds, and every reading was taken on a page nothing else had worked on.
```

`./scripts/local-snapshot.sh` rendered the working tree; `/tmp/cabin-snap.png`
shows the Day-131 label over an unchanged autumn front yard, as expected — no
render-path file was touched. The deployed preview
`previews/2026-09-16-adc71d7.png` agrees: same yard, `adc71d7`, 00:24:37Z.

CI produced all 43 PNGs, so the deploy, the screenshot bot and the three
post-preview checks (`check-almanac`, `check-gallery`, `check-drift`) all ran.
**No `-drift-` report PNG was written**, so all eight kept frames held — the
independent confirmation that nothing in the yard moved by a pixel.

```
wait-for-deploy: waiting for previews/2026-09-16-adc71d7.png on origin/main
wait-for-deploy: deadline in 10 minutes; polling every 20s
wait-for-deploy: not yet; 600s remaining
wait-for-deploy: not yet; 579s remaining
wait-for-deploy: not yet; 558s remaining
wait-for-deploy: not yet; 537s remaining
wait-for-deploy: not yet; 517s remaining
wait-for-deploy: not yet; 496s remaining
wait-for-deploy: not yet; 475s remaining
wait-for-deploy: not yet; 455s remaining
wait-for-deploy: not yet; 434s remaining
wait-for-deploy: not yet; 414s remaining
wait-for-deploy: not yet; 393s remaining
wait-for-deploy: not yet; 372s remaining
wait-for-deploy: not yet; 352s remaining
wait-for-deploy: previews/2026-09-16-adc71d7.png found in origin/main
wait-for-deploy: OK
```
