# Log — Day 11 — 2026-05-19

## Build & deploy

- Code commit: `d5a2af9` "inside: hearth wall sub-page — the brick from the side that has the fire (Day 11)"
- Build SHA stamped in build-sha.js: `d5a2af9` (will be regenerated by CI on deploy; locally regenerated after the push for `wait-for-deploy.sh`'s glob match).
- Deploy verified: ✓ via `wait-for-deploy.sh`.
- Preview screenshots: `previews/2026-05-19-d5a2af9.png` (home, unchanged from yesterday), `previews/2026-05-19-d5a2af9-around.png` (around, unchanged from yesterday), `previews/2026-05-19-d5a2af9-inside.png` (inside, **new** — first per-commit visual record of the interior).

## Session metadata

- Model: claude-opus-4-7 (Opus 4.7, 1M context)
- Tokens used: in ~150,000 / out ~14,000 (rough — full read of RULES + CLAUDE + MILESTONES + daily.md + the diary/log/meta/messages READMEs + all ten diary entries + the Day-7 meta + Day 9/10 logs + all open messages including the full door-or-window thread + the screenshot-all-views thread in `done/` for views.json precedent + theme.css + scene.html + scene.css + around/* + index.html + diary/index.html + the latest home/around previews + the local snapshot iteration + this writeup).
- Approximate session duration: ~50 min.

## Environment notes

- **`git push` worked first try.** Fourth consecutive clean push (Days 8, 9, 10, 11). The CLAUDE.md note about the 403 fallback to `mcp__github__push_files` is now stale enough that softening it could be reasonable — but four days isn't much weight against the historical pattern from Days 2/3/4/5/7, and the cost of keeping the note is low. Leaving it as-is for now.
- The sandbox's initial `origin/main` tracking ref was stale on session start (18 commits behind the actual remote HEAD). A fresh `git fetch origin main` reconciled it. Same papercut Day 10 logged. Not blocking.
- Playwright launched cleanly via the pre-staged Chromium at `/opt/pw-browsers/chromium_headless_shell-1194/chrome-linux/headless_shell` for the `/tmp/test-inside.js` interaction test (asserted four-link nav across home/around/inside, the inside link click works, the hearth elements render at non-zero sizes, and the around view's door is still intact). Pass on first run.
- `scripts/views.json` is technically under the `scripts/` lock per RULES.md Article I, but the founder's 2026-05-15 follow-up on `messages/done/2026-05-13-screenshot-all-views.md` explicitly granted that the lock's carve-out covers adding support files under `scripts/` for the screenshot work — and `views.json` itself was Wren's creation on Day 10 specifically as the editable manifest. Adding the `inside` entry here is the intended use of the file.

## Files touched

- `inside/index.html` — new. Mirrors `around/index.html`'s shell contract (build-sha meta, build-sha.js script, day/build labels). Inlines the hearth markup directly rather than fetching a fragment, same as the around page does. Detailed file-header comment documents the cross-view geometry (Article XIII): hearth wall = south wall, brick visible through front-view window = back of this column.
- `inside/inside.css` — new. Styles the interior: `.inside__wall` (upper 70%, wood planks) and `.inside__floor` (bottom 30%, lighter wood planks) replace the `.scene__sky` / `.scene__ground` exterior layers. The `.hearth` component is a centred column with sub-elements for the brick column, the mantle, the fire opening, the log, and three stacked flame layers each flickering on its own clock (1.2s / 0.9s / 0.7s mismatched periods, so the fire breathes rather than strobes). Mobile-narrow override scales 3x → 2x mirroring scene.css and around.css.
- `index.html` — nav strip extended from 3 to 4 links: home / diary / around / **inside**. No other change.
- `diary/index.html` — same nav extension.
- `around/index.html` — same nav extension.
- `scripts/views.json` — appended `{ "name": "inside", "url_path": "inside/" }`. CI's manifest-driven screenshot loop picks this up automatically; no workflow or script change needed (the Day-10 work was designed exactly for this case).
- `diary/2026-05-19.md` — new; today's Wren entry under the four-section schema. Pondering drafted at Step 2.7 (after memory pass, before implementation), the rest during the `wait-for-deploy.sh` poll. Passes `./scripts/lint-diary.sh`.
- `logs/2026-05-19.md` — new; this file.

## Verification output

```
wait-for-deploy: waiting for previews/2026-05-19-d5a2af9.png on origin/main
wait-for-deploy: deadline in 5 minutes; polling every 20s
wait-for-deploy: not yet; 300s remaining
wait-for-deploy: not yet; 279s remaining
wait-for-deploy: not yet; 259s remaining
wait-for-deploy: not yet; 239s remaining
wait-for-deploy: previews/2026-05-19-d5a2af9.png found in origin/main
wait-for-deploy: OK
```

Three preview PNGs landed for this commit: `previews/2026-05-19-d5a2af9.png` (home, unchanged five-stone path / brick window / smoke / two trees / now four-link nav including `inside`), `previews/2026-05-19-d5a2af9-around.png` (around, unchanged side cabin with door + angled stones / four-link nav), `previews/2026-05-19-d5a2af9-inside.png` (inside, new — wood plank wall, lighter plank floor, centred brick column from top to floor, dark wood mantle, fire opening with a small orange/yellow flame on a single log). The cross-view check (RULES.md Article XIII) reads cleanly: the cream-stone brick of the inside hearth column matches the brick visible through the front-view window from outside; the chimney visible above the cabin in the home and around views is the top of this same column.
