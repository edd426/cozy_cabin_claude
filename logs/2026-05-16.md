# Log — Day 8 — 2026-05-16

## Build & deploy

- Code commit: `36f2b29` "scene: front path emerges from behind cabin's right side (Day 8)" — `scene.css` (path layout rework + mobile-narrow override) and `messages/open/2026-05-11-door-or-window.md` (Wren's notes, Day 8 progress).
- Build SHA stamped in build-sha.js: `36f2b29` (re-run was not needed today — `build.sh` ran once before the push and the push went straight through, so `build-sha.txt` matched `HEAD` on the first poll).
- Deploy verified: ✓ via `wait-for-deploy.sh`.
- Preview screenshot: `previews/2026-05-16-36f2b29.png`.

## Session metadata

- Model: claude-opus-4-7
- Tokens used: in ~115,000 / out ~15,000 (rough — full read of RULES + CLAUDE + MILESTONES + daily.md + three open messages + four done messages + diary/log/meta schemas + Day 0/1/2/3/4/5/7 diary + Day 4/5/7 logs + Day 7 meta + the latest preview + scene.html + scene.css + index.html + around/index.html + around/around.css + the path iteration cycle + this writeup).
- Approximate session duration: ~30 min.

## Environment notes

- **`git push` worked first try.** This is the first time in the routine's history (Days 2 / 3 / 4 / 5 / 7 all 403'd through the local proxy and had to fall back to `mcp__github__push_files`). Today the push went through to the local `127.0.0.1:36357` proxy and landed on `origin/main` cleanly with the expected SHA `36f2b29`. I did nothing different — same `git push -u origin main` command shape as the CLAUDE.md "Things I've learned" note describes. Worth noting but not worth changing the playbook over: a single happy-path session doesn't override the four-session pattern that motivated the MCP fallback. If Day 9+ pushes also work, the CLAUDE.md note can soften.
- The single-push code commit means the `build.sh` → `git push` → `wait-for-deploy.sh` sequence ran without needing the `git reset --hard origin/main` realignment step that the MCP-multi-commit sessions have all needed. The build SHA matched `HEAD` on the first poll.
- Playwright launched cleanly via the pre-staged Chromium at `/opt/pw-browsers/chromium_headless_shell-1194/chrome-linux/headless_shell` for both the home-page snapshot and the `/tmp/snap-around.js` quick-snap of the around view (no interaction assertions today — just a visual check that the scene.css edit didn't regress the around scene's path).

## Files touched

- `scene.css` — `.sprite--path` wrapper changed from a small bottom-anchored container to scene-spanning (`inset: 0`); three `.path-stone--N` rules repositioned to put the back stone at mid-cabin-height pressed against the cabin's right edge (partially occluded by cabin's `z-index: 2`), with mid + front stones descending diagonally to exit the bottom-right. `@media (max-width: 379px)` block extended with the path stones rebased to the 2x cabin's geometry. Comment header rewritten to describe the new "emerging from behind" reading.
- `messages/open/2026-05-11-door-or-window.md` — appended a `### Wren's notes — 2026-05-16 (Day 8)` subsection. File stays in `open/` per the founder's screenshot-all-views note ("Close door-or-window's gate. Per the acceptance criteria above, once CI captures the around view, update the closure criterion in messages/open/2026-05-11-door-or-window.md so the fresh-viewer's-eye check explicitly covers all rendered views.").
- `diary/2026-05-16.md` — new; today's Wren entry under the post-reform four-section schema. Pondering section was drafted at Step 2.7 (right after the read-pass), the rest after the local snapshot showed the new layout reading correctly.
- `logs/2026-05-16.md` — new; this file.

## Verification output

```
wait-for-deploy: waiting for previews/2026-05-16-36f2b29.png on origin/main
wait-for-deploy: deadline in 5 minutes; polling every 20s
wait-for-deploy: not yet; 300s remaining
wait-for-deploy: not yet; 280s remaining
wait-for-deploy: not yet; 259s remaining
wait-for-deploy: not yet; 239s remaining
wait-for-deploy: previews/2026-05-16-36f2b29.png found in origin/main
wait-for-deploy: OK
```

The deployed preview at `previews/2026-05-16-36f2b29.png` shows the home page with the front path reworked: the back stone appears as a small earth-brown nub at mid-cabin-height on the cabin's right wall (most of the stone occluded behind the cabin), the mid stone sits on the grass strip to the right of the cabin, and the front stone exits the bottom-right edge of the scene partly off-frame. Smoke + cream sky + brick-infill window + two trees flanking are unchanged. Day-8 label and SHA `36f2b29` render correctly in the footer.
