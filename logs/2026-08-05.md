# Log — Day 89 — 2026-08-05

## Build & deploy

- Code commit: a29f47f "inside: the fire's light reaches the floor, and leans with the year; an inside season rail for the gallery"
- Build SHA stamped in build-sha.js: a29f47f
- Deploy verified: ✓ via wait-for-deploy.sh
- Preview screenshot: previews/2026-08-05-a29f47f.png

## Session metadata

- Model: claude-opus-5[1m] (from .claude/settings.json; effortLevel max)
- Tokens used: in ~450,000 / out ~30,000
- Approximate session duration: ~65 min

## Environment notes

Clean day. Sandbox dropped the session on `claude/eloquent-ride-mthkqu`
(routine outcomes pin, as expected); `git checkout -B main HEAD` reattached,
and `git fetch origin main` was needed before HEAD read as equal to
`origin/main` rather than "18 commits ahead" — same stale-tracking-ref
behaviour Day 88's log recorded. Plain `git push` worked first try (no 403).
Playwright launched against the pre-staged Chromium at
`/opt/pw-browsers/chromium_headless_shell-1194/chrome-linux/headless_shell`.
Yesterday's writeup (1ee4a0e) is confirmed deployed — `c1883ef ci: deploy
preview for 1ee4a0e` is in the log.

Today closes the gap Day 88's log flagged for the founder's attention: the
forced-state gallery was home-only, so Days 82 / 83 / 88's season-gated
*indoor* work existed in no permanent capture. `GALLERY_STATES` entries in
`scripts/screenshot.js` now take an optional `view` (defaulting to the home
root), and four inside-season states were added. **`pages.yml` needed no
change** — the workflow already invokes `--gallery` and the new states ride
along under the existing `continue-on-error: true` step, so the unsuffixed
home PNG that `wait-for-deploy.sh`'s locked contract depends on is still
written first by the manifest step and cannot be knocked over.

Repo-growth note, since the founder named growth as the reason the gallery
was home-only: +4 PNGs per commit at ~35–46 KB each (≈ +160 KB). For scale,
the two map captures alone are ~400 KB of every commit and the three motion
strips ~285 KB. `previews/` is 110 MB; the Pages artifact excludes it
(2026-07-03 fix), so this costs repo size only.

Interaction between the runbook's Step-2.8 pondering pre-draft and `git add
-A` bit again exactly as Day 88's log predicted — `diary/2026-08-05.md`
existed before the code commit and was staged by `add -A`, then unstaged
with `git restore --staged` so the two commits stay clean. Worth a runbook
note if a third day hits it.

## Files touched

- `inside/index.html` — new `.hearth-light` div (aria-hidden) after
  `.inside__floor`; the hearth's `role="img"` label extended to mention the
  pool of firelight on the boards.
- `inside/inside.css` — the `.hearth-light` block: an ellipse centred on the
  firebox floor line (`left:50%; bottom: calc(11px * var(--s));
  transform: translate(-50%, 50%)`), sized from `--light-w` / `--light-h` in
  native px, gradient on `::before` with a 2.1s breathe, and three season
  rules (autumn/spring, winter) swapping the two vars plus `opacity`. The
  whole 2x override is the single line `.hearth-light { --s: 2; }`.
- `scripts/screenshot.js` (RULES Art I carve-out) — `GALLERY_STATES` entries
  may carry a `view`; `galleryRun` resolves the URL per state instead of once;
  `GALLERY_INSIDE_VIEW` + four `inside-{spring,summer,autumn,winter}` states;
  header docs updated.
- `CLAUDE.md` — new "Things I've learned" entry: the `radial-gradient`
  `farthest-corner`-default hard-edge trap and the `closest-side` fix (plus
  its dimming second-order effect), the element-opacity / `::before`-gradient
  split so a season and an animation don't fight over `opacity`, and the
  gallery's new per-state `view` field + `-state-inside-<season>` naming.
- `diary/2026-08-05.md` (new), `logs/2026-08-05.md` (this file).

## Verification output

```
wait-for-deploy: waiting for previews/2026-08-05-a29f47f.png on origin/main
wait-for-deploy: deadline in 10 minutes; polling every 20s
wait-for-deploy: not yet; 600s remaining
wait-for-deploy: not yet; 579s remaining
wait-for-deploy: not yet; 558s remaining
wait-for-deploy: not yet; 538s remaining
wait-for-deploy: not yet; 517s remaining
wait-for-deploy: not yet; 496s remaining
wait-for-deploy: not yet; 476s remaining
wait-for-deploy: not yet; 455s remaining
wait-for-deploy: previews/2026-08-05-a29f47f.png found in origin/main
wait-for-deploy: OK
```

CI bot commit `a2d30ce ci: deploy preview for a29f47f`; **22 PNGs** written
(8 view + 11 state + 3 motion), up from 19 — the four new ones are
`previews/2026-08-05-a29f47f-state-inside-{spring,summer,autumn,winter}.png`.
The deployed `-state-inside-winter.png` shows the deeper Day-88 fire with
today's pool washing out across the boards to the chair and the woodpile;
`-state-inside-summer.png` shows the small taper and a tight halo at the
hearth's foot. The live `-inside.png` (captured at night, in summer) shows
the summer pool, correct — the room turns the hour back at the door, so the
pool answers the fire and the year only.

Local verification, two disposable Playwright scripts in `/tmp/` (never
committed): `shot-inside.js` (floor-band crops per season at 375 and 390) and
`test-hearth-light.js`, which asserts, at 375 / 390 / desktop-900:

1. the pool is centred on the hearth's centre (±1.5px) and seated wholly
   inside the floor band, never lapping the floor/wall joint even at its
   winter widest;
2. widths are exactly native × `--s` (52 → 78 native), i.e. 104/156 at 2x and
   156/234 at 3x — the scale rebase is real, not eyeballed;
3. stacking is pool `z:1` under chair / woodpile / hearth `z:2`;
4. width, height and opacity all increase monotonically summer → autumn →
   winter, and winter reaches ≥20 native further past the chair than summer,
   while staying under 62% of the floor's width (it must remain a pool);
5. the pool is byte-identical at `tod:day` and `tod:night` — the room is
   fire-lit, not sky-lit;
6. `.hearth-light` appears on `/inside/` only, zero occurrences on the home
   and around views.

```
snapshot: using pre-staged Chromium at /opt/pw-browsers/chromium_headless_shell-1194/chrome-linux/headless_shell
snapshot: starting local server on port 8765
snapshot: running test script /tmp/test-hearth-light.js
phone-375: ok  (summer 104x72 @0.72, winter 156x88 @1)
phone-390: ok  (summer 104x72 @0.72, winter 156x88 @1)
desktop: ok  (summer 156x108 @0.72, winter 234x132 @1)
test: PASS
snapshot: done
```

`--gallery` was also run end-to-end against a local `python3 -m http.server`
before pushing, to prove the new per-state `view` resolves and that the four
indoor frames write under the expected names:

```
screenshot: navigating to http://localhost:8799/inside/ for state inside-spring
screenshot: writing /tmp/gal/2026-08-05-test-state-inside-spring.png
screenshot: navigating to http://localhost:8799/inside/ for state inside-summer
screenshot: writing /tmp/gal/2026-08-05-test-state-inside-summer.png
screenshot: navigating to http://localhost:8799/inside/ for state inside-autumn
screenshot: writing /tmp/gal/2026-08-05-test-state-inside-autumn.png
screenshot: navigating to http://localhost:8799/inside/ for state inside-winter
screenshot: writing /tmp/gal/2026-08-05-test-state-inside-winter.png
screenshot: done — 12 forced-state capture(s)
```
