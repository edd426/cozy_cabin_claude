# Log — Day 26 — 2026-06-03

## Build & deploy

- Code commit: `2ce6cdc` "Reconcile inside hearth to running-bond brick matching the front window"
- Build SHA stamped in build-sha.js: `2ce6cdc` (build.sh run before commit; build-sha.txt matches the pushed HEAD)
- Deploy verified: ✓ via wait-for-deploy.sh (see Verification output)
- Preview screenshot: `previews/2026-06-03-2ce6cdc.png` (home) + `-around` / `-inside` / `-map` siblings

## Session metadata

- Model: claude-opus-4-8
- Tokens used: in ~185,000 / out ~16,000 (rough — full memory pass: all 25 diary entries plus day-zero/day-1, 4 meta entries, 3 open messages incl. the long visual-coherence + door-or-window threads, the 4 view previews, and theme.css / scene.css / inside.css in full)
- Approximate session duration: ~50 min

## Environment notes

- **Remaking step 3 (action-ask, high priority).** Continued the multi-day
  remaking from `messages/open/2026-05-30-visual-coherence.md` — outranks
  self-selected work (Article X). Steps 1 (map/nav, Day 24) and 2 (front cabin
  rebuilt in CSS, Day 25) are done; today is step 3: pending item #1, the
  interior hearth reconciled into the one-building brick idiom. File stays in
  `open/` with a fresh Wren's-notes handoff; the roof chimney (still a dark
  block, flagged as new pending #1), the map-orientation fix, the strip→map nav
  end-state, and the deferred small-fixes list are all still pending.
- **Material reuse, not look-alike.** The hearth now draws its brick from the
  same `.brick-course` / `.brick-course--b` classes the front-view window uses
  (defined in `scene.css`, which the inside page already loads) — set `--s` on
  `.hearth` so the shared class scales 3x/2x. Changing the window brick will now
  change the hearth brick automatically; that coupling is intentional (one
  masonry column = one material, per the founder's "material truth" note).
- **Branch:** sandbox dropped the session on `claude/sharp-gauss-KfONK`, not
  detached-`main`. Pushed both commits with `git push origin HEAD:main` per the
  routine override; confirmed origin/main advanced (`560f0b0..2ce6cdc` for the
  code commit).
- Playwright/Chromium launched fine (pre-staged at /opt/pw-browsers/). Default
  `local-snapshot.sh` renders the root (home) view only; captured `/inside/`
  with a small `/tmp/snap-inside.js` that navigates there and asserts the brick
  body's 20 courses.

## Files touched

- `inside/inside.css` — rebuilt `.hearth__column` from a pale-stone gradient
  (8-native horizontal courses, no vertical joints, dark-trim box-shadow) into a
  wood-dark frame holding a new inset `.hearth__brick` body; the brick itself is
  the shared `.brick-course` idiom. Added `--s` (3 desktop / 2 mobile) to
  `.hearth`; dropped the now-obsolete `.hearth__column` mobile override.
- `inside/index.html` — filled `.hearth__column` with `.hearth__brick` + 20
  running-bond course divs; updated the hearth aria-label (pale stone-brick →
  running-bond brick, same as the window).
- `messages/open/2026-05-30-visual-coherence.md` — Day-26 Wren's notes (inside
  reconciled; roof-chimney flagged as new pending).
- `diary/2026-06-03.md`, `logs/2026-06-03.md` (this file) — writeup.

## Verification output

In-session Playwright snapshot (`/tmp/snap-inside.js` via local-snapshot.sh), verbatim:

```
snapshot: using pre-staged Chromium at /opt/pw-browsers/chromium_headless_shell-1194/chrome-linux/headless_shell
snapshot: starting local server on port 8765
snapshot: running test script /tmp/snap-inside.js
test: PASS — inside renders, brick body has 20 courses
snapshot: done
```

The rendered `/inside/` snapshot was read: the hearth column now reads as
running-bond brick (staggered vertical joints, warm peach body with wood
mortar) matching the front window, replacing the pale horizontal-slab stone;
mantle, fire, candle, jar, chair, and woodpile all unchanged.

Post-deploy verification (`wait-for-deploy.sh`), verbatim:

```
wait-for-deploy: waiting for previews/2026-06-03-2ce6cdc.png on origin/main
wait-for-deploy: deadline in 5 minutes; polling every 20s
wait-for-deploy: not yet; 300s remaining
wait-for-deploy: not yet; 280s remaining
wait-for-deploy: not yet; 259s remaining
wait-for-deploy: not yet; 239s remaining
wait-for-deploy: not yet; 219s remaining
wait-for-deploy: previews/2026-06-03-2ce6cdc.png found in origin/main
wait-for-deploy: OK
```

The deployed inside preview (`previews/2026-06-03-2ce6cdc-inside.png`) was read
and confirms the brick hearth landed cleanly at 375px: running-bond brick with
staggered vertical joints and the warm peach body, matching the front-view
window's brick; mantle, fire, candle, jar, chair, and woodpile all intact.
