# Log — Day 25 — 2026-06-02

## Build & deploy

- Code commit: `c02e804` "scene: rebuild front cabin in CSS, retire Day-0 PNG (remaking step 2)"
- Build SHA stamped in build-sha.js: `c02e804` (build.sh run after the push so build-sha.txt matches HEAD)
- Deploy verified: ✓ via wait-for-deploy.sh (see Verification output)
- Preview screenshot: `previews/2026-06-02-c02e804.png` (home) + `-around` / `-inside` / `-map` siblings

## Session metadata

- Model: claude-opus-4-8
- Tokens used: in ~140,000 / out ~25,000 (rough — one large code change; full memory pass including all 25 diary entries, 4 meta entries, 3 open messages, and the 4 view previews)
- Approximate session duration: ~55 min

## Environment notes

- **Remaking step 2 (action-ask, high priority).** Continued the multi-day
  remaking from `messages/open/2026-05-30-visual-coherence.md` — outranks
  self-selected work (Article X). Step 1 (map/nav) shipped Day 24; today is
  step 2: the home-view cabin rebuilt in CSS, Day-0 PNG retired. File stays in
  `open/` with a fresh Wren's-notes handoff; `inside` reconciliation, the map
  orientation fix, and the deferred small-fixes list are still pending.
- **Spatial-coherence snag flagged, not silently resolved.** Found a
  contradiction in `map/map.css`: it claims/draws long-axis-E–W, but the
  committed view widths (front 48, around 76) imply long-axis-N–S. Built the
  front coherent with the established side view (front = short window-face) and
  wrote the discrepancy up for the founder's eye rather than reworking the
  one-day-old map under time pressure. Detail in the message's Wren's notes.
- **PNG retirement.** `assets/composed/cabin-day-0.png` is no longer referenced
  (was only in scene.html). Left the file on disk as the Day-0 artifact per
  ASSETS.md's preserve-history ethos; "retired from the scene" satisfied by
  removing the reference. No ASSETS.md row change — the CSS cabin is not a
  composed sprite file (consistent with the smoke/path/flowers/bee/bird, none
  of which are logged there).
- **Branch:** sandbox dropped the session on `claude/sharp-gauss-WsVBg`, not
  detached-`main`. Pushed with `git push origin HEAD:main` per the routine
  override; confirmed origin/main advanced to `c02e804`.
- Playwright/Chromium launched fine (pre-staged). Tool-results channel behaved
  normally this session (contrast Day 24's flakiness).

## Files touched

- `scene.html` — replaced the `<img class="sprite--cabin">` PNG with the
  `.front-cabin` CSS structure (chimney / roof / wall / brick-filled window);
  rewrote the file-header comment.
- `scene.css` — replaced the `.sprite--cabin` block with the `.front-cabin`
  component (plank wall, shingle-band roof, ink-capped chimney, running-bond
  brick window) using a `--s` scale var; updated the mobile-narrow override to
  set `--s: 2` instead of duplicating every rule.
- `messages/open/2026-05-30-visual-coherence.md` — Day-25 Wren's notes (WIP
  handoff + map-orientation snag).
- `diary/2026-06-02.md`, `logs/2026-06-02.md` (this file) — writeup.

## Verification output

In-session Playwright test (`/tmp/test-front-cabin.js` via local-snapshot.sh), verbatim:

```
snapshot: using pre-staged Chromium at /opt/pw-browsers/chromium_headless_shell-1194/chrome-linux/headless_shell
snapshot: starting local server on port 8765
snapshot: running test script /tmp/test-front-cabin.js
test: PASS — PNG retired, CSS cabin renders, window mid-wall, no overflow, around intact
snapshot: done
```

The test asserts: no `<img>` cabin sprite remains (PNG retired); `.front-cabin`
and its wall/roof/chimney/window/brick parts all render at non-zero size; four
brick courses present; the window sits mid-wall (≥4px gap to the wall foot, so
it reads as a window not a door); no horizontal overflow at 375px; and the
around view's side wall still renders (no regression). Home and around were
also read as images side by side — they now share roof, wall, and chimney
material at matching heights.

Post-deploy verification (`wait-for-deploy.sh`), verbatim:

```
wait-for-deploy: waiting for previews/2026-06-02-c02e804.png on origin/main
wait-for-deploy: deadline in 5 minutes; polling every 20s
wait-for-deploy: not yet; 300s remaining
wait-for-deploy: not yet; 280s remaining
wait-for-deploy: not yet; 259s remaining
wait-for-deploy: not yet; 239s remaining
wait-for-deploy: previews/2026-06-02-c02e804.png found in origin/main
wait-for-deploy: OK
```

The deployed home preview (`previews/2026-06-02-c02e804.png`) was read and
confirms the CSS front cabin landed cleanly at 375px: plank wall, shingle-band
roof, ink-capped chimney, and the staggered-brick window, with the smoke,
flowers, bee, path, and trees all intact.
