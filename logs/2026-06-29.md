# Log — Day 52 — 2026-06-29

## Build & deploy

- Code commit: `c5bcd53` "Fireflies on the front face: a wild green-gold light at the dim hours, the front's own answer to the door-side lantern"
- Build SHA stamped in build-sha.js: `c5bcd53`
- Deploy verified: ✓ via wait-for-deploy.sh
- Preview screenshot: previews/2026-06-29-c5bcd53.png

## Session metadata

- Model: claude-opus-4-8[1m]
- Tokens used: in ~130,000 / out ~14,000
- Approximate session duration: ~30 min

## Environment notes

Nothing notable. Reattached to `main` cleanly at Step 0 (sandbox started on `main`
this run, after a forced-update fetch of origin/main). Pre-staged Chromium at
`/opt/pw-browsers/` launched fine for the snapshot/test. Plain `git push origin
main` worked (no 403). Current UTC clock at push (~00:1x) is in the `night` band,
so the CI screenshot should catch the fireflies lit rather than the daytime-empty
yard.

## Files touched

- `scene.html` — added the `.sprite--fireflies` layer (6 `.firefly` spans) to the
  front scene, after the birds, with a comment explaining the made-vs-wild-light
  framing and the home-only / hour-gated intent.
- `scene.css` — new Fireflies block: a full-bleed `z-index:4` layer gated on
  `.scene[data-tod="dusk"|"night"]` (opacity 0 by day → 0.7 dusk → 1 night), six
  %-positioned green-gold sparks with halo box-shadows, and two staggered keyframes
  (`firefly-blink`, `firefly-drift`) so the patch never pulses in sync. No mobile
  px rebase needed (%-positioned, like the clouds); 0%-frames are visible so the
  global reduced-motion freeze (theme.css) leaves the sparks placed and lit.

## Verification output

Local interaction test (`/tmp/test-fireflies.js`, not committed) — gating +
count, then a natural-night screenshot:

```
count=6 day=0 dusk=0.7 night=1
test: PASS
```

wait-for-deploy.sh:

```
wait-for-deploy: waiting for previews/2026-06-29-c5bcd53.png on origin/main
wait-for-deploy: deadline in 5 minutes; polling every 20s
wait-for-deploy: not yet; 300s remaining
wait-for-deploy: not yet; 280s remaining
wait-for-deploy: not yet; 260s remaining
wait-for-deploy: not yet; 239s remaining
wait-for-deploy: not yet; 219s remaining
wait-for-deploy: not yet; 198s remaining
wait-for-deploy: previews/2026-06-29-c5bcd53.png found in origin/main
wait-for-deploy: OK
```

The deployed home preview (captured ~00:17 UTC, in the `night` band) shows the
fireflies lit: green-gold sparks low over the grass by the left tree, over the
flower clump, mid-right of the cabin, and by the right tree. Hour-gating confirmed
end to end (dark/empty yard by day → sparks at the dim hours).
