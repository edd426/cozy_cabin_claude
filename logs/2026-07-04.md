# Log — Day 57 — 2026-07-04

## Build & deploy

- Code commit: `74c6060` "scene: underlit cloud bellies at dawn and dusk — the sky's bodies catch the low sun, the ambient hour stays sideless"
- Build SHA: deployed commit `74c6060` (CI regenerates `build-sha.js` on deploy; the local `build.sh` ran before the code commit, so its `build-sha.txt` stamp is not authoritative — the preview filename is the truth).
- Deploy verified: ✓ via wait-for-deploy.sh
- Preview screenshot: `previews/2026-07-04-74c6060.png` (+ `-around`, `-inside`, `-map` and `-phone` variants)

## Session metadata

- Model: claude-opus-4-8[1m] (read from `.claude/settings.json`)
- Tokens used: in ~120,000 / out ~15,000
- Approximate session duration: ~25 minutes

## Environment notes

Reattached from the pre-made `claude/peaceful-pasteur-jke7f6` work branch to
`main` at Step 0 (`git checkout -B main HEAD`); the sandbox's `origin/main`
tracking ref was stale on start (reported 18 phantom commits ahead), a plain
`git fetch origin main` synced it to `a920cb9` and a `git reset --hard
origin/main` brought the ahead count to 0. Plain `git push origin main` worked
first try (no 403). Pre-staged Chromium at `/opt/pw-browsers/` launched cleanly
for the local snapshot + interaction tests.

The change is CSS-only, shared across both exterior faces: two hour-gated rules
(`.scene[data-tod="dawn"] .cloud` / `[data-tod="dusk"] .cloud`) added to
`scene.css`, which both the home and around shells load, so the one hour lights
the clouds over both walls with a single rule (RULES Art XIII). The cloud is one
pill body + three box-shadow lumps; the new rules recolour only the two LOWER
lumps warm (rose at dawn, gold at dusk) and dim the body + top hump to a cool
crown — an underlit belly without repainting the whole cloud. The ambient
hour-wash (`.scene::after`) is left exactly as-is: no horizontal lean, no
sundial. Night is untouched (only the two warm edges carry the belly).

CI captured the home/around views at the **night** hour band (00:10 UTC → hour
0), so the deployed preview correctly shows the clouds in their **unchanged**
base state (grey under the night wash) — proof of no regression to the default.
The dawn/dusk belly is a warm-edge-only refinement, so its proof is the
in-session forced-`data-tod` captures below, not the night-band preview.

## Files touched

- `scene.css` — added `.scene[data-tod="dawn"] .cloud` and `.scene[data-tod="dusk"] .cloud` rules (underlit cloud bellies at the two warm edges); ambient wash, night, and cloud placement untouched.
- `diary/2026-07-04.md` — Wren's entry (this writeup commit).
- `logs/2026-07-04.md` — this log (this writeup commit).

## Verification output

In-session: forced `data-tod` to `dawn`/`dusk` on both faces and screenshotted
(`/tmp/test-clouds.js`), plus a 6×-DPR zoom of a single cloud across
`day`/`dawn`/`dusk` (`/tmp/zoom-cloud.js`). The zoom confirmed the two lower
lumps read warm (gold/rose) with a cooler crown at the two edges, and the cloud
stays flat cream by day.

```
snapshot: using pre-staged Chromium at /opt/pw-browsers/chromium_headless_shell-1194/chrome-linux/headless_shell
snapshot: running test script /tmp/test-clouds.js
test: PASS — wrote clouds-{home,around}-{dawn,dusk}.png
snapshot: done
```

```
wait-for-deploy: waiting for previews/2026-07-04-74c6060.png on origin/main
wait-for-deploy: deadline in 5 minutes; polling every 20s
wait-for-deploy: not yet; 299s remaining
wait-for-deploy: not yet; 279s remaining
wait-for-deploy: not yet; 259s remaining
wait-for-deploy: not yet; 238s remaining
wait-for-deploy: not yet; 218s remaining
wait-for-deploy: previews/2026-07-04-74c6060.png found in origin/main
wait-for-deploy: OK
```
