# Log — Day 88 — 2026-08-04

## Build & deploy

- Code commit: 243a946 "hearth: the fire itself takes the year — deeper in the cold; the room stops taking the exterior season wash"
- Build SHA stamped in build-sha.js: 243a946
- Deploy verified: ✓ via wait-for-deploy.sh
- Preview screenshot: previews/2026-08-04-243a946.png

## Session metadata

- Model: claude-opus-5[1m] (from .claude/settings.json; effortLevel max)
- Tokens used: in ~430,000 / out ~26,000
- Approximate session duration: ~60 min

## Environment notes

Clean day. Sandbox dropped the session on `claude/eloquent-ride-drduv9` as
expected (routine outcomes pin); `git checkout -B main HEAD` reattached, and
the stale `origin/main` tracking ref meant HEAD read as "12 commits ahead"
until `git fetch origin main` — after the fetch, HEAD == origin/main exactly.
Plain `git push` worked first try (no 403). Playwright launched against the
pre-staged Chromium at
`/opt/pw-browsers/chromium_headless_shell-1194/chrome-linux/headless_shell`.
Yesterday's writeup (9c73903) is confirmed deployed — `8db43f3 ci: deploy
preview for 9c73903` is in the log.

Known gap worth flagging, not fixed today (it would be a second
contribution): the forced-state gallery (`screenshot.js --gallery`, Day 71)
is **home-only by design**, so today's winter hearth lands in no permanent
capture — the four-season firebox exists only in this session's `/tmp`
strips, which is exactly the "see your own work" problem the Day-71 gallery
was built for, one view over. The season rail would need an inside variant
(or a `view` field per gallery state) to hold it. The founder flagged
repo-growth as the reason the gallery is home-only + single-width, so this
is a trade, not an oversight — but Days 82, 83 and 88 have now all put
season-gated work indoors where the record cannot reach it.

Note for the log's own record: `diary/2026-08-04.md` was created early (the
runbook's Step-2.8 pondering pre-draft) and so appeared in `git add -A` for
the code commit; it was unstaged with `git restore --staged` so the two
commits stay clean. Worth knowing — the pre-draft step and `add -A` interact.

## Files touched

- `inside/inside.css` — the hearth fire's season block (autumn/spring colour
  lean, winter mass + heat); `.hearth__embers` / `.hearth__log` /
  `.hearth__fire--{base,flame,tip}` rebased from hardcoded `* 3` to
  `* var(--s)`, which let their whole 2x override block be deleted from the
  `@media (max-width: 599px)` section (one season block now covers both
  scales); `--ember-glow` custom property added for the coal bed's hot edge;
  `.scene.scene--inside::before { content: none; }` added beside the Day-46
  `::after` twin to stop scene.css's whole-frame season wash leaking indoors.
- `CLAUDE.md` — new "Things I've learned" entry: the inside shell loads
  scene.css and its scene element carries both `.scene` and `.scene--inside`,
  so unscoped `.scene[data-*]` rules apply in the room; the season wash had
  been leaking since Day 69 while three places in the record said it didn't.
  Plus the `var(--s)`-is-the-only-scale-knob note for the firebox.
- `diary/2026-08-04.md` (new), `logs/2026-08-04.md` (this file).

Geometry verified by computed style at both scales — winter is the only
season that moves anything, and it tops out at the same y=25 native as
summer (tip flush under the lintel), so the firebox is unchanged at every
season:

```
375px summer base 20x6 @32 | flame 12x6 @18,38 | tip 6x6 @21,44
375px autumn base 20x6 @32 | flame 12x6 @18,38 | tip 6x6 @21,44
375px winter base 20x8 @32 | flame 16x6 @16,40 | tip 8x4 @20,46
375px spring base 20x6 @32 | flame 12x6 @18,38 | tip 6x6 @21,44
desktop --s = 3 | season = summer
  embers 30px x 6px @21,36
  log    30px x 6px @21,42
  base   30px x 9px @21,48
  flame  18px x 9px @27,57
  tip     9px x 9px @31,66
```

The desktop numbers are byte-for-byte the pre-change values (10x2 embers at
native 7,12; 10x3 base at 7,16; 6x3 flame at 9,19; 3x3 tip at 10⅓,22), which
is the no-regression check for the `var(--s)` rebase.

## Verification output

```
wait-for-deploy: waiting for previews/2026-08-04-243a946.png on origin/main
wait-for-deploy: deadline in 5 minutes; polling every 20s
wait-for-deploy: not yet; 300s remaining
wait-for-deploy: not yet; 280s remaining
wait-for-deploy: not yet; 259s remaining
wait-for-deploy: not yet; 239s remaining
wait-for-deploy: not yet; 219s remaining
wait-for-deploy: not yet; 198s remaining
wait-for-deploy: not yet; 178s remaining
wait-for-deploy: not yet; 158s remaining
wait-for-deploy: previews/2026-08-04-243a946.png found in origin/main
wait-for-deploy: OK
```

CI bot commit `f69bad9 ci: deploy preview for 243a946`; 19 PNGs written (8
view + 7 state + 3 motion + the unsuffixed home shot). The deployed
`previews/2026-08-04-243a946-inside.png` shows the room exactly as it stood
yesterday — correct, since it is August and summer carries no rule.

Local render, three disposable Playwright scripts in `/tmp/` (never
committed): `test-hearth-year.js` (inside view forced through all four
seasons at 375 and 390, composited into one strip via the Day-73 in-browser
canvas trick), `test-hearth-zoom.js` (the firebox alone at 6x, four seasons
side by side), `test-live.js` (all three views unforced at phone and desktop
widths, plus the computed-style dump above). All three exited 0.

```
snapshot: using pre-staged Chromium at /opt/pw-browsers/chromium_headless_shell-1194/chrome-linux/headless_shell
snapshot: starting local server on port 8765
snapshot: running test script /tmp/test-live.js
desktop --s = 3 | season = summer
test: PASS
snapshot: done
```
