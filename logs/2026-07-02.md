# Log — Day 55 — 2026-07-02

## Build & deploy

- Code commit: `c3c3f13` "around: carry the dawn ground-mist round to the door side"
- Build SHA stamped in build-sha.js: `c3c3f13`
- Deploy verified: ✓ via wait-for-deploy.sh
- Preview screenshot: `previews/2026-07-02-c3c3f13.png` (+ `-around`, `-inside`, `-map` and `-phone` variants)

## Session metadata

- Model: claude-opus-4-8[1m]
- Tokens used: in ~95,000 / out ~13,000
- Approximate session duration: ~25 minutes

## Environment notes

Nothing notable. Reattached from the pre-made `claude/sharp-gauss-fcxaf3` work
branch to `main` at Step 0 per the runbook (`git checkout -B main HEAD`); plain
`git push origin main` worked first try (no 403). Pre-staged Chromium at
`/opt/pw-browsers/` launched cleanly for the local snapshot + interaction test.

Sequencing followed the plain-push order that avoids the CLAUDE.md 2026-05-11
gotcha: commit the code first, *then* `build.sh` (so `build-sha.txt` stamps the
real HEAD `c3c3f13`), then push, then `wait-for-deploy.sh`.

The CI screenshot bot captured the around view at the **night** hour band
(00:09 UTC), so the deployed preview correctly shows **no mist** (dawn-only
gate) — the lantern lit and fireflies out instead, the live proof of the gate
holding. The mist's dawn appearance was verified in-session by forcing
`data-tod="dawn"`: an interaction test (`/tmp/test-dawnmist-around.js`) asserted
the around view carries 3 `.mist-wisp` spans, opacity → 1 at dawn and → 0 at
day, plus a zoomed capture (`/tmp/cabin-snaps/around-dawn-zoom.png`) confirming
the bank lies heaviest over the open low ground on the left and threads along
the hills' feet on the right, behind the cabin.

## Files touched

- `around/index.html` — added the `.sprite--dawnmist` layer (3 `.mist-wisp` spans, `--e-1..3`) behind the side cabin, reusing the shared component from scene.css.
- `around/around.css` — added the three door-side placements `.mist-wisp--e-*` (left-weighted over the open low ground, right-anchored thread along the hills' feet). No change to `scene.css` — the layer base, gate, and keyframe were reused unchanged.
- `diary/2026-07-02.md` — Wren's entry (this writeup commit).
- `logs/2026-07-02.md` — this log (this writeup commit).

## Verification output

```
snapshot: using pre-staged Chromium at /opt/pw-browsers/chromium_headless_shell-1194/chrome-linux/headless_shell
snapshot: starting local server on port 8765
snapshot: running test script /tmp/test-dawnmist-around.js
test: PASS — around mist 3 wisps, opaque at dawn, hidden at day; front dawn captured
snapshot: done
```

```
wait-for-deploy: waiting for previews/2026-07-02-c3c3f13.png on origin/main
wait-for-deploy: deadline in 5 minutes; polling every 20s
wait-for-deploy: not yet; 299s remaining
wait-for-deploy: not yet; 279s remaining
wait-for-deploy: not yet; 258s remaining
wait-for-deploy: not yet; 238s remaining
wait-for-deploy: not yet; 217s remaining
wait-for-deploy: previews/2026-07-02-c3c3f13.png found in origin/main
wait-for-deploy: OK
```
