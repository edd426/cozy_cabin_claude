# Log — Day 91 — 2026-08-07

## Build & deploy

- Code commit: 1bc816c "inside: the candle lights the shelf it stands on — a glow on the brick, and lit sides on the stone and the jar"
- Build SHA stamped in build-sha.js: 1bc816c
- Deploy verified: ✓ via wait-for-deploy.sh
- Preview screenshot: previews/2026-08-07-1bc816c.png

## Session metadata

- Model: claude-opus-5[1m] (from .claude/settings.json; effortLevel max)
- Tokens used: in ~470,000 / out ~34,000
- Approximate session duration: ~70 min

## Environment notes

Clean day. Sandbox dropped the session on `claude/eloquent-ride-5oyi4e` (the
routine outcomes pin); `git fetch origin main` then `git checkout -B main` at
the fetched tip reattached. The stale-`origin/main`-on-start behaviour is now
four days running (Days 88–91) and is plainly the steady state, not a fault.
Plain `git push` worked first try (no 403). Playwright launched against the
pre-staged Chromium at
`/opt/pw-browsers/chromium_headless_shell-1194/chrome-linux/headless_shell`.
Yesterday's writeup (0a8513a) is confirmed deployed — `de71bec ci: deploy
preview for 0a8513a` is in the log.

Fourth day running that the runbook's Step-2.8 pondering pre-draft collides
with Step 5's `git add -A`. Days 88, 89 and 90 all logged it; Day 90's log
asked the founder for the one-line runbook fix. This session simply used
`git add -A ':!diary/' ':!logs/'` — the exact form Day 90 proposed — and it
worked cleanly with no `git restore --staged` follow-up needed, so the
suggested fix is confirmed good if the founder wants to land it in
`.claude/commands/daily.md` (locked to the agent, RULES Art I).

Dry-running `--gallery` locally needs one thing the runbook doesn't mention:
`scripts/screenshot.js` launches its own browser, and the repo's pinned
Playwright resolves to Chromium build **1148** while the pre-staged binary is
**1194**, so a bare `node scripts/screenshot.js --gallery …` dies with
"Executable doesn't exist … chromium_headless_shell-1148". screenshot.js
honours `COZY_CABIN_CHROMIUM_PATH`, so exporting it (plus `python3 -m
http.server` to serve the tree) runs the whole 15-state gallery locally.
Recorded in CLAUDE.md.

`messages/open/2026-07-26-a-correspondent-for-the-box.md` was read and left in
`open/` untouched, per the instruction at the top of the file (the founder
moves it himself); it was answered in full on Day 80 and carries no pending
ask.

## Files touched

- `inside/inside.css` — the day's substance.
  - New `.candle-light` block: a `radial-gradient(ellipse closest-side …)` glow
    on the brick behind the flame, 30×22 native, `left: 11.5px * --s` (the
    flame's centre column) with `bottom` keyed to
    `calc((49px + var(--candle-h) * 1px + 2px) * var(--s))` and
    `translate(-50%, 50%)`, so it stays centred on the flame and SINKS as the
    wax spends. Breathes on the candle's own 1.7s period, not the fire's 2.1s.
  - `--candle-h` **hoisted** from `.mantle-item--candle` to `.hearth` so
    `.candle-light` can read it; the two `data-tod` overrides moved to
    `.scene--inside[data-tod=…] .hearth`. Candle/wick/flame still inherit it,
    so their behaviour is byte-identical.
  - `.mantle-item--stone` re-shaded: the lit `#d4a373` cells moved from the
    crown row to the flame-facing diagonal (crown x2 + waist x2, right side);
    everything else is body `#c19465`.
  - `.mantle-item--jar` gains a lit left flank (`--c-wood-light` inset) and is
    rebased from hardcoded `* 3` to `var(--s)`; `.jar__sprig` and both leaf
    pseudo-elements rebased likewise. Their entire 2x block is retired
    (−28 lines) — the Day-77 candle / Day-88 fire / Day-90 chair move.
  - `.mantle-item--candle`: shading moved from a right-edge inset to a
    bottom-row inset. Same one native px of `--c-cream-soft`; the axis was the
    bug (a light source has no shadowed side).
- `inside/index.html` — Day-91 paragraph in the header comment (why the shelf
  gets a glow and lit sides instead of the cast shadows the floor got); the
  `.candle-light` div added before `.hearth__mantle` so the shelf and its
  objects paint in front of the glow; `.hearth` `role="img"` label extended to
  describe the glow, its sinking, and the two facing lit flanks.
- `scripts/screenshot.js` (mutable carve-out, RULES Art I) — `GALLERY_STATES`
  gains `inside-dawn` / `inside-dusk` / `inside-night`. Day 89's comment
  claiming an indoor hour rail "would be four identical pictures" was already
  false when written: the room turns back the hour *wash* (Day 46) and the
  season wash (Day 88) at its door, but the candle's wax (Day 77), the sprig's
  lean (Day 78) and the window glass (Day 46) are all `data-tod`-keyed. Comment
  corrected in place. Plain `day` is deliberately not captured — it is exactly
  the existing `-state-inside-summer` frame. **No `pages.yml` change needed.**
- `CLAUDE.md` — new "Things I've learned" entry: the surface-with-width rule for
  cast shadows (and the projection arithmetic that shows the candle's would
  overrun the shelf), the draw-only-the-lit-side rule at 4–5 native px and why
  `--c-wood` specifically eats a silhouette, the `--candle-h` hoist, the
  hour-vs-year knob difference between `.candle-light` and `.hearth-light`, the
  jar/sprig `--s` rebase (and the note that only `.coat__cloak` and the
  window/sill group still carry `* 3`), the exempt-from-the-wash ≠
  exempt-from-the-hour catch, and the local `--gallery` Chromium-build gotcha.
- `diary/2026-08-07.md` (new), `logs/2026-08-07.md` (this file).

Net: `inside/inside.css` +180/−85 despite the new feature, because the jar and
sprig 2x block went away with the rebase.

## Verification output

```
wait-for-deploy: waiting for previews/2026-08-07-1bc816c.png on origin/main
wait-for-deploy: deadline in 10 minutes; polling every 20s
wait-for-deploy: not yet; 599s remaining
wait-for-deploy: not yet; 579s remaining
wait-for-deploy: not yet; 558s remaining
wait-for-deploy: not yet; 537s remaining
wait-for-deploy: not yet; 517s remaining
wait-for-deploy: not yet; 496s remaining
wait-for-deploy: not yet; 475s remaining
wait-for-deploy: not yet; 455s remaining
wait-for-deploy: previews/2026-08-07-1bc816c.png found in origin/main
wait-for-deploy: OK
```

CI bot commit `43d2fac ci: deploy preview for 1bc816c`; **26 PNGs** written —
8 view + 15 state + 3 motion, up from 23 (the three new `-state-inside-<hour>`
frames). The frames to read for today's change are the pair that only now
exists: `-state-inside-dawn.png` (candle full, glow high on the brick, sprig at
its lean, window glass rose) against `-state-inside-night.png` (candle at half,
glow slid a third of the way down the wall, sprig upright at rest, glass
dusk-blue). The live `-inside.png` is captured at ~00:20 UTC, which sky.js bins
as `night`, so it shows the low end of that range and always did — the *other*
end of it had never been photographed.

Local verification, four disposable Playwright scripts in `/tmp/` (never
committed):

- `test-mantle-zoom.js` — 8× crops centred on each mantle object, per hour, for
  the eye. This is what caught the first pass's real defect: the away-side
  shade I gave the stone and jar was `--c-wood`, the plank wall's own colour,
  so each object's unlit half vanished into the boards behind it. Numbers
  could not have caught it; only the magnified render did.
- `test-mantle-band.js` — the whole shelf at 4×, per width, to judge whether
  the glow was too big before committing to a size (it went 22→30 native so it
  would actually reach the two objects it is said to light).
- `test-inside-full.js` — the unmagnified room at 375 and 390 px, day/night and
  summer/winter, confirming the glow reads at real phone size and that Day 88's
  deeper winter fire, Day 89's pool and Day 90's cast shadows are all intact.
- `test-mantle-light.js` — 30 assertions, run at both layout scales:

```
--- viewport 375px (expect --s = 2) ---   [30/30 ok]
--- viewport 900px (expect --s = 3) ---   [30/30 ok]
test: PASS
```

Covering: the glow exists and is exactly 30×22 native at each scale; it paints
a `radial-gradient` with `closest-side` (i.e. the rule matched and the Day-89
hard-edge trap is avoided); it sits at `z-index: 1`, under the mantle and its
objects; it **sinks** day → dusk → night by exactly 3 native and **never
changes size**; it stays centred on the flame at all three hours; it is
unchanged between summer and winter (it answers the hour, not the year); the
stone has exactly 4 lit cells and no plank-wall shade; the jar has a lit lip
plus a lit left flank and no shade; the candle's inset is on the bottom axis,
not the right; the jar box and sprig stem measure exactly 4×4 and 1×5 native
at *both* scales (the var(--s) rebase, proving the retired 2x block is not
missed); and the glow never escapes the scene frame.

Gallery mode was also dry-run end-to-end against a local `python3 -m
http.server` before pushing — all 15 states captured, the three new inside-hour
frames among them — so the CI step was not the first place the new entries ran.

Also confirmed unchanged: `./scripts/local-snapshot.sh` on the home view
renders identically (no `scene.css` / `scene.html` edits today).
