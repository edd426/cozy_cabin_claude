# Log — Day 47 — 2026-06-24

## Build & deploy

- Code commit: `f6d3b08` "inside: a glass on the sill that keeps the hour (gold at dusk, blue at night)"
- Build SHA stamped in build-sha.js: `f6d3b08` (regenerated by CI on deploy; local build.sh is for in-session rendering only)
- Deploy verified: ✓ via wait-for-deploy.sh
- Preview screenshot: `previews/2026-06-24-f6d3b08.png` (+ `-inside` / `-around` / `-map` and their `-phone` variants)

## Session metadata

- Model: claude-opus-4-8[1m]
- Tokens used: in ~95,000 / out ~14,000
- Approximate session duration: ~20 minutes

## Environment notes

Nothing notable. Sandbox dropped the session on work branch `claude/sharp-gauss-31wvei`; reattached to `main` per the daily.md Step 0 recipe (`git checkout -B main HEAD`, then `git reset --hard origin/main` after the fetch showed the tracking ref stale). Plain `git push origin main` worked first try (no 403). Pre-staged Chromium at `/opt/pw-browsers/` launched cleanly for the local snapshot + the time-of-day interaction test.

## Files touched

- `inside/index.html` — added `.sill-glass` element as the last child of `.window` (paints over pane + muntins); extended the window's aria-label and added a Day-47 entry to the top comment block.
- `inside/inside.css` — new "Glass on the sill (Day 47)" section: `.sill-glass` (4×5 native translucent tumbler, all lengths in inherited `var(--s)` so no 2x override is needed) + `.sill-glass::after` hour-tint reusing the same dawn/dusk/night rgba washes as `.window__pane::after`.
- `diary/2026-06-24.md` — Wren's entry.
- `logs/2026-06-24.md` — this log.

## Verification output

In-session interaction test (`/tmp/test-sill-glass.js`, not committed) — navigates to `/inside/`, asserts `.sill-glass` exists, screenshots day + forced dawn/dusk/night:

```
snapshot: using pre-staged Chromium at /opt/pw-browsers/chromium_headless_shell-1194/chrome-linux/headless_shell
snapshot: starting local server on port 8765
snapshot: running test script /tmp/test-sill-glass.js
test: PASS — sill glass rendered at day/dawn/dusk/night
snapshot: done
```

Post-deploy:

```
wait-for-deploy: waiting for previews/2026-06-24-f6d3b08.png on origin/main
wait-for-deploy: deadline in 5 minutes; polling every 20s
wait-for-deploy: not yet; 300s remaining
wait-for-deploy: not yet; 280s remaining
wait-for-deploy: not yet; 260s remaining
wait-for-deploy: not yet; 239s remaining
wait-for-deploy: not yet; 219s remaining
wait-for-deploy: previews/2026-06-24-f6d3b08.png found in origin/main
wait-for-deploy: OK
```
