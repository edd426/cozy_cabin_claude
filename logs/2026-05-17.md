# Log — Day 9 — 2026-05-17

## Build & deploy

- Code commits (two in this session):
  - `9b7c9bb` "messages: read-and-close ambition-reform FYI" — the first-commit-of-session move per the read-and-close pattern; no code touched.
  - `81399c1` "path: angle reconciliation, side commit, more stones (Day 9)" — the substantive contribution: front-view stones 3→5, around-view stones re-angled leftward, side-vs-back lexical commit in all three relevant file headers (`scene.css`, `around/around.css`, `around/index.html`), and a Wren's notes block on `messages/open/2026-05-11-door-or-window.md`.
- Build SHA stamped in build-sha.js (deployed): `81399c1`. (Local `build-sha.js` had `9b7c9bb` at the time of the first home-view snapshot because `./scripts/build.sh` was run after the read-and-close commit but before the path-rework commit — this didn't affect what CI deployed, since `build-sha.js` is gitignored and CI rebuilds it on deploy. Sequence for future agents: run `build.sh` *after* the final code commit, not between commits.)
- Deploy verified: ✓ via `wait-for-deploy.sh`.
- Preview screenshot: `previews/2026-05-17-81399c1.png`.

## Session metadata

- Model: claude-opus-4-7
- Tokens used: in ~145,000 / out ~14,000 (rough — full read of RULES + CLAUDE + MILESTONES + daily.md + all four open messages including the 2026-05-16 follow-up + the diary/log/meta schemas + Day 0/1/2/3/4/5/7/8 diary + the Day 7 meta + Day 8 log + the latest preview + scene.html + scene.css + around/index.html + around/around.css + the two snapshot cycles + the door-or-window message's full back-and-forth + this writeup).
- Approximate session duration: ~40 min.

## Environment notes

- **`git push` worked first try again.** Second consecutive day of clean push to the local proxy (Day 8 was the first). The four-session 403 pattern (Days 2/3/4/5/7) hasn't returned. Still keeping the `mcp__github__push_files` fallback noted in CLAUDE.md — two clean sessions isn't enough to retire the playbook, but if Day 10+ also pushes cleanly, the CLAUDE.md note can be softened.
- `build.sh` was run mid-session (after the read-and-close commit, before the path-rework commit), which left local `build-sha.js` pointing at the wrong SHA. This is the same kind of sequencing miss the MCP-era CLAUDE.md note warns about, just in a regular-push session. Recorded above under Build & deploy. Not blocking.
- Playwright launched cleanly via the pre-staged Chromium at `/opt/pw-browsers/chromium_headless_shell-1194/chrome-linux/headless_shell` for both the home-view snapshot and the `/tmp/snap-around.js` around-view snapshot. No npm or browser-fetch network needed.

## Files touched

- `scene.html` — added two new `<span class="path-stone path-stone--{4,5}">` elements after the existing three. The class numbering was reassigned in the CSS so that `--1` through `--5` runs back→front along the diagonal trajectory: `--1` (back, occluded), `--2` (between back and mid, new), `--3` (mid, was `--2`), `--4` (between mid and front, new), `--5` (front, was `--3`). Two new HTML spans; five CSS rules touched.
- `scene.css` — `.path-stone--1` through `.path-stone--5` rewritten as a five-stone diagonal; mobile-narrow override extended to all five; file-header comment block rewritten to describe the new cross-view geometry (Article XIII) and the same-path-from-different-angles relationship to the around view.
- `around/around.css` — `.around-path__stone--{back,mid,front}` re-positioned (mid shifted ~16px left of centre, front shifted ~34px left) so the path arrives at the door from the lower-left rather than head-on. File-header comment block rewritten to commit explicitly to "side" (right/east face), describe the cross-view geometry, and explain why the head-on Day-7 layout was incompatible.
- `around/index.html` — file-header comment extended with a Day-9 line covering the re-angle and the side-vs-back commit.
- `messages/open/2026-05-11-door-or-window.md` — appended a `### Wren's notes — 2026-05-17 (Day 9)` subsection covering all three pieces of the 2026-05-16 follow-up. File stays in `open/` per the previous Wren's notes and the founder's note that closure waits on the screenshot-all-views work.
- `messages/done/2026-05-16-ambition-reform.md` — moved from `open/` to `done/` as the first commit of the session per the read-and-close pattern.
- `diary/2026-05-17.md` — new; today's Wren entry under the post-reform four-section schema. Pondering section drafted at Step 2.7, the rest after the local snapshots confirmed the geometry read correctly.
- `logs/2026-05-17.md` — new; this file.

## Verification output

```
wait-for-deploy: waiting for previews/2026-05-17-81399c1.png on origin/main
wait-for-deploy: deadline in 5 minutes; polling every 20s
wait-for-deploy: not yet; 300s remaining
wait-for-deploy: not yet; 280s remaining
wait-for-deploy: not yet; 259s remaining
wait-for-deploy: previews/2026-05-17-81399c1.png found in origin/main
wait-for-deploy: OK
```

The deployed preview at `previews/2026-05-17-81399c1.png` shows the front view with the five-stone diagonal path: back stone (small, poking from cabin's right edge at mid-height), an intermediate stone just past the cabin's right edge, mid stone on the grass strip, a second intermediate stone closer to the foreground, and the front stone partly off-frame at the bottom-right. Smoke + brick-window + two trees unchanged. Day-9 label and SHA `81399c1` render correctly. The around view (not captured by CI per `messages/open/2026-05-13-screenshot-all-views.md`) was verified locally via `/tmp/snap-around.js`: stones angle in from the lower-left of the frame, no longer head-on.
