# Log — Day 90 — 2026-08-06

## Build & deploy

- Code commit: 557ffb0 "inside: the fire's light throws shadows — the chair's left, the woodpile's right, longer in the cold"
- Build SHA stamped in build-sha.js: 557ffb0
- Deploy verified: ✓ via wait-for-deploy.sh
- Preview screenshot: previews/2026-08-06-557ffb0.png

## Session metadata

- Model: claude-opus-5[1m] (from .claude/settings.json; effortLevel max)
- Tokens used: in ~430,000 / out ~26,000
- Approximate session duration: ~55 min

## Environment notes

Clean day. Sandbox dropped the session on `claude/eloquent-ride-m98h3c` (the
routine outcomes pin, as expected); `git checkout -B main HEAD` reattached, and
`git fetch origin main` was needed first for the tracking ref — the same stale
`origin/main` behaviour Days 88 and 89 both logged, now three days running and
plainly the steady state rather than a one-off. Plain `git push` worked first
try (no 403). Playwright launched against the pre-staged Chromium at
`/opt/pw-browsers/chromium_headless_shell-1194/chrome-linux/headless_shell`.
Yesterday's writeup (9342472) is confirmed deployed — `2b2dbbf ci: deploy
preview for 9342472` is in the log.

Third day in a row that the runbook's Step-2.8 pondering pre-draft collided
with `git add -A`: `diary/2026-08-06.md` existed before the code commit and was
staged by `add -A`, then unstaged with `git restore --staged` so the two commits
stay clean. Days 88 and 89 both logged the same interaction and Day 89's log
said it would be "worth a runbook note if a third day hits it" — this is the
third. `.claude/commands/daily.md` is locked to the agent (RULES Art I), so
flagging it here for the founder rather than editing: **Step 5's `git add -A`
should be `git add -A ':!diary/' ':!logs/'`, or the step should name the
`git restore --staged` follow-up explicitly.** It is a two-second fix each time
and has never actually shipped a bad commit, so this is a papercut note, not a
defect.

## Files touched

- `inside/inside.css` — new "Cast shadows on the boards" block: `--cast` /
  `--cast-o` on `.chair` and `.woodpile`, a shared `::before` (`bottom:
  calc(-2px * var(--s))`, `height: 2px * --s`, `width: --cast * --s`) and two
  side-specific `linear-gradient`s, plus autumn/spring and winter rules keyed to
  `.scene--inside[data-season]`. Separately, `.chair` and its four children were
  rebased from hardcoded `* 3` to `* var(--s)` (the Day-77 candle / Day-88 fire
  move), collapsing the 2x chair override from 26 lines to `--s: 2` + the
  centre-relative `right:` anchor — a net **−9 lines** in the file despite the
  new feature.
- `inside/index.html` — Day-90 paragraph in the header comment (why cast shadows
  are allowed indoors and refused outdoors); `.chair` and `.woodpile`
  `role="img"` labels extended to name their shadows and the opposite
  directions.
- `CLAUDE.md` — new "Things I've learned" entry: the drawn-vs-offstage light
  rule, derived direction + the parallel-vs-radiating tell, shadow length tied
  to the Day-89 pool's own season table, the `::after` contact / `::before` cast
  split, the `z-index: 2` stacking-context reason the cast paints over the
  `z-index: 1` pool, the woodpile's open-corner octagon gotcha, and the `.chair`
  `--s` rebase.
- `diary/2026-08-06.md` (new), `logs/2026-08-06.md` (this file).

## Verification output

```
wait-for-deploy: waiting for previews/2026-08-06-557ffb0.png on origin/main
wait-for-deploy: deadline in 10 minutes; polling every 20s
wait-for-deploy: not yet; 599s remaining
wait-for-deploy: not yet; 579s remaining
wait-for-deploy: not yet; 558s remaining
wait-for-deploy: not yet; 538s remaining
wait-for-deploy: not yet; 517s remaining
wait-for-deploy: not yet; 497s remaining
wait-for-deploy: not yet; 476s remaining
wait-for-deploy: not yet; 456s remaining
wait-for-deploy: previews/2026-08-06-557ffb0.png found in origin/main
wait-for-deploy: OK
```

CI bot commit `ab5964b ci: deploy preview for 557ffb0`; **23 PNGs** written —
8 view + 12 state + 3 motion, the same set as Day 89. (Day 89's log put the
state count at 11 and the total at 22; the actual `ls` is 12 and 23 —
`-state-winter-night.png`, added Day 84, was left out of that tally. Noting it
here rather than editing a committed log.) No new capture states were needed
today, because the four
`-state-inside-<season>` frames added yesterday are exactly where this change
lives. The deployed `-state-inside-winter.png` is the frame to read: it shows
both smears at their longest against the deep winter fire and its wide pool.
`-state-inside-summer.png` shows them at their shortest, barely clearing each
object. The live `-inside.png` (captured at night, in summer) shows the summer
pair, correct — the room turns the hour back at its door, so both the pool and
the shadows it casts answer the fire and the year only.

Local verification, two disposable Playwright scripts in `/tmp/` (never
committed): `test-cast-zoom.js` (4× magnified crops of the floor band per
season, for the eye) and `test-cast-shadows.js`, which asserts at both 375 and
390 px, across all four seasons:

1. both `::before` smears exist with non-zero width and a `linear-gradient`
   background — i.e. the rule is actually matching, not silently dropped;
2. direction is opposite and correct: the chair's gradient runs `to left`, the
   woodpile's `to right`;
3. neither smear overflows the scene frame at its winter longest, at either
   width;
4. winter's cast is strictly longer than summer's;
5. computed widths are exactly native × `--s` — 16 / 22 / 30 px at 2x (8 / 11 /
   15 native), confirming the season table lands identically at both scales from
   the one block.

```
snapshot: using pre-staged Chromium at /opt/pw-browsers/chromium_headless_shell-1194/chrome-linux/headless_shell
snapshot: starting local server on port 8765
snapshot: running test script /tmp/test-cast-shadows.js
375px/summer: cast 16px / 16px, opacity 0.75
375px/autumn: cast 22px / 22px, opacity 0.88
375px/winter: cast 30px / 30px, opacity 1
375px/spring: cast 22px / 22px, opacity 0.88
390px/summer: cast 16px / 16px, opacity 0.75
390px/autumn: cast 22px / 22px, opacity 0.88
390px/winter: cast 30px / 30px, opacity 1
390px/spring: cast 22px / 22px, opacity 0.88
test: PASS
snapshot: done
```

The first magnified pass caught one thing the numbers could not: the woodpile's
smear anchored at `left: 100%` left a visible one-native gap between the bark
and the shadow, because the log ends are octagons drawn with their corners left
open — the element's box is a pixel wider than the wood that actually meets the
floor. Re-anchored to `left: calc(7px * var(--s))` and re-rendered clean.

`messages/open/2026-07-26-a-correspondent-for-the-box.md` was read and left in
`open/` untouched, per the instruction at the top of the file (the founder moves
it himself); it was answered in full on Day 80 and carries no pending ask.
