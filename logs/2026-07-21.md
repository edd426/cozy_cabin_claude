# Log — Day 74 — 2026-07-21

## Build & deploy

- Code commit: `858234f` "diary archive: Day numbers on every card, distinct meta cards"
- Build SHA stamped in build-sha.js: `858234f`
- Deploy verified: ✓ via wait-for-deploy.sh
- Preview screenshot: `previews/2026-07-21-858234f.png`
- Writeup commit: (this commit) — `diary/2026-07-21.md` + `logs/2026-07-21.md`
- Action-ask closed: `messages/done/2026-07-17-day-numbers-and-metas-on-the-shelf.md` (moved in the code commit with Completion notes)

## Session metadata

- Model: `claude-opus-4-8[1m]` (from `.claude/settings.json`; effort `max`)
- Tokens used: in ~95,000 / out ~15,000
- Approximate session duration: ~30 min

## Environment notes

Clean day. Sandbox dropped the session on `claude/intelligent-lovelace-hwxsma`;
Step 0 reattached to `main` (`git checkout -B main HEAD`) and reset to
`origin/main`; plain `git push origin main` worked first try (no 403). Pre-staged
Chromium at `/opt/pw-browsers/` detected by `local-snapshot.sh`; three throwaway
Playwright scripts (`/tmp/diary-shot.js`, `/tmp/diary-bottom.js`,
`/tmp/diary-dark.js`) ran cleanly to verify the archive at 375/390 and in dark
mode. `diary/manifest.json` is gitignored (regenerated by `build.sh` in-session
and by CI at deploy), so it is not staged — the new render reads the same fields
CI writes.

The worker process was restarted once while `wait-for-deploy.sh` was polling in
the background; no harm — the code commit had already pushed, CI committed
`18f7756 ci: deploy preview for 858234f`, and the poll's own output
(`/tmp/wait-out.txt`) had captured the `OK` before the restart, so verification
was recovered intact. Writeup was assembled after the restart.

## Files touched

- `diary/diary.js` — render cards from manifest `day`/`date`/`kind` fields; new
  `dayLabel`/`dateOf`/`descOf` helpers; same-date tiebreak (daily above its
  meta); meta cards get badge + out-of-voice note.
- `diary/diary.css` — replaced `.diary-list__date`/`.diary-list__title` with
  `.entry__head`/`.entry__day`/`.entry__date`/`.entry__desc`; added meta card
  treatment (`.diary-list__entry--meta`, `.entry__badge`, `.entry__note`);
  dark-mode overrides for all of the above.
- `diary/index.html` — blurb gains one sentence naming the plum meta cards
  (shell contract — build-sha tag, `build-sha.js`, `#day-label`/
  `#build-sha-label` — left intact).
- `messages/open/... → messages/done/...` — action-ask closed with Completion
  notes.
- `diary/2026-07-21.md`, `logs/2026-07-21.md` — this writeup.

## Verification output

```
wait-for-deploy: waiting for previews/2026-07-21-858234f.png on origin/main
wait-for-deploy: deadline in 5 minutes; polling every 20s
wait-for-deploy: not yet; 300s remaining
wait-for-deploy: not yet; 279s remaining
wait-for-deploy: not yet; 259s remaining
wait-for-deploy: not yet; 239s remaining
wait-for-deploy: not yet; 218s remaining
wait-for-deploy: not yet; 198s remaining
wait-for-deploy: previews/2026-07-21-858234f.png found in origin/main
wait-for-deploy: OK
```

Local archive verification (working tree, before push) — `/tmp/diary-shot.js`
at 375 and 390, `/tmp/diary-bottom.js`, `/tmp/diary-dark.js`:

```
w=375: firstDay="Day 74" metaCount=11 metaBadge="week in review" firstCardISOdates=1
w=390: firstDay="Day 74" metaCount=11 metaBadge="week in review" firstCardISOdates=1
diary-shot: PASS
last card: "Day 0 | the scaffold"  |  0000-00-00 shown=0
diary-bottom: PASS
diary-dark: PASS
```
