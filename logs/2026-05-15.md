# Log — Day 7 — 2026-05-15

## Build & deploy

- Code commits (split across two MCP pushes since `mcp__github__push_files`
  produces one commit per call):
  - `a29dd8c` "around: lay three stepping stones approaching the door (Day 7)"
    — `around/index.html` + `around/around.css`.
  - `87daaee` "messages: Wren's notes (Day 7) on door-or-window; pushback
    on screenshot-all-views" — the two `messages/open/*.md` updates.
- Build SHA stamped in build-sha.js: `87daaee` (re-run after the second
  MCP push so build-sha.txt matches origin/main HEAD).
- Deploy verified: ✓ via `wait-for-deploy.sh` (preview for `87daaee`).
- Preview screenshot: `previews/2026-05-15-87daaee.png`. Note: CI also
  produced `previews/2026-05-15-a29dd8c.png` for the first commit; both
  are visually identical because the home view is unchanged and CI only
  captures the home view (see `messages/open/2026-05-13-screenshot-all-views.md`
  and Wren's pushback added today).

## Session metadata

- Model: claude-opus-4-7
- Tokens used: in ~110,000 / out ~28,000 (rough — full read of RULES +
  CLAUDE + MILESTONES + daily.md + four open messages + two done messages
  + diary/log/meta schemas + Day 0/1/2/3/4/5 diary + Day 4/5 logs + the
  latest preview + scene/around/theme/index files + scripts/screenshot.js
  + .github/workflows/pages.yml + the path iteration cycle + two MCP push
  payloads + the writeup + meta-reflection).
- Approximate session duration: ~60 min.

## Environment notes

- `git push` to `origin` returned HTTP 403 as expected (Cloudflare-fronted
  via `api.anthropic.com`; matches the CLAUDE.md Day 2/3/4 notes). Fell
  back to `mcp__github__push_files` for both pushes.
- Two MCP pushes today rather than one: I forgot the message files in
  the first call (built the `files` array with only the two `around/*`
  paths) and only realized after the response came back. Pushed the two
  message files in a second call. Functionally fine — the result is two
  meaningful commits anyway (code change + bookkeeping) — but worth a
  CLAUDE.md note: when constructing an MCP push payload, build the full
  list before sending the call, not from memory.
- After the second push, `git fetch && git reset --hard origin/main`
  realigned local main with the MCP commits (the local-only commits had
  different SHAs and were discarded), then `./scripts/build.sh` was
  re-run so `build-sha.txt` matched HEAD (`87daaee`) before
  `wait-for-deploy.sh` polled.
- Playwright launched cleanly via the pre-staged Chromium at
  `/opt/pw-browsers/chromium_headless_shell-1194/chrome-linux/headless_shell`
  for both the around-page snapshot and the
  `/tmp/test-around-path.js` interaction test (asserts three stones,
  ordered back<mid<front by width, stacked back-above-front by top
  coordinate, door still intact). Test passed on the second iteration;
  the first attempt clustered all three stones at the bottom of the
  scene because I'd anchored them inside a wrapper whose `height: calc(6%
  + 60px)` left the percentage-based `bottom` values reading against ~75px
  of wrapper instead of ~257px of scene. Switching the wrapper to
  `inset: 0` fixed it.
- Day 6 (cb21b98) shipped no diary or log; today's diary acknowledges
  the gap in the "What I've been pondering since yesterday" section per
  `messages/open/2026-05-14-day-6-no-writeup.md`. The message moves to
  `done/` in the writeup commit (the diary acknowledgment is the close
  criterion).

## Files touched

- `around/index.html` — added `.around-path` div with three
  `.around-path__stone` spans; updated the file-header comment to record
  Day 7's addition.
- `around/around.css` — added the `.around-path` and
  `.around-path__stone` styles at the bottom of the file; minor edits to
  the file-header comment.
- `messages/open/2026-05-11-door-or-window.md` — appended a `### Wren's
  notes — 2026-05-15 (Day 7)` subsection. File stays in `open/`; closure
  still gated on the front-view path no longer reading as terminating at
  the window.
- `messages/open/2026-05-13-screenshot-all-views.md` — appended a `###
  Wren's pushback — 2026-05-15 (Day 7)` subsection explaining the lock
  conflict (everything the message asks me to edit lives under
  `scripts/` or `.github/`). File stays in `open/`; awaits the founder's
  response.
- `messages/open/2026-05-14-day-6-no-writeup.md` → `messages/done/`
  (moved in the writeup commit; the diary acknowledgment is the close).
- `diary/2026-05-15.md` — new; today's Wren entry under the post-reform
  four-section schema.
- `diary/meta/2026-05-15.md` — new; weekly meta-reflection (Day 7,
  `day_n % 7 == 0`).
- `logs/2026-05-15.md` — new; this file.

## Verification output

```
wait-for-deploy: waiting for previews/2026-05-15-87daaee.png on origin/main
wait-for-deploy: deadline in 5 minutes; polling every 20s
wait-for-deploy: not yet; 299s remaining
wait-for-deploy: not yet; 279s remaining
wait-for-deploy: not yet; 258s remaining
wait-for-deploy: not yet; 238s remaining
wait-for-deploy: previews/2026-05-15-87daaee.png found in origin/main
wait-for-deploy: OK
```

The deployed preview at `previews/2026-05-15-87daaee.png` shows the
home page from Day 6 unchanged — chimney + two cream smoke puffs +
brick-infill window + two trees + the path stones in the bottom-right
exiting the frame. Day-7 label and SHA `87daaee` render correctly in
the footer. The around view (where today's three new stones actually
landed) is not captured by CI; the local snapshot at
`/tmp/cabin-snaps/around.png` from today's session is the only visual
record of it, until the screenshot-coverage push back is resolved.
