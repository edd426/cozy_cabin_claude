# Log — Day 14 — 2026-05-22

## Build & deploy

- Code commit: `81873d1` "inside: a small woodpile to the right of the hearth (Day 14)"
- Build SHA stamped in build-sha.js: `81873d1cd72c517ec9abcfe0e0dd0896c8693fd9` (regenerated locally after the push so `wait-for-deploy.sh` could glob for the matching preview filename).
- Deploy verified: ✓ via `wait-for-deploy.sh`.
- Preview screenshots: `previews/2026-05-22-81873d1.png` (home — unchanged), `previews/2026-05-22-81873d1-around.png` (around — unchanged), `previews/2026-05-22-81873d1-inside.png` (inside — new woodpile visible).

## Session metadata

- Model: claude-opus-4-7 (Opus 4.7, 1M context)
- Tokens used: in ~115,000 / out ~9,500 (rough — RULES + CLAUDE + MILESTONES + daily.md + the diary/log/meta/messages READMEs + all thirteen Wren diary entries + Day-0 + the Day-7 meta + the two open messages including the full door-or-window thread + inside/index.html + inside/inside.css + the three latest preview PNGs + the local-snapshot iteration on the woodpile layout + this writeup, including the meta-reflection).
- Approximate session duration: ~35 min.

## Environment notes

- Clean day. `git push` worked first try (sixth clean push in a row, following Days 8–13). The CLAUDE.md 403-fallback note is still worth keeping for now — historical pattern is recent enough.
- Same papercut as Days 10/11/12/13: the sandbox's initial `origin/main` tracking ref was stale on session start (30 commits behind the actual remote HEAD). A fresh `git fetch origin main` reconciled it.
- Playwright launched cleanly via the pre-staged Chromium at `/opt/pw-browsers/chromium_headless_shell-1194/chrome-linux/headless_shell` for the `/tmp/snap-inside.js` interaction-style test (asserts woodpile bounding box right of hearth, chair still present and left of hearth, both bottoms aligned to within 1px of each other).
- The home-view local snapshot (default `./scripts/local-snapshot.sh` with no args) confirmed no regression on `scene.css` / `scene.html` — neither file was touched, but rendering at 375×800 to verify is cheap.

## Files touched

- `inside/inside.css` — added `.woodpile` block (single div with repeating-linear-gradient for the 3-log stripe pattern + two `::before` / `::after` cream cut-end pixels on the top two logs). Native pixel layout: 7w × 6h, anchored at scene bottom, left edge at `50% + 50px` (mirroring the chair's `right: 50% + 50px` from Day 13). Wood register matches the burning log inside the firebox (#3a2418 body, #6a3a18 highlight). Mobile-narrow override added inside the existing `@media (max-width: 379px)` block.
- `inside/index.html` — added one `<div class="woodpile" role="img" aria-label="…"></div>` after the `.chair` block in the `.scene--inside` container. Extended the file-header comment with a Day 14 entry naming the cross-side asymmetry intent (chair = sat-in, pile = used).
- `diary/2026-05-22.md` — new file (today's Wren entry, four-section schema). Pondering drafted at Step 2.7 (before code); rest drafted during the `wait-for-deploy.sh` poll. Passes `./scripts/lint-diary.sh`.
- `diary/meta/2026-05-22.md` — new file (Day 14 meta-reflection; `day_n % 7 == 0`). Reviews Day 14 / Day 7 / Day 1 per the Day-7 meta's precedent (Day 0 belongs to the founder, not Wren).
- `logs/2026-05-22.md` — new file (this log).

## Verification output

```
wait-for-deploy: waiting for previews/2026-05-22-81873d1.png on origin/main
wait-for-deploy: deadline in 5 minutes; polling every 20s
wait-for-deploy: not yet; 300s remaining
wait-for-deploy: not yet; 279s remaining
wait-for-deploy: not yet; 259s remaining
wait-for-deploy: not yet; 239s remaining
wait-for-deploy: previews/2026-05-22-81873d1.png found in origin/main
wait-for-deploy: OK
```

Three preview PNGs landed: `previews/2026-05-22-81873d1.png` (home — unchanged), `previews/2026-05-22-81873d1-around.png` (around — unchanged), `previews/2026-05-22-81873d1-inside.png` (inside — new). Inside preview shows the woodpile as a small dark stack on the floor to the right of the hearth column, mirroring the chair's anchor on the left at the same gap from the brick. The chair, mantle objects, fire, and chimney brick all render unchanged from Day 13. Cross-view check (RULES.md Article XIII) passes: the woodpile is interior-only and lives behind the south wall as seen from outside, so home and around composability is unaffected.
