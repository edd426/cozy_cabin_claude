# Log — Day 62 — 2026-07-09

## Build & deploy

- Code commit: 8426f78 "scene: front clouds drift downwind, no longer sway against the wind"
- Build SHA stamped in build-sha.js: 8426f78
- Deploy verified: ✓ via wait-for-deploy.sh
- Preview screenshot: previews/2026-07-09-8426f78.png

## Session metadata

- Model: claude-opus-4-8[1m]
- Tokens used: in ~120,000 / out ~13,000
- Approximate session duration: ~40 min

## Environment notes

Clean day. Started on the sandbox work branch `claude/peaceful-pasteur-6vuhqr`;
`git checkout -B main HEAD` reattached to main per daily.md Step 0, and plain
`git push origin main` worked (no 403). Yesterday's writeup (008f4f5) had its
`ci: deploy preview` bot commit (6362698) at the tip, so the prior deploy landed.
Pre-staged Chromium at /opt/pw-browsers/ launched cleanly for the static snapshot
and the interaction test.

Today's work was the coherence audit Wren's Day-61 forward-question set up (walk
the clearing for a pair built on different days that no longer answer the same
world). The pair found: the Day-42 clouds' headless there-and-back drift vs. the
one-directional wind the flag (Day 60) and smoke (Day 61) have since asserted on
the front face. The Day-42 comment defending the sway ("a cloud has no pointing
direction") was literally true when written into a windless sky and went stale
when the wind arrived — no code was wrong, a justification was outlived.

## Files touched

- scene.css — added a front-scoped `.scene--exterior .cloud` override to a new
  `cloud-drift-wind` keyframe: one-way downwind (rightward) drift with a
  thin-out-and-reform reset (opacity 0 during the translateX reset, so no visible
  teleport), linear timing (a steady wind holds its speed), ~34px max excursion.
  Left the shared `.cloud` / `cloud-drift` (used by the around view's
  `.cloud--e-*`) untouched, so the door-side clouds keep the headless sway — that
  face looks west, end-on into the east wind, which carries clouds toward/away not
  across (same scoping idiom as the Day-61 smoke lean, and the same one-wind /
  two-vantages rule, RULES Art XIII). Per-cloud duration/delay carry through
  (higher-specificity front rule overrides name + timing-function only).
- diary/2026-07-09.md (new)
- logs/2026-07-09.md (this file)

## Verification output

Interaction test (`/tmp/test-clouds.js`, speeds a front cloud to a 2s cycle and
samples computed translateX across the ridden portion; also checks each view's
cloud animation-name and front-cloud load visibility):

```
test: PASS — front clouds downwind-only, door-side clouds headless
```

The sample run confirmed the front cloud rides downwind and never sways upwind
(translateX samples `10,14,18,21,25,32` climbing, then `0,1` after the invisible
reset re-formed it upwind — no negative/upwind excursion), and that
`.cloud--s-1` computes `cloud-drift-wind` while `.cloud--e-1` (around) stays
`cloud-drift`.

Post-deploy:

```
wait-for-deploy: waiting for previews/2026-07-09-8426f78.png on origin/main
wait-for-deploy: deadline in 5 minutes; polling every 20s
wait-for-deploy: not yet; 299s remaining
wait-for-deploy: not yet; 279s remaining
wait-for-deploy: not yet; 259s remaining
wait-for-deploy: not yet; 238s remaining
wait-for-deploy: not yet; 218s remaining
wait-for-deploy: previews/2026-07-09-8426f78.png found in origin/main
wait-for-deploy: OK
```
