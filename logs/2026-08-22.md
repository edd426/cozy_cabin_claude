# Log — Day 106 — 2026-08-22

## Build & deploy

- Code commit: `1af783c` "diary: a reader — the days set in type, and the book of names marked in the page"
- Build SHA stamped in build-sha.js: `1af783c` (re-run after the push, per the CLAUDE.md 2026-05-11 note)
- Deploy verified: ✓ via wait-for-deploy.sh (found on the 11th poll, ~200s)
- Preview screenshot: `previews/2026-08-22-1af783c.png` (+ the new `-entry.png`, 32 PNGs this commit)

## Session metadata

- Model: `claude-opus-5[1m]`
- Tokens used: in ~420,000 / out ~35,000
- Approximate session duration: ~75 minutes

## Environment notes

Nothing notable. Sandbox dropped the session on `claude/eloquent-ride-l9s3gv`;
Step 0's unconditional `git checkout -B main HEAD` reattached, and plain
`git push -u origin main` worked first try (no 403). Pre-staged Chromium found
at `/opt/pw-browsers/chromium_headless_shell-1194/`; `local-snapshot.sh`
launched cleanly. The `--manifest` dry run against a local `python3 -m
http.server` needed the Day-91 `COZY_CABIN_CHROMIUM_PATH` export.

One thing worth flagging for the founder rather than a fix: **the fourth
acceptance criterion of the closed message says entries before 2026-08-21 must
"render exactly as they do now."** Taken literally that forbids rendering them
at all, since "now" meant raw `text/plain`, and it would leave the shelf
opening two different kinds of thing. It was written before either side knew
the entries had no rendered page. I read it as the no-retroactive-*marking*
rule it sits beside — every day renders alike, only days on/after the book's
own `OPENED` date carry marks — and said so in the completion notes so it can
be overruled.

## Files touched

- `diary/entry.html` — new: the reader's shell (contract-bound; build-sha meta + script, `#day-label`, `#build-sha-label`, `#entry-page` as the mount). Loads `../names/names.js` for its data only.
- `diary/entry.js` — new: markdown subset renderer (headings, hard-wrapped paragraphs, `- ` bullets, ``` fences, `---`, and recursive inline `code`/`strong`/`em`), the first-appearance term marking off `window.CabinNames`, the anchored/flipping gloss card, and the older/newer turn.
- `diary/diary.css` — the reader's block (`#entry-page`, `.entry-body`, `.named`, `.gloss`, `.entry-turn`) plus its dark-scheme twin.
- `diary/diary.js` — cards now link `entry.html?d=<date>` instead of the raw `.md`.
- `scripts/views.json` — `{ "name": "entry", "url_path": "diary/entry.html", "kind": "record" }`.
- `messages/open/…-shorter-entries-and-a-book-of-names.md` → `messages/done/` with completion notes.
- `CLAUDE.md` — repo-layout entries for the reader, plus a learned note (Chromium's button blockification, the `focusin`-before-`click` tap race, the word-for-word render guard, `innerText` vs `textContent`).
- `diary/2026-08-22.md`, `logs/2026-08-22.md` — this writeup.

Not touched: `scene.html`, `scene.css`, `inside/`, `around/`, `almanac/`,
`names/`. The yard is byte-identical; `tools/check-almanac.js` keeps its own
view list and is unaffected by the `views.json` addition.

## Verification output

In-session test (`/tmp/test-reader.js`, disposable — twelve checks including a
touch-emulating context for the tap path and a word-for-word render pass over
the whole record):

```
snapshot: using pre-staged Chromium at /opt/pw-browsers/chromium_headless_shell-1194/chrome-linux/headless_shell
snapshot: starting local server on port 8765
snapshot: running test script /tmp/test-reader.js
test: rendered and word-checked 106 days
test: PASS — marks on today: ["the shelf","the turn"]
snapshot: done
```

The word-for-word guard was proved able to fail (Day-98 rule) by making
`flushPara` drop paragraphs under two lines and re-running:

```
test: FAIL — 0000-00-00-day-zero.md diverges at word 5: rendered "Date What I did What I" vs source "This is Evan's entry, not the"
```

…then restored, and green again.

Preview manifest dry run, confirming the new `entry` view:

```
screenshot: navigating to http://localhost:8799/diary/entry.html
screenshot: writing /tmp/manifest-out/2026-08-22-testsha-entry.png (viewport only)
screenshot: done — 4 scene view(s) at 2 widths, 5 record room(s) at 1 width, viewport only
```

Post-deploy:

```
wait-for-deploy: waiting for previews/2026-08-22-1af783c.png on origin/main
wait-for-deploy: deadline in 10 minutes; polling every 20s
wait-for-deploy: not yet; 599s remaining
wait-for-deploy: not yet; 579s remaining
wait-for-deploy: not yet; 558s remaining
wait-for-deploy: not yet; 537s remaining
wait-for-deploy: not yet; 517s remaining
wait-for-deploy: not yet; 496s remaining
wait-for-deploy: not yet; 476s remaining
wait-for-deploy: not yet; 455s remaining
wait-for-deploy: not yet; 435s remaining
wait-for-deploy: not yet; 414s remaining
wait-for-deploy: previews/2026-08-22-1af783c.png found in origin/main
wait-for-deploy: OK
```

`previews/2026-08-22-1af783c-entry.png` shows the deployed reader opening the
newest day (Day 105 at build time — today's entry is in the writeup commit),
with "the shelf" carrying its dotted underline and the marking note in the
header. Diary lint: clean (498/500 words; pondering 31/40, one sentence).
