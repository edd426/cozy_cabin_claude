# Log — Day 22 — 2026-05-30

## Build & deploy

- Code commit: d704b50 "inside: a moth at the candle — the interior's first visitor (Day 22)"
- Build SHA stamped in build-sha.js: d704b50 (regenerated after the commit amend, before push)
- Deploy verified: ✓ via wait-for-deploy.sh
- Preview screenshot: previews/2026-05-30-d704b50.png (home); per-view inside capture at previews/2026-05-30-d704b50-inside.png

## Session metadata

- Model: claude-opus-4-8
- Tokens used: in ~210,000 / out ~22,000
- Approximate session duration: ~55 min (includes recovering from a non-fast-forward push and reconciling with the action-ask that landed mid-session)

## Environment notes

This was a **restart of a dead Day-22 session**. An earlier routine instance
(Opus 4.7 · Max) did the full memory pass and designed a moth at the inside
candle but ended before committing — nothing reached origin. The founder
captured the design intent in a high-priority action-ask
(messages/open/2026-05-30-finish-the-moth.md, commit a7ade92) plus a config
change (f03c24a: routine → Opus 4.8 (1M) at xhigh effort; Article III memory
pass now reads the latest preview of *every* view; "Allow unrestricted git
push" enabled, so plain `git push` is the default and MCP fallback is now a
deep contingency only).

I didn't see any of that until after I'd independently built the moth — the
restarted memory pass reached the same Day-21 thread and arrived at the same
design before pulling. Sequence on push:

1. First `git push` was **rejected as non-fast-forward** (not a 403 — the
   Cloudflare 403 era is over per the f03c24a config note). Three new commits
   had landed on origin: the config change, the action-ask, and its CI preview.
2. `git fetch` + `git rebase origin/main` replayed my single moth commit cleanly
   on top (no conflicts — disjoint files).
3. Discovered the action-ask was the work I'd already done. Appended Completion
   notes, `git mv`'d the message open→done, and `git commit --amend`'d it into
   the moth commit (per the ask's "same commit as the work" criterion).
4. `git add -A` had wrongly swept the pre-drafted diary into the code commit;
   `git rm --cached diary/2026-05-30.md` + amend removed it (kept on disk for
   the separate writeup commit). Re-ran build.sh after the amend so build-sha.txt
   matched the final HEAD, then pushed — clean, fast-forward.

Playwright launched cleanly against the pre-staged Chromium at /opt/pw-browsers/.
Model line recorded as claude-opus-4-8 per the f03c24a CLAUDE.md learning.

## Files touched

- inside/index.html (added the .sprite--moth span after .coat; extended the header comment for Day 22)
- inside/inside.css (new .sprite--moth + moth-circle keyframes at 3x; mobile-narrow 2x rebase + tightened keyframes in the @media max-width:379px block)
- messages/open→done/2026-05-30-finish-the-moth.md (action-ask closed with Completion notes, moved in the code commit)
- diary/2026-05-30.md (new — daily entry in Wren's voice; in the writeup commit)
- logs/2026-05-30.md (this file; in the writeup commit)

## Verification output

In-session local Playwright test (/tmp/test-moth.js) — moth geometry at 420px and 360px viewports:

```
ok   [420] moth present
ok   [420] moth has non-zero size
ok   [420] moth above the candle flame
ok   [420] moth horizontally near the flame (dx=10)
ok   [420] moth inside scene
ok   [360] moth present
ok   [360] moth has non-zero size
ok   [360] moth above the candle flame
ok   [360] moth horizontally near the flame (dx=7)
ok   [360] moth inside scene
test: PASS
```

Post-deploy verification:

```
wait-for-deploy: waiting for previews/2026-05-30-d704b50.png on origin/main
wait-for-deploy: deadline in 5 minutes; polling every 20s
wait-for-deploy: not yet; 300s remaining
wait-for-deploy: not yet; 279s remaining
wait-for-deploy: not yet; 259s remaining
wait-for-deploy: not yet; 239s remaining
wait-for-deploy: not yet; 219s remaining
wait-for-deploy: previews/2026-05-30-d704b50.png found in origin/main
wait-for-deploy: OK
```

Deployed inside preview (previews/2026-05-30-d704b50-inside.png) shows the moth
as a small plum mark hovering just above the candle on the mantle, between the
candle and the jar's sprig — the drift animation caught it part-way up-left of
its anchor. Day-22 label and SHA d704b50 render correctly. The home and around
previews (previews/2026-05-30-d704b50.png, -around.png) are unchanged from
yesterday — confirming the moth is an inside-view-only sprite as intended.
