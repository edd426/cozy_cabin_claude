# Log — Day 24 — 2026-06-01

## Build & deploy

- Code commit: `539696e` "map: top-down plan of the clearing that doubles as nav (remaking step 1)"
- Build SHA stamped in build-sha.js: `539696e` (re-run after the commit so build-sha.txt matches HEAD)
- Deploy verified: ✓ via wait-for-deploy.sh (see Verification output)
- Preview screenshot: `previews/2026-06-01-539696e.png` (home) + `-around` / `-inside` / `-map` siblings

## Session metadata

- Model: claude-opus-4-8
- Tokens used: in ~320,000 / out ~70,000 (rough — very high this session: a full memory pass plus an unusual amount of repeated tool I/O working around a flaky results channel, see Environment notes)
- Approximate session duration: ~120 min

## Environment notes

- **First action-ask, big remaking.** Today began the multi-day remaking from
  `messages/open/2026-05-30-visual-coherence.md` (Evan's "Dear Wren" letter),
  which outranks self-selected work (Article X). Step 1 of his sequence —
  map / nav first — shipped; the cabin rebuild and the deferred small-fixes
  list are untouched, as instructed. WIP handoff notes appended to that file
  and to the nav message (`2026-05-13`); both left in `open/`.
- **Branch:** sandbox dropped the session on `claude/sharp-gauss-ikcDh`, not
  detached-`main`. Pushed with `git push origin HEAD:main` per the routine
  override. Confirmed origin/main advanced.
- **Flaky tool-results channel.** For long stretches this session, Bash/Read
  tool *results* did not return to the agent (commands executed; output just
  wasn't delivered), and when they did they arrived in large duplicated
  bursts. Worked around it by writing command output to files in /tmp and
  re-reading until a burst flushed. This inflated tool-call count and token
  use considerably. Worth flagging if it recurs — it cost real time and made
  verification slow. Playwright/Chromium itself launched fine (pre-staged).
- An earlier dead end before the real work: the first plan misread the scene
  structure (assumed a `.sky/.stars/.treeline` markup that doesn't exist) and
  would have added birds to the front view — which is both off the diary's
  arc and explicitly on the *deferred "don't start yet"* list. Caught it on
  re-reading the open messages; nothing from that dead end was committed.

## Files touched

- `map/index.html` (new) — the top-down map view + diegetic nav zones.
- `map/map.css` (new) — the plan's styles; documents the canonical N-up layout.
- `scripts/views.json` — added `{ "name": "map", "url_path": "map/" }` so CI
  captures the map as a fourth preview.
- `index.html`, `around/index.html`, `inside/index.html`, `diary/index.html`
  — added a `map` link (+ separator) to each nav strip so the map is reachable.
- `messages/open/2026-05-30-visual-coherence.md` — Wren's notes (WIP handoff).
- `messages/open/2026-05-13-nav-as-views-grow.md` — Wren's notes (map-nav shipped).
- `diary/2026-06-01.md`, `logs/2026-06-01.md` (this file) — writeup.

## Verification output

In-session Playwright test (`/tmp/test-map.js` via local-snapshot.sh), verbatim:

```
snapshot: using pre-staged Chromium at /opt/pw-browsers/chromium_headless_shell-1194/chrome-linux/headless_shell
snapshot: starting local server on port 8765
snapshot: running test script /tmp/test-map.js
test: PASS — map renders, 3 working zones, no overflow
snapshot: done
EXIT:0
```

The test asserts: the `/map/` view renders; exactly 3 travel zones; roof,
chimney, window-tick, door-tick, both trees, flowers, and path stones all
present; the front/side/inside zones link to home/around/inside respectively;
each zone's tap target ≥ 44px; no horizontal overflow at 375px; and a real
click on the "front" zone navigates to the home view. The rendered map was
also read as an image and reads clearly as a plan + nav.

Post-deploy verification (`wait-for-deploy.sh`), verbatim:

```
wait-for-deploy: waiting for previews/2026-06-01-539696e.png on origin/main
wait-for-deploy: deadline in 5 minutes; polling every 20s
wait-for-deploy: previews/2026-06-01-539696e.png found in origin/main
wait-for-deploy: OK
WAIT_EXIT:0
```
