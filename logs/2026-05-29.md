# Log — Day 21 — 2026-05-29

## Build & deploy

- Code commit: 726043f "scene: a bird drifts past the smoke — a second visitor, high above (Day 21)"
- Build SHA stamped in build-sha.js: 726043f (regenerated after push; original pre-push stamp was f28792c for the local-snapshot run)
- Deploy verified: ✓ via wait-for-deploy.sh
- Preview screenshot: previews/2026-05-29-726043f.png

## Session metadata

- Model: claude-opus-4-7
- Tokens used: in ~165,000 / out ~14,000
- Approximate session duration: ~40 min

## Environment notes

`git push` succeeded directly on first try — no HTTP 403, third clean push in a
row (Days 19/20/21). The intermittent Cloudflare 403 pattern of earlier days
(2/3/18) is looking resolved for now; MCP fallback remains the documented
backup per CLAUDE.md but has not been needed this week. Playwright launched
cleanly against the pre-staged Chromium at /opt/pw-browsers/.

Both open action-asks (door-or-window, nav-as-views-grow) remain in
messages/open/ in "awaiting the founder's eye" state — neither blocked today's
self-selected contribution per Article X. The bird lives only on the home view
(sky above the chimney); it is ephemeral, like the bee, not architecture, so it
raises no Article XIII cross-view geometry obligation. The chimney is visible
in the around view too, but no bird is rendered there — visitors are home-view
only for now.

**lint-diary.sh / meta-schema mismatch (predates today):** the locked
scripts/lint-diary.sh expects the meta-reflection headings `## What I want to
keep` / `## What I want to drop`, but the locked diary/meta/README.md schema
specifies `## What to keep` / `## What to drop`, and both prior meta entries
(2026-05-15.md and 2026-05-22.md) use the latter. Today's meta follows the
schema and the precedent set by the past entries, so the lint fails by design:

```
lint-diary: diary/meta/2026-05-29.md missing required heading(s):
  - ## What I want to keep
  - ## What I want to drop
```

Noting for founder visibility — both the linter and the schema are locked, so
this is not something the agent can reconcile. Could be fixed by aligning
either one with the other.

## Files touched

- scene.html (added the .sprite--bird span after .sprite--bee)
- scene.css (new .sprite--bird + bird-drift keyframes at desktop scale; mobile-narrow rebased position and tightened keyframes in the @media max-width:379px block)
- diary/2026-05-29.md (new — daily entry in Wren's voice)
- diary/meta/2026-05-29.md (new — Day-21 meta-reflection; day_n % 7 == 0)
- logs/2026-05-29.md (this file)

## Verification output

In-session local Playwright test (/tmp/test-bird.js) — bird geometry assertions:

```
ok   bird present
ok   bird inside scene
ok   bird in upper half of scene
ok   bird above bee
ok   bird overlaps smoke band vertically
test: PASS
```

Post-deploy verification:

```
wait-for-deploy: waiting for previews/2026-05-29-726043f.png on origin/main
wait-for-deploy: deadline in 5 minutes; polling every 20s
wait-for-deploy: not yet; 300s remaining
wait-for-deploy: not yet; 279s remaining
wait-for-deploy: not yet; 259s remaining
wait-for-deploy: not yet; 239s remaining
wait-for-deploy: previews/2026-05-29-726043f.png found in origin/main
wait-for-deploy: OK
```

Deployed home preview (previews/2026-05-29-726043f.png) shows the bird as a
small dark crescent silhouette in the upper-right sky, near the rising smoke
column — the drift animation caught it mid-arc, partway between the right
anchor and the leftward swing toward the chimney. CI also captured around and
inside views (previews/2026-05-29-726043f-around.png, -inside.png), both
unchanged from yesterday — confirming the bird is a home-view-only sprite as
intended.
