# Log — Day 38 — 2026-06-15

## Build & deploy

- Code commit: `321348f` "inside: fire to the floor, brick to the ceiling; migrating birds; side-chimney smoke"
- Build SHA stamped in build-sha.js: `321348f`
- Deploy verified: ✓ via wait-for-deploy.sh
- Preview screenshot: `previews/2026-06-15-321348f.png` (+ `-around`, `-inside`, `-map`, and `-phone` variants)

Writeup commit (diary + log) is the second commit of the day, per the standard two-commit cadence.

## Session metadata

- Model: `claude-opus-4-8[1m]`
- Tokens used: in ~95,000 / out ~22,000
- Approximate session duration: ~55 minutes

## Environment notes

Clean day. Started on the pre-made work branch `claude/sharp-gauss-os2lw9`; reattached to `main` with `git checkout -B main HEAD` per Step 0 before doing anything, and `git push origin main` worked directly (unrestricted-push permission). Playwright launched with the pre-staged Chromium at `/opt/pw-browsers/`. No 403s, no MCP fallback needed.

One verification note worth recording for future sessions: the bird animation was rewritten to a one-directional crossing, which starts off-screen at the 0% keyframe. A static screenshot taken right after load would have caught an empty sky. Fixed with a negative `animation-delay` so the flock begins part-way across (in clear sky) the instant the page loads — confirmed via local snapshot that the skein is on-screen at the load frame.

## Files touched

- `scene.css` — birds: `bird-drift` → `bird-cross` (one-directional migrating pass, both breakpoints) + negative animation-delay.
- `around/index.html` — added the `.sprite--smoke` markup to the side view (reuses the front's smoke component from scene.css unchanged).
- `inside/inside.css` — hearth container spans full scene height; column base lifted 8 native; brick top runs off-frame (no cap), `justify-content:flex-end`; firebox/embers/log/fire lowered 8 native; cloak raised 38→46 native; chair re-seated to 8 native (woodpile depth) with a contact-shadow `::after`. Mantle/items/moth left unchanged.
- `inside/index.html` — brick course count 20 → 44 (over-supplied for the now floor-to-ceiling column); hearth aria-label updated.
- `messages/done/2026-06-14-almost-one-place.md` (+ `-inside.png`) — action-ask completed; Completion notes appended; moved from `open/`.
- `messages/done/2026-05-30-visual-coherence.md` — the remaking thread closed (founder's coherence read in his 06-14 letter + the last cross-view gap now resolved); closing note appended; moved from `open/`.

Both `messages/open/` slots are now empty for the first time in a while.

## Verification output

```
wait-for-deploy: waiting for previews/2026-06-15-321348f.png on origin/main
wait-for-deploy: deadline in 5 minutes; polling every 20s
wait-for-deploy: not yet; 299s remaining
wait-for-deploy: not yet; 279s remaining
wait-for-deploy: not yet; 259s remaining
wait-for-deploy: not yet; 238s remaining
wait-for-deploy: not yet; 218s remaining
wait-for-deploy: not yet; 198s remaining
wait-for-deploy: previews/2026-06-15-321348f.png found in origin/main
wait-for-deploy: OK
```

Deployed previews read as intended at both bands: home shows the V skein crossing open sky right of the chimney (both faces smoking); around shows the side chimney now smoking; inside shows brick floor-to-ceiling, fire low on its coals with floor in front, cloak up on the wall, chair/woodpile set back on the floor plane.
